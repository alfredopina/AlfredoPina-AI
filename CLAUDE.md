# CLAUDE.md — alfredopina.ai

## Sobre este proyecto

Sitio de marca personal de Alfredo Piña (Ingeniero Industrial, instructor/consultor de Excel, Power BI, Power Platform e IA, 15 años de experiencia, cientos de empresas, miles de profesionistas capacitados). El sitio centraliza y automatiza su negocio de capacitación: landing, catálogo de cursos con temario personalizable, calendario de disponibilidad, y a futuro diagnósticos de nivel, cotizador, recursos descargables y un backoffice/CRM.

Al responder o construir, prioriza legibilidad y mantenibilidad sobre cleverness. Pregunta antes de tomar decisiones de arquitectura grandes que no estén ya cubiertas aquí.

Si algún día hay que reconstruir la infraestructura desde cero (o algo se borró por accidente), ver **`RUNBOOK.md`** — checklist accionable de pasos de portal en orden, no la narrativa de decisiones que vive aquí.

## Stack técnico

- **Front-end:** HTML/CSS/JS puro (sin frameworks, sin build step). No usar Power Pages ni Power Platform de bajo nivel para el front — decisión intencional de no depender solo del ecosistema Microsoft para el sitio.
- **Hosting:** Azure Static Web Apps (tier **Free**), recurso `Web-AlfredoPina`, grupo de recursos `GR_AlfredoPina`, región Central US. **URL real de producción:** `https://icy-smoke-071e3ec10.7.azurestaticapps.net/` (ojo con el `.7.` antes de `azurestaticapps.net` — el nombre del workflow de GitHub Actions no lo incluye, es fácil adivinar mal la URL a partir de él, ya pasó una vez).
- **Deploy:** GitHub → GitHub Actions → Azure Static Web Apps, automático al hacer push a `main`. No tocar el workflow YAML autogenerado salvo que se sepa exactamente qué se está cambiando.
- **Backend:** Azure Functions (carpeta `api/`), modelo **v3 clásico** (carpeta + `function.json`), NO modelo v4 (`app.http(...)`) — el v4 falla en Static Web Apps managed functions por una feature flag no configurable en ese hosting. Ya hubo que corregir esto una vez, no repetir el error.
- **Base de datos:** Azure Table Storage para módulos aislados (ej. Calendario). Para el futuro backoffice (que cruza Diagnóstico + Cotizaciones + Recursos), evaluar Azure SQL Database (tier serverless barato) en vez de Table Storage, porque los reportes van a necesitar cruzar datos entre módulos.
- **Repo:** `github.com/alfredopina/AlfredoPina-AI` (público).
- **Dominio:** `alfredopina.ai` **conectado y en producción desde 2026-09-10** — DNS + dominio personalizado dados de alta en el Static Web App (`Web-AlfredoPina` → Custom domains), certificado válido. El correo `@alfredopina.ai` todavía NO existe (solo el sitio) — mientras tanto, `alfredo.pina@lifezen.com.mx` sigue siendo el correo real de contacto (login de `/admin` y `COTIZACION_CONTACT_EMAIL`, ver Fase 3 Cotizaciones en Pendientes).

## Identidad de marca — YA CERRADA, no reabrir sin que Alfredo lo pida explícitamente

**Manual de marca (2026-09-09):** artifact publicado con logo/colores/tipografías/motivo `fx`/tono de voz en un solo lugar visual, pensado para estandarizar documentos nuevos (empezando por el rediseño de Cotizaciones) — `https://claude.ai/code/artifact/f8f8603f-4310-4b79-9b17-bf64cc32670f`. Resume lo que ya está en esta sección, no la reemplaza.

- **Logo:** firma vectorizada de Alfredo (NO un ícono de piña, aunque alguna variable interna del código se llame parecido a "pineapple" — verificar código real antes de asumir, puede ser solo un nombre de variable mal puesto). **Corregido 2026-09-09:** el color real del trazo (`assets/img/firma-ap.png`) es el azul de marca `#3d7fff` — la versión anterior de este documento decía "gris carbón `#3a3a3a`", nunca se había verificado contra el archivo real.
- **Motivo visual insignia:** barra de fórmula estilo Excel (`fx =FUNCIÓN()`) — ya usado en Hero, "Hablemos"/Contacto, y el constructor de temario de Cursos. Reutilizar cuando tenga sentido narrativo en módulos nuevos (ej. Diagnóstico).
- **Paleta por herramienta/curso:** Excel `#22c55e` · Power BI `#f2c94c` · Power Apps `#c026d3` · Power Automate `#06b6d4` · IA Aplicada `#a78bfa` · Ofimática `#f97316`. Azul de marca general `#3d7fff` / `#6b9fff`.
- **Variables de diseño base:** `--bg: #0a0d12`, `--panel: #10141b`. Tipografías: `Space Grotesk` (display/títulos), `Inter` (body), `JetBrains Mono` (elementos tipo código/fórmula).
- **Alfredo tiene deuteranomalía** (daltonismo parcial rojo-verde) — NUNCA codificar información solo por color; siempre acompañar con ícono, texto o posición.
- **Sin emojis en el diseño del sitio** — iconografía SVG de línea (stroke, no relleno).
- **Sin subtítulos largos bajo títulos de sección** — patrón: eyebrow corto + título con gancho, directo al contenido.
- Estilo general: dark/tech, dinámico y moderno, dashboards animados. Tono de marca (copy): directo, práctico, con humor/sarcasmo ligero, sin caer en genérico motivacional.

## Patrón de trabajo establecido: fases

Para módulos que lo permiten (no todos), construir en 2 fases:
1. **Fase 1 — visual/funcional sin backend**, iterar rápido en el navegador
2. **Fase 2 — backend real** (Azure Function + storage), una vez que la UX ya convenció

Excepción: el módulo de Diagnóstico y el futuro backoffice SÍ necesitan backend desde el día uno (no se puede separar), porque el valor central depende de persistir datos ligados a identidad desde el principio.

## Patrón de acceso a páginas privadas/semi-privadas

- **Público pero oculto** (ej. `agenda.html`): sin login, sin liga en el menú de navegación, con meta `noindex`, accesible solo por quien tiene el link directo.
- **Link + código corto** (ej. futuro módulo de Recursos y Diagnóstico): además del link, un código corto que Alfredo comparte manualmente (en clase, por WhatsApp) — no es cuenta de usuario, no se guarda contraseña. Los códigos deben ser autogestionables por Alfredo desde un panel simple, sin que requiera tocar código cada vez que necesita uno nuevo.
- **Login real** (zona de reportes/backoffice, datos personales de terceros): usar la autenticación integrada de Azure Static Web Apps con Microsoft Entra ID, aprovechando la cuenta de M365 que Alfredo ya tiene. No construir sistema de usuarios/contraseñas desde cero.

## Modelo del negocio y roadmap estratégico — definido 2026-09-05, sesión de teorización

Alfredo identificó que, sin planearlo, ya está automatizando 3 roles de su negocio. Este modelo es el norte para ordenar todo lo que falta — antes de agregar un módulo nuevo, ubicarlo en uno de estos 3 pilares:

- **Comercial** (adquirir + cerrar venta): Landing, Cursos (catálogo público), Agenda, Solicitudes, Cotizaciones. Esto es, en la práctica, su CRM.
- **Producto / Catálogo** (plantillas de lo que vende, sin dueño/cliente): Cursos (Temarios), Recursos. Ya construidos.
- **Operación** (instancias de entrega, cada una le pertenece a un Cliente): Cliente (empresa — Alfredo solo vende a empresas, no modelar alumnos sueltos sin empresa), Grupo/Cohorte (instancia de un Temario entregada a un Cliente, con fase: *por arrancar → en curso → proyecto práctico → cerrado*), Alumno (persona, siempre bajo un Cliente), Instructor (Alfredo + 3 instructores externos ocasionales), **Diagnóstico** (nivelar el grupo por conocimiento previo — **corregido 2026-09-10**: la teoría original lo puso en Comercial asumiendo que calificaba leads antes de cotizar; el uso real de Alfredo es casi siempre post-venta, ya con el curso vendido, para dividir el grupo por nivel — a veces sí ayuda a vender, pero es la excepción, no la regla), Diploma, Encuesta.

**Nota importante sobre la fase "proyecto práctico"**: no es trámite administrativo, es una herramienta de venta. ~2 semanas después de terminar el curso, Alfredo hace una sesión de 1-2h donde los alumnos presentan un proyecto práctico que él dejó de tarea; él solo modera/da retro, y RH invita a jefes/decisores. Eso genera prueba social en vivo frente a quien decide la siguiente compra. Cuando se diseñe el Grupo a detalle, esta fase debe capturar más que un status: fecha, quién asistió como invitado, y una nota/resultado reusable como material de venta (cierra el círculo de vuelta hacia Comercial).

**Decisión de infraestructura — Azure SQL para este bloque nuevo, YA CONFIRMADA:** Cursos/Recursos se quedan en Table Storage (funcionan bien, aislados). Cliente/Solicitud/Cotización/Grupo/Alumno/Instructor/Diploma/Encuesta son una cadena genuinamente relacional (reportes cruzados: qué Cliente compró qué, qué Instructor califica mejor, conversión Solicitud→Cotización→Cierre) — ahí Table Storage sin JOINs sería doloroso. A la escala real del negocio (~150 clientes en 15 años, ~5 grupos/mes, ~15 alumnos/grupo) el volumen total es mínimo, una base Azure SQL tier Basic (~$5 USD/mes, 2GB) sobra sin subir de tier.

**Ajustado 2026-09-06 — base NUEVA, no reutilizar la existente:** la decisión original era reutilizar la base Basic que Alfredo ya paga (de un ejercicio viejo de Power BI), pero esa base vive en el grupo de recursos de LifeZen, no en `GR_AlfredoPina`. Se decidió mejor crear una base Azure SQL nueva **dentro de `GR_AlfredoPina`** (misma región Central US, mismo tier Basic ~$5 USD/mes) — así toda la infraestructura de alfredopina.ai queda junta, y si algún día Alfredo limpia/borra recursos viejos de LifeZen, no hay riesgo de tocar accidentalmente data real de negocio (Clientes, Alumnos, Diplomas). Costo mientras conviven ambas bases: ~$10 USD/mes total; Alfredo eventualmente eliminará la vieja (no urgente, es limpieza suya, no bloquea nada). Alfredo crea el servidor+base desde el portal (mismo patrón que ya usó para Storage Account/tablas — él da de alta el recurso, Claude construye sobre la cadena de conexión que resulte) y activa el firewall "Allow Azure services and resources to access this server".

**Fases del roadmap — versión confirmada 2026-09-05 (reemplaza cualquier versión anterior de este documento; la primera propuesta de Claude, ordenada por dependencia técnica, quedó descartada a favor de esta, ordenada por el ciclo de vida real de una venta):**

- **Fase 0** (en curso): reorganizar el sidebar del admin en **4 secciones visuales — Comercial/Producto/Operación/Cierre** (ajustado 2026-09-05; la primera versión de este documento decía 3, sin "Cierre" separado — ya no vigente), y una primera versión de Dashboard con 4 secciones de métricas, solo con tarjetas reales en Producto (las otras 3, placeholder "Próximamente"). Como hoy no existe ningún módulo de Operación todavía (los 8 módulos actuales ya cubren Comercial/Producto/Cierre), la sección Operación del sidebar necesita un ítem placeholder nuevo (tag "PRONTO", mismo patrón que los demás módulos sin construir) solo para reflejar el roadmap. Sin filtros en el Dashboard por ahora — no hay todavía datos transaccionales que valga la pena filtrar, se revisita en Fase 1/2. Ver Historial de sesiones para el prompt de handoff generado. En paralelo, del lado de Alfredo: alimentar contenido real de Cursos (Power Apps/Automate/IA Aplicada/Ofimática, ver "Pendiente" abajo) y mantener la Agenda actualizada.
- **Fase 1 — Comercial — completa en su alcance original (2026-09-10):**
  - 1.1 Solicitudes — construida.
  - 1.2 Cotizaciones — construida (**Cliente nace aquí, no hasta Fase 2**: Alfredo ya tenía clientes reales (empresas), no todos leads nuevos, así que la tabla Cliente existe desde que se puede generar la primera Cotización).
  - ~~1.3 CRM en admin~~ — **cerrada sin construir aparte (2026-09-10):** la idea original (vista que conecta Solicitudes+Cotizaciones+su seguimiento) se resuelve con **Tracking Comercial** — el tablero de Seguimiento que ya se construyó dentro de Cotizaciones, promovido a ítem de sidebar propio en el reacomodo de navegación del 2026-09-10 (ver "Pendientes de Alfredo" y el historial de esa fecha). No hace falta un módulo nuevo.
  - ~~1.4 Diagnósticos~~ — **movido a Fase 2 — Operación (2026-09-10)**, ver abajo. Corrección sobre la teoría original: Alfredo lo usa casi siempre después de vender (nivelar grupo por conocimiento previo), no para calificar leads.
- **Fase 2 — Operación:**
  - ~~2.0 Diagnósticos~~ — **construido 2026-09-10** (diseño de metodología cerrado en sesión de teorización el mismo día, construido en la sesión constructora inmediatamente después — ver "Pendientes de Alfredo" → Diagnósticos y el Historial). En el admin vive agrupado junto a Encuestas/Diplomas (ver reacomodo de navegación del 2026-09-10).
  - 2.1 Clientes (se extiende con Grupos y Alumnos), **Grupos — construido 2026-09-11** (Alta de Grupo + Consultar Grupos, ver "Estado del proyecto" → Grupos y "Pendientes de Alfredo" → Grupos), Alumnos (pendiente evaluar después de Grupos), Instructor (atribución simple — Alfredo + 3 instructores externos ocasionales, sin gestión compleja, reusa `getInstructores` que ya existe de Diplomas)
  - ~~2.2 Tracking de instancias por estatus~~ — **construido 2026-09-11** ("Tracking Operación" en el admin, con stepper de fase — ver "Pendientes de Alfredo" → Grupos y el Historial de esa fecha) — scorecard, alerta de cierre atrasado, tabs y semáforo, tal como se diseñó. **Idea sin construir, sigue pendiente:** cruzar las fechas de los Grupos contra los eventos reales del calendario de Outlook de Alfredo (misma integración de Microsoft Graph que ya usa Agenda) para detectar conflictos de horario al agendar un Grupo nuevo, sin salir del admin.
- **Fase 3 — Cierre — completa (2026-09-07):**
  - ~~3.1 Encuestas~~ — **construida el 2026-09-07** (ver "Estado del proyecto" abajo). Cierra Fase 3 en su orden natural del roadmap.
  - ~~3.2 Diplomas~~ — **adelantada y construida el 2026-09-07** (fuera de orden, por el atraso real de 7-8 grupos sin diploma emitido — ver "Estado del proyecto" abajo y el historial de esa fecha). Primer módulo del proyecto en Azure SQL.
  - **Nota 2026-09-10:** en el sidebar del admin, Encuestas/Diplomas ya no viven bajo una sección llamada "Cierre" — esa etiqueta se reemplazó por "Operación" (el mismo nombre que ya usaba el pilar de arriba), porque en el uso día a día Alfredo los agrupa mentalmente junto con Diagnósticos como una sola cosa: "todo lo que pasa después de vender". La numeración de fases (0/1/2/3) sigue siendo el registro histórico de EN QUÉ ORDEN se construyó cada cosa — no tiene que coincidir 1 a 1 con cómo se agrupan visualmente en el sidebar.

## Pendientes de Alfredo — checklist acumulado (fuera de código, no requieren a Claude)

Se actualiza en cada sesión: se agregan pendientes nuevos, se tachan/quitan los ya resueltos. Es el lugar único para esto — evita que queden dispersos en prosa narrativa por todo el documento.

**Siguiente paso acordado del roadmap (actualizado 2026-09-10):** el reacomodo de navegación del admin quedó completo (Prompt 1 y Prompt 2, ver ambos arriba y el Historial) — Fase 1 Comercial ya no tiene ningún pendiente de construcción. Ambos SQL (`007`/`008`) y la Application Setting `COTIZACION_CONTACT_EMAIL` ya están confirmados corridos/configurados en producción (2026-09-10) — falta solo la confirmación funcional en vivo de los flujos (ver checklists de abajo, todavía sin marcar porque eso sí requiere probar el flujo completo, no solo el dato/variable). El botón "Despertar" + extender `apiGetConReintento` a Diplomas/Encuestas (y a 2 buscadores de Cliente que se habían escapado) ya se construyó también el 2026-09-10 — ver "Deuda técnica" y el Historial. **Diagnóstico también quedó construido el 2026-09-10** (diseño + construcción en la misma sesión, ver "Pendientes de Alfredo" → Diagnósticos y el Historial) — falta correr `sql/009` y cargar el banco real de 30 preguntas. **Grupos (Alta + Consultar) quedó construido el 2026-09-11** (ver Historial de esa fecha), `sql/010_grupos.sql` ya corrido. **Tracking Operación + stepper de fase con historial también quedó construido el mismo día** (segunda sesión, ver Historial), `sql/011_grupo_fase_historial.sql` ya corrido — falta la confirmación funcional en vivo de ambos módulos. Lo único que queda del roadmap estratégico de Fase 2 Operación es Alumnos. Aparte del roadmap (son herramientas personales, no pilares del modelo de negocio): **Pendientes** — panel de captura rápida, diseño cerrado y **construido 2026-09-11**, sin SQL que correr, falta solo confirmar en producción. **Modelo** — visor interactivo del esquema completo del sitio (23 tablas/30 relaciones, con filas+última fecha por tabla vía botón "Actualizar"), **construido 2026-09-11**, sin SQL que correr, falta solo confirmar en producción.

**Grupos — Fase 2 Operación, diseño cerrado y construido 2026-09-11 (Alta de Grupo + Consultar Grupos, y en una segunda sesión el mismo día Tracking Operación + stepper de fase):**
- [x] **Conflicto de nombres resuelto:** el módulo nuevo se llama **Grupos**, no Cursos — el "Cursos" existente (Catálogo/Temarios) se queda tal cual, sin renombrar. Coincide con el término que ya usaba el roadmap desde 2026-09-05 ("Grupo/Cohorte") y con la columna "Gpo" que Alfredo ya usaba en su Excel de control.
- [ ] **Pendiente de resolver, con más peso ahora (no bloquea construir Grupos, pero hay que volver a esto):** la ambigüedad de nombres entre `TemariosEstandar` (Cursos/Catálogo) y `Cursos` (tabla de Recursos, códigos de descarga) — propuesta vieja sin retomar: Temario Estándar→Curso, Curso de Recursos→Ficha de Recursos. Alfredo la pospuso en su momento por falta de ejemplos; ahora que existe un tercer "Cursos" candidato (ya resuelto como "Grupos" arriba) puede ser buen momento para una sesión dedicada solo a esto.
- [x] **Alta de Grupo — diccionario de campos cerrado** (ver "Estado del proyecto" → Grupos para el detalle completo): Cliente + Cliente final (dos FK a `Cliente`, para el caso de reventa/intermediario tipo Capacitanet→Clarios) + Contacto (resuelto contra Cliente final si existe, si no contra Cliente) + Modalidad (mismo enum `Online`/`Presencial`/`Híbrido` que ya usan Solicitud/Cotizacion) + Grupo/código + Herramientas (multi-select) + Nombre del curso (texto libre) + Nivel (multi-select Básico/Intermedio/Avanzado) + Horas/Sesiones + fechas + Instructor (reusa `getInstructores`) + Estatus del curso (Por iniciar/En proceso/Terminado) + Estatus de cierre (vacío/Proyecto/Calificaciones/Diplomas/**Cerrado**, confirmado 2026-09-11 — reemplaza lo que hacía la columna `¿Completo` del Excel viejo) + Cotización de origen (FK opcional) + Fotos RS (checkbox — **Correos ML/Pagado se quitaron 2026-09-11**, ver "Ronda de pulido" en "Estado del proyecto" → Grupos) + Fecha de cierre + Notas.
- [ ] **Pendiente, anotado para evaluar después de Grupos, no ahora:** **Alumno** no existe en ningún lado del sistema — Diagnóstico/Diplomas/Encuestas capturan nombre como texto suelto, sin Alumno real ligado a Cliente ni a Grupo. Roadmap ya lo preveía (Fase 2.1). Evaluar cuando Grupos ya esté construido y probado.
- [x] **Construido 2026-09-11** (mismo día que el diseño, ver Historial) — solo **Alta de Grupo + Consultar Grupos**, sin Tracking Operación todavía (eso se diseña después de probar Grupos, mismo patrón que Tracking Comercial nació de Cotizaciones).
- [x] `sql/010_grupos.sql` corrido en el Query Editor de `apcweb-backoffice` (confirmado 2026-09-11).
- [ ] Confirmar en producción el flujo completo de Grupos (ver checklist detallado en "Estado del proyecto" → Grupos).
- [x] **Ronda de pulido tras la primera prueba real — construida 2026-09-11** (toggle de Cliente final, contacto nuevo para cliente nuevo, dropdown de Herramientas/Nivel, se quitan Correos ML/Pagado — ver "Estado del proyecto" → Grupos, bloque "Ronda de pulido", para el detalle completo).
- [x] `sql/012_grupo_quita_columnas.sql` corrido en el Query Editor de `apcweb-backoffice` (confirmado 2026-09-11) — elimina las columnas `correos_ml`/`pagado` de `Grupo`.
- [x] **Segunda ronda de pulido — construida 2026-09-12** (toggles agrupados al inicio, Sesiones a texto libre, Estatus de cierre bloqueado hasta Terminado, datalist de Nombre del curso — ver "Estado del proyecto" → Grupos, bloque "Segunda ronda de pulido").
- [ ] Correr `sql/013_grupo_sesiones_texto.sql` en el Query Editor de `apcweb-backoffice` (convierte `Grupo.sesiones` de `INT` a texto libre).
- [x] **Ronda de pulido 3 — construida 2026-09-12** (reestructura Consultar Grupos: búsqueda automática sin botones, Filtrar/Ordenar en su propia línea con íconos, columna Fase unificada + Horas, acciones Editar/Mover/Eliminar separadas en pestaña "Modificar Grupo" con regreso al origen correcto, `eliminarGrupo` nuevo — única excepción del proyecto a "nunca borrar de verdad" — y de paso corrige un bug real de Tracking Operación que no se refrescaba al navegar ahí. Ver "Estado del proyecto" → Grupos, bloque "Ronda de pulido 3", para el detalle completo).
- [x] **Tracking Operación + stepper de fase — construido 2026-09-11** (segunda sesión constructora el mismo día, ver "Estado del proyecto" → Tracking Operación y el Historial para el detalle técnico completo, no repetido aquí). Mockup del stepper: `https://claude.ai/code/artifact/fc84f38f-7f8d-4362-bbbd-98d4bd086360`.
- [x] `sql/011_grupo_fase_historial.sql` corrido en el Query Editor de `apcweb-backoffice` (confirmado 2026-09-11).
- [ ] Confirmar en producción el flujo completo de Tracking Operación (ver checklist detallado en "Estado del proyecto" → Tracking Operación).

**Modelo (panel nuevo, fuera del roadmap estratégico) — construido 2026-09-11:**
- [x] **Idea:** Alfredo pidió un visor del modelo de datos completo del sitio dentro del propio `/admin` — nace del mismo diagrama ya publicado como Artifact aparte (`https://claude.ai/code/artifact/64be899d-0c26-43a8-b9a8-7ce1cea32279`), que le recordó a la vista de Modelo de Power BI. Se le confirmó que embeberlo en su repo no tiene costo extra (es HTML/CSS/JS estático, mismo tipo de recurso que cualquier otra página del sitio) y que queda 100% independiente de Claude — corre en su propia Azure sin llamar de vuelta a claude.ai.
- [x] **Ubicación:** ítem suelto del sidebar, justo debajo de Pendientes (mismo criterio: herramienta personal de Alfredo, no un pilar del modelo de negocio) — protegido automáticamente por la regla `/admin/*` ya existente, sin tocar `staticwebapp.config.json` para eso.
- [x] **Contenido:** las 23 tablas del modelo real (Azure SQL + Table Storage) con sus campos, PK/FK, y las 30 relaciones entre ellas — mismo diagrama arrastrable/zoom/buscador/leyenda del Artifact, ahora con IDs/clases `mdl-*` para no chocar con el resto del admin y reusando los tokens de color del sitio en vez de definir los suyos (solo se agregaron 5 variables nuevas, una por "pilar" del diagrama + el color de las relaciones "foto/snapshot").
- [x] **Filas + última fecha por tabla, con botón de actualizar (pedido explícito de Alfredo):** Function nueva `getModeloEstadisticas` — un solo `UNION ALL` contra `apcweb-backoffice` para las 15 tablas SQL (`MAX(fecha_creacion)` o la columna de fecha real de cada una; las que no tienen ninguna columna de fecha en su esquema — Cliente, TarifaHerramienta, Alumno, EncuestaPregunta, EncuestaRespuestaDetalle, DiagnosticoRespuestaDetalle — se listan con NULL explícito, nunca se inventa una fecha) + `listEntities()` sobre las 8 tablas de Table Storage (Azure Table Storage no tiene `COUNT` nativo, hay que recorrer todas las entidades — volumen real mínimo, sin problema). Para Table Storage la "fecha" es el `Timestamp` de sistema (última escritura, no forzosamente creación — ej. archivar un Pendiente lo actualiza) — el front lo etiqueta "última actividad" en vez de "última fecha" para no confundirlo con las fechas reales que sí reportan las tablas SQL.
- [x] **Decisión deliberada, no pedida explícitamente:** las estadísticas NO se cargan solas al abrir el panel — solo al pulsar el ícono de refrescar (reusa `.admin-icon-btn`/`.despertando`, mismo look que el botón "Despertar" del topbar). Mismo criterio ya establecido en el proyecto para Tarifas/Respaldos en Configuración: evita despertar la base SQL serverless o recorrer Table Storage completo solo por entrar a mirar el diagrama.
- [x] Probado con `node --check` en la Function nueva y en el `<script>` completo del admin extraído, más un grep de integridad referencial (23 `id:` en `TABLES`, 30 pares `from/to` en `RELATIONS`, sin huérfanos) — mismo patrón de verificación que ya usa el proyecto para cambios grandes al admin. `npm test` (las 3 suites existentes) sigue en verde.
- [ ] Confirmar en producción — sin ningún script SQL que correr (la Function solo lee, no crea nada nuevo): que el diagrama se vea y se pueda arrastrar/zoom igual que en el Artifact, y que "Actualizar" traiga filas/fechas reales.
- [x] **Decisión 2026-09-11:** Alfredo confirmó que de aquí en adelante solo se mantiene el panel de `/admin` — el Artifact público queda congelado (no se vuelve a tocar cuando se agreguen tablas). Ver detalle en "Estado del proyecto" → Modelo.

**Pendientes (módulo nuevo) — diseño cerrado y construido 2026-09-11:**
- [x] **Idea:** Alfredo anota pendientes/ideas en Excels sueltos o libreta y los termina perdiendo — panel nuevo dentro del admin para captura rápida, sin fricción, ahí donde ya pasa la mayor parte de su tiempo.
- [x] **Nombre y ubicación:** se llama **Pendientes** (no "Notas"/"Task", coincide con el vocabulario que ya se usa en todo este documento). Vive como ítem suelto del sidebar, fuera de los 3 grupos (Comercial/Operación/Productos), justo debajo de Productos — mismo nivel que Dashboard/Tracking Comercial/Tracking Operación, porque no pertenece a ningún pilar del modelo de negocio (de hecho 2 de sus 5 contenedores tampoco).
- [x] **Decisión técnica — Table Storage, no la base SQL:** el punto de esta herramienta es capturar al instante; el auto-pause de la base SQL serverless (20-60s) mataría justo el caso de uso que se busca resolver. Tabla `Pendientes` en la cuenta `apcwebrecursos` ya existente, auto-creada por código (sin script SQL, sin pasos de portal).
- [x] **Diseño cerrado — mockup aprobado tal cual:** `https://claude.ai/code/artifact/39cc5d78-bd6f-4750-9505-975df610c4bd`. 5 contenedores fijos (Comercial/Operación/Productos/Marca Personal/Sitio, no configurables por ahora) → tarjetas de solo texto, editables (clic en el texto abre edición inline) → checkbox que **archiva, nunca borra** (reversible, con link "ver archivados (N)" por contenedor) → botón "+ Agregar" con formulario inline por contenedor.
- [x] **Construido 2026-09-11** (ver Historial de esa fecha) — tabla + 4 Functions + panel completo, probado de punta a punta local. Sin SQL que correr.
- [ ] Confirmar en producción el flujo completo (ver detalle en "Estado del proyecto" → Pendientes).

**Diagnósticos — metodología, diseño de interacción y construcción, todo el 2026-09-10:**
- [x] **Formato cerrado:** deja de ser opción múltiple teórica estilo Google Forms — cada pregunta trae una imagen de referencia real (tabla/hoja) + 4 respuestas, evaluando método (fórmula correcta, no solo el resultado) o criterio (qué gráfica usar), no memorización. Solo Excel y Power BI, 15 preguntas por herramienta (5 Básico + 5 Intermedio + 5 Avanzado), banco editable/borrable desde el admin.
- [x] **Mockups explorados**: [3 tipos de contenido de pregunta](https://claude.ai/code/artifact/48688dc0-d7f7-4921-b3ef-37ec698b81d4) (versión inicial del artifact, ya reemplazada) → [6 mecánicas de interacción](https://claude.ai/code/artifact/48688dc0-d7f7-4921-b3ef-37ec698b81d4) (celda activa, riel de datos, pastillas, validación de datos, slicer Power BI, lista de campos).
- [x] **Decisión final de interacción — combinación de celda activa + lista de campos:** cada respuesta es una fila con su letra de referencia (A/B/C/D, del diseño "celda activa") + un checkbox con palomita a la derecha (del diseño "lista de campos", sin la manija de arrastre decorativa). **Regla no negociable:** el estado seleccionado usa un color neutro (azul de marca, no el verde/amarillo de la herramienta) y **nunca** se colorea en verde/rojo ni se confirma correcto/incorrecto en ningún momento — ni al seleccionar, ni al enviar, ni en pantalla de agradecimiento — porque eso dejaría "pescar" la respuesta correcta a prueba y error.
- [x] **Arquitectura acordada:** mismo patrón de Encuestas — Azure SQL (`apcweb-backoffice`), 3 tablas (`DiagnosticoPregunta`/`DiagnosticoRespuesta`/`DiagnosticoRespuestaDetalle`, `pregunta_id` sin FK igual que Encuestas, pero con `fue_correcta` guardado como foto al momento de responder para que editar una pregunta después no reescriba resultados históricos), imágenes en Blob público (`apcwebrecursos` → contenedor nuevo `diagnostico`), admin de 3 pestañas (Preguntas/QR y Link/Resultados, calcado de Encuestas), página pública nueva `diagnostico.html` (oculta, sin login, un solo archivo con `?h=excel|powerbi`, 2 QR distintos desde el admin). **Resultados = datos crudos** (aciertos por nivel + detalle de las 15 respuestas) — el nivel final/reporte interpretativo es una 2ª fase que arma Alfredo aparte, fuera de este alcance.
- [x] **Construido 2026-09-10** (ver Historial de esa fecha) — esquema + 9 Functions + admin (3 pestañas) + `diagnostico.html`, en una sola fase. Falta correr `sql/009_diagnostico.sql` y cargar el banco real de 30 preguntas (15 Excel + 15 Power BI) con sus imágenes de referencia — ver "Pendientes de Alfredo" → Diagnósticos.

**Reacomodo de navegación del admin (sesión de teorización 2026-09-10 → construido el mismo día, ver Historial — 2 prompts separados para no chocar con la sesión constructora del PDF de pdfmake):**
- [x] **Prompt 1 — sidebar + extracción de Tracking — construido:** el tablero de Seguimiento de Cotizaciones se movió a un ítem propio "Tracking Comercial" (con el punto que pulsa, `.admin-nav-live-dot` — mismo criterio visual que `.float-chip .dot` del sitio público), "Tracking Operación" quedó como placeholder nuevo (reemplaza el "Tracking" viejo sin contenido), Clientes se movió de "Operación" a "Comercial" (junto a Solicitudes/Cotizaciones), la sección se renombró "Cierre"→"Operación" y ahí se movió Diagnósticos, y "Producto"→"Productos". Detalle completo en "Estado del proyecto" → Cotizaciones/admin.
- [x] **Prompt 2 — pestañas Consultar/Crear — construido (2026-09-10, ver Historial):** Clientes, Solicitudes y Cotizaciones reescritos al mismo patrón de 2 pestañas que ya usa Diplomas ("Consultar X" / "Crear X" — en Cotizaciones son "Crear Cotización"/"Ver Cotizaciones"). Clientes: Consultar quedó 100% solo lectura (nombre/código/antigüedad/notas/Contactos, sin ningún botón de editar ahí), un solo botón "Editar" manda a Crear Cliente con todo precargado (título dinámico "Editando Cliente: {nombre}"), Contactos se gestionan solo ahí. Solicitudes: mismo patrón create/edit unificado en una sola función (`abrirFormSolicitud(sol)`), Cliente queda de solo lectura en modo edición (mismo criterio que el backend `editarSolicitud`), nueva acción "Editar" (lápiz) por fila, y los 4 campos (`participantes`/`modalidad`/`ciudad_sede`/`fecha_tentativa`) se agregaron al desglose expandible en vez de columnas nuevas. Cotizaciones: nueva pestaña "Ver Cotizaciones" (filtros Cliente/Estatus/fechas, carga perezosa al abrir la pestaña) — "Recotizar" reusa tal cual la función `precargarFormDesdeCotizacion` que ya usaba "Nueva versión" desde Tracking Comercial, sin duplicar lógica. `listCotizacionesAdmin` ganó el parámetro `?estatus=` (exacto, para navegar cualquier estatus individual — ya aceptaba `clienteId`/`desde`/`hasta`, no hizo falta tocar nada más). Separador `.scorecard-block` (Clientes/Solicitudes) — Cotizaciones no lo necesita, su scorecard vive en Tracking Comercial desde el Prompt 1.

**Fase 3 — Cotizaciones, rediseño del PDF a pdfmake (sesión 2026-09-10, se suma al rediseño de flujo del 2026-09-09 de abajo):**
- [x] Correr `sql/008_cotizacion_datos.sql` en el Query Editor de `apcweb-backoffice` (agrega `fecha_tentativa`/`ciudad_sede`/`participantes`/`modalidad` a `Cotizacion`, mismos 4 campos que ya tiene `Solicitud`) — **corrido y confirmado 2026-09-10.**
- [x] **Decisión de diseño cerrada:** el PDF de Cotización deja `pdf-lib` + plantilla de fondo (PNG que Alfredo tenía que diseñar y subir) por **`pdfmake`** — genera el documento completo desde código, sin imagen de fondo ni coordenadas x/y a mano, y pagina solo si el temario es largo. **La pestaña "Plantilla" de Cotizaciones desaparece por completo** — ya no hay nada que diseñar ni subir para este módulo. Sigue sin Puppeteer/navegador.
- [x] **Manual de marca publicado** (logo real color azul `#3d7fff` — no gris carbón como se documentó antes, corregido — más colores/tipografías/tono de voz): `https://claude.ai/code/artifact/f8f8603f-4310-4b79-9b17-bf64cc32670f`.
- [x] **Diseño del documento aprobado** tras 3 rondas de mockup (dark/tech extremo → descartado por impráctico para imprimir/archivar en un contexto B2B tradicional → punto medio: papel claro con tono/acento de color, cuadrícula sutil, data-bars estilo Excel, franja/resplandor de color por herramienta): `https://claude.ai/code/artifact/95e92c06-bf83-41df-8aa9-34b8fdbcea19`. 3 páginas: Portada → Temario (con data-bars proporcionales a horas) → Cierre (precio+6 términos completos+bio real de Alfredo tomada de index.html, NO de LifeZen+foto+contacto).
- [x] **Construido** (sesión constructora 2026-09-10, ver Historial): `api/src/cotizacion-pdf.js` reescrito sobre pdfmake, fuentes Space Grotesk/JetBrains Mono agregadas a `api/assets/fonts/`, `firma-ap.png`/`alfredo-work-photo.png` copiadas a `api/assets/img/`, las 3 Functions de plantilla eliminadas (código+rutas+contenedor Blob), pestaña Plantilla quitada del admin. Probado localmente con 6 combinaciones de herramienta + temario corto/largo, ver detalle completo en "Estado del proyecto" → Cotizaciones.
- [x] **Ronda 2 — pulido visual hecho directo por Claude (2026-09-10, no vía prompt):** a Alfredo no le convenció el primer resultado (franja a rayas, "glow" como sticker plano, sin cuadrícula, mucho vacío en un temario Personalizado/corto). En vez de escribir otro prompt para la sesión constructora, se editó `cotizacion-pdf.js` directamente en esta sesión: franja de acento de 24 pasos de interpolación (antes 3), "glow" con 6 círculos concéntricos de fuerza creciente (simula el difuminado que pdfmake no tiene nativo), cuadrícula de fondo agregada de verdad vía el callback `background` de pdfmake, y un margen superior fijo cuando falta Objetivo/Dirigido a o el temario es corto (≤3 temas) para que el vacío no se sienta "roto". Probado generando 2 PDF reales (caso corto sin Objetivo/Dirigido a, caso largo KEMET/IA con 6 temas) y viéndolos de verdad en el navegador (Chrome renderiza PDF nativo) — confirmado visualmente que se ve notoriamente mejor.
- [x] **Ronda 3 — el "glow" circular se reemplaza por un acento de cuadrados anidados (mismo día):** Alfredo lo siguió sintiendo fuera de lugar — el resto del documento es rectilíneo (pills, tabla, marcas de esquina, cuadrícula) y el círculo era la única forma geométrica distinta, no un problema de calidad del efecto en sí. Se cambió `glow()` por `acentoEsquina()`: mismo truco de contornos concéntricos de fuerza creciente, pero con cuadrados de esquina redondeada (solo contorno, sin relleno) en vez de círculos — consistente con el resto del lenguaje visual. Antes de mandarlo, se probó también la primera versión de este cambio con el acento posicionado casi fuera de la página — quedó prácticamente invisible; se corrigió acercando el centro y subiendo grosor/intensidad de línea. Confirmado visualmente en el navegador con los mismos 2 casos de prueba. **Lección de esta ronda completa (2-3):** cuando algo no convence visualmente, vale más verlo renderizado de verdad (generar el PDF y abrirlo) antes de mandarlo — la primera versión del acento cuadrado se sentía bien en el código pero era invisible en el resultado real.
- [x] **Ronda 4 — el acento de cuadrados se quita por completo (mismo día):** Alfredo lo probó y esa esquina se sintió confusa/saturada, no era el geometría lo que fallaba sino tener un tercer elemento decorativo ahí. `acentoEsquina()` se eliminó del todo (función y llamada) — la portada se queda solo con la franja de acento superior, las marcas de esquina y la cuadrícula de fondo como lenguaje decorativo. Menos es más en esta zona.
- [ ] **Idea explorada y estacionada (no descartada, no urgente):** construir la Cotización como página HTML real del sitio (con los datos ya resueltos) e imprimirla a PDF desde un navegador de verdad (Alfredo con Ctrl+P, o un script local con Chrome/Edge en modo `--print-to-pdf` corriendo en su propia compu, nunca en la Function) — evita por completo el riesgo de meter un navegador headless dentro de las Functions administradas, porque el navegador que imprime corre fuera de Azure. Le daría fidelidad 100% igual al mockup (blur y degradados reales). El costo real: deja de ser 100% automático — hoy el PDF se genera y se guarda solo al crear la cotización, con esto alguien (Alfredo o un script en su equipo) tendría que generar el PDF manualmente cada vez, y ya no funcionaría igual desde el celular. Se decidió NO construirlo todavía — primero se agotó la ruta de pulir pdfmake (rondas 2 y 3 arriba), que sí resultó suficiente. Retomar esta idea solo si en el futuro el resultado de pdfmake deja de convencer de verdad.
- [x] **Correo de contacto real:** el código ya lee `COTIZACION_CONTACT_EMAIL` desde una Application Setting (nunca hardcodeado) — **configurada en Azure (Web-AlfredoPina → Variables de entorno) y confirmada 2026-09-10** con `alfredo.pina@lifezen.com.mx` mientras no exista un correo `@alfredopina.ai`.
- [ ] Confirmar en producción, ya con `sql/008` y la variable listos: el flujo rediseñado de Crear Cotización (Lista/Detalle/Editar, siempre nace de una Solicitud), el PDF nuevo generando bien con datos reales (folio real, cliente real), y el resto del checklist ya validado (10% de descuento en grupos chicos, "Nueva versión"→Reemplazada, los 3 botones de Seguimiento).

**Fase 2 — Solicitudes (sesión 2026-09-09):**
- [x] Correr `sql/007_solicitud_datos_cotizacion.sql` en el Query Editor de `apcweb-backoffice` (agrega `fecha_tentativa`/`ciudad_sede`/`participantes`/`modalidad` a `Solicitud`) — **corrido y confirmado 2026-09-10.**
- [ ] Confirmar en producción: dar de alta una Solicitud con cliente nuevo, otra con cliente existente, una con Temario Estándar y otra Personalizada (revisando que el desglose de temas con descripción se guarde en ambos casos), agregar un contacto nuevo desde el mini-formulario inline, cambiar el estatus de una solicitud desde la tabla, probar los 3 botones de orden (Fecha/Cliente/Horas) y revisar que el scorecard (Manuales/Sitio/% Atendidas) muestre números reales.

**Fase 1 — Clientes/Contactos/Tarifas (sesión 2026-09-08):**
- [x] Correr `sql/005_comercial.sql` en el Query Editor de `apcweb-backoffice` (agrega `notas` a `Cliente`, crea `Contacto`/`Solicitud`/`TarifaHerramienta`/`Cotizacion` — Solicitud/Cotizacion quedan sin Functions todavía, es solo esquema adelantado).
- [x] Correr `sql/006_comercial_ajustes.sql` (ajustes de la ronda de feedback del 2026-09-09: agrega `planta` a `Contacto` y `cliente_desde` a `Cliente`) — **confirmado corrido 2026-09-11** vía el script de validación de los 11 SQL.
- [ ] Confirmar en producción: dar de alta un Cliente real con año de antigüedad, agregarle 2 contactos (uno con planta), marcar uno principal (y que el otro pierda la marca), editar notas, probar los 3 botones de orden (Nombre/Última cotización/Antigüedad) y buscar por nombre de un contacto, revisar que el scorecard de arriba (Clientes/Contactos/% Activos) muestre números reales, y ajustar las 6 Tarifas por hora (Configuración → Tarifas por hora → Desplegar) a los precios reales — hoy están sembradas en $1,200 de ejemplo, y ahora piden confirmación antes de guardar.

**Urgente / de esta sesión (auditorías de seguridad/resiliencia/calidad, 2026-09-08):**
- [x] Correr `sql/004_encuesta_constraint.sql` en el Query Editor de `apcweb-backoffice` — **esta casilla estaba marcada por error desde 2026-09-08, nunca se había corrido de verdad; detectado y corregido 2026-09-11** con un script de validación que confirmó tablas/columnas/constraints de los 11 scripts SQL del proyecto contra la base real.
- [x] Confirmar en producción que el lote de correcciones no rompió nada — Alfredo confirmó frontend/admin bien y probó el bloqueo de `getRecursos` manualmente, funcionó.
- [x] Respaldo automático: secreto `BACKUP_CRON_SECRET` generado y configurado en Azure + GitHub, workflow corrido a mano con éxito.
- [x] Confirmado: el respaldo del workflow ya se ve etiquetado "Automático" — módulo de Respaldos cerrado de punta a punta.
- [x] Activados soft-delete de blobs (30 días) + soft-delete de contenedores (7 días) + versionado de blobs (mantener todas las versiones) en `apcwebrecursos`.
- [x] Confirmado: PITR de `apcweb-backoffice` = 7 días (copia cada 12h) — límite fijo de las bases con oferta gratuita + auto-pausa, no se puede subir sin cambiar de tier.
- [x] **Acceso de Viridiana — resuelto y confirmado en producción 2026-09-11, rol `admin` funcionando** (confirmado tanto por ella entrando a `/admin` como por la sección Actividad de Configuración, que ya la muestra con su propio conteo de accesos junto al de Alfredo — primera prueba real de esa función con 2 administradores de verdad). Su correo `viridiana.garza@lifezen.com.mx` (Google Workspace) **ya estaba dado de alta como usuario dentro del mismo tenant de Microsoft 365 de LifeZen donde vive el login de Alfredo** (con licencia gratuita "Microsoft Fabric (Free)", irrelevante para esto) — no hizo falta crear nada nuevo, solo resetear su contraseña (Microsoft 365 admin center → Users → Active users → Viridiana Garza → Reset password) para que pudiera completar su primer login y aceptar la invitación al rol `admin` de `Web-AlfredoPina`. **Corrige el supuesto que se manejó al inicio de esta conversación:** un dominio ya "reclamado" dentro de un tenant M365 (como `lifezen.com.mx`, porque Alfredo tiene licencia ahí) NO permite crear una cuenta Microsoft personal libre con ese mismo dominio — cualquier correo de ese dominio solo puede autenticarse como usuario real DENTRO de ese tenant específico; si el usuario ya existe pero nunca inició sesión, el fix es resetear su contraseña desde el admin center, no intentar "crear una cuenta Microsoft" desde cero. Ver el detalle completo en "Ya resueltos".
- [ ] **Pendiente, opcional:** Rol **Lector** en `GR_AlfredoPina` (Azure IAM) — para que Viridiana también vea la infraestructura en el portal de Azure, no solo `/admin`. No es necesario para su uso normal del panel, solo si algún día ayuda con temas de portal.
- [x] Alerta de Cost Management configurada (enfocada al recurso SQL, no a todo el grupo de recursos, para que un umbral bajo tenga sentido).
- [x] Node.js instalado en el equipo de Alfredo (2026-09-08) — `package-lock.json` regenerado de verdad, workflow cambiado a `npm ci`, y las 3 suites de prueba corridas por primera vez en el proyecto (encontraron y corrigieron un bug real de timezone en `test-availability.js`, ver "Ya resueltos"). Desde ahora, cualquier sesión de Claude Code en esta máquina puede correr pruebas reales en vez de solo leer código.
- [ ] Decidir qué hacer con las 6 vulnerabilidades moderadas que `npm audit` encontró (transitivas, vía `mssql`→`tedious`→`@azure/identity` y vía `@azure/data-tables`→`uuid`) — el fix requiere `mssql@12.7.1` (cambio mayor, riesgo de romper Diplomas/Encuestas). Recomendación pendiente de confirmar contigo, ver el mensaje de esta sesión.

**Contenido:**
- [ ] Completar el contenido de Recursos (materiales por curso) en las herramientas que aún falten.
- [ ] Terminar de cargar Power Apps y cargar Power Automate/IA Aplicada/Ofimática desde cero (admin → Cursos).
- [ ] Reemplazar los 11 textos de pregunta de ejemplo de Encuestas por los definitivos (admin → Encuestas → Preguntas).
- [ ] Subir la plantilla rediseñada de Diplomas (marca alfredopina.ai, no LifeZen) usando la guía de coordenadas, y procesar el backlog real de 7-8 grupos sin diploma.
- [ ] Mostrar el QR de Encuestas en la próxima sesión de cierre real para probar el flujo completo.

**Marketing / marca (menor prioridad):**
- [ ] Imagen social dedicada (1200×630) para `og:image` — hoy usa `firma-ap.png` como placeholder.
- [x] `og:url` ya apuntaba a `alfredopina.ai` desde antes (se configuró preventivamente) — confirmado que sigue correcto ahora que el dominio está conectado de verdad (2026-09-10), no requirió cambio de código.
- [ ] Link real de YouTube en el footer (hoy `href="#"` placeholder).

**Deuda técnica (no es tarea tuya en portal — para una sesión constructora cuando quieras invertir en limpieza):**
- [ ] Separar el CSS/JS que sigue inline en `cursos.html` (~670 líneas de CSS + ~500 de JS de 1,292 totales) hacia `/assets/` — no bloquea nada, mencionado varias veces en auditorías/revisiones sin resolverse.
- [x] Extender el reintento con pausas crecientes (`apiGetConReintento`, agregado 2026-09-09 a Clientes/Solicitudes/Tarifas) al resto de secciones del admin que consultan la base SQL — **construido 2026-09-10** en Diplomas y Encuestas (más 2 buscadores de Cliente que se habían escapado en Solicitudes y "Ver Cotizaciones", ver Historial de esa fecha). Páginas públicas nuevas que se conecten a esa misma base a futuro siguen debiendo este mismo tratamiento.
- [x] **Botón "Despertar" en el topbar** — **construido 2026-09-10** (ver Historial): ícono junto a Configuración/Notificaciones, `api/despertarBackoffice` (SELECT 1, sin reusar ninguna Function de negocio), 3 estados con ícono propio cada uno (nunca solo color). **Ajuste el mismo día** (ver Historial): reintento automático de 1 vez si el primer intento truena — Alfredo reportó que el primer clic a veces daba tachita roja pero un segundo clic inmediato sí funcionaba (el timeout de conexión de 60s a veces gana la carrera contra el arranque real de la base, que sigue despertando de fondo aunque el cliente se rinda) — ya no hace falta el segundo clic a mano.
- [ ] **Analítica de tráfico del sitio público** — pedida por Alfredo 2026-09-10, explícitamente pospuesta ("me interesa pero no ahorita, déjalo en pendientes"). Azure Static Web Apps (plan Free) no trae nada de esto integrado. 3 caminos evaluados, sin decidir todavía: (1) contador propio (tabla + Functions chicas, vista dentro del mismo admin, mismo patrón que Encuestas/Diagnóstico), (2) servicio externo gratuito tipo GoatCounter (cero código propio, dashboard aparte), (3) Application Insights de Azure (conecta directo al recurso, reportes en el portal de Azure, no en el admin). Retomar cuando Alfredo lo pida — no adivinar cuál eligió.

**Consideración estratégica, sin acción inmediata:** el correo `alfredo.pina@lifezen.com.mx` (dominio de otra empresa) sostiene todo el login de `/admin` — riesgo de bus factor a tener en el radar, no urgente.

## Estado del proyecto (puede desactualizarse — confirmar contra el repo real)

**Publicado y en producción:**
- `index.html` — landing con Hero, Stats, Experiencia, Certificaciones, Clientes, Herramientas (6 tarjetas enlazando a cursos.html), Metodología
- `cursos.html` — constructor de temario interactivo por categoría, con la barra de fórmula animada, código de color por herramienta. 100% data-driven desde 2026-09-05 (ver sección Cursos abajo) — ya no hay nada hardcodeado aquí, ni siquiera para Excel
- `agenda.html` + `api/getAvailability/` — calendario de disponibilidad conectado a Outlook real vía Microsoft Graph, funcionando en producción

**En construcción — Backoffice, base de autenticación (arranque de Fase 2):**
- `staticwebapp.config.json` (nuevo, raíz del repo) — protege `/admin` y `/admin/*` exigiendo el rol `admin`. Si alguien sin ese rol entra, Azure lo manda a `/.auth/login/aad` (login de Microsoft) automáticamente.
- `admin/index.html` — shell del panel: menú lateral, agrupado visualmente reflejando el roadmap (ver "Modelo del negocio y roadmap estratégico"). **Snapshot histórico de Fase 0 (2026-09-05), YA NO VIGENTE — ver el reacomodo de navegación del 2026-09-10 en el Historial para la estructura real de hoy** (Tracking Comercial/Operación como ítems propios arriba del todo, luego COMERCIAL/OPERACIÓN/PRODUCTOS). En Fase 0 era: **COMERCIAL** (Solicitudes, Cotizaciones, Diagnósticos), **OPERACIÓN** (Clientes, **Tracking** — mapa de qué cursos está dando Alfredo y en qué estatus, ambos placeholder nuevos sin módulo detrás todavía), **CIERRE** (Encuestas, Diplomas), **PRODUCTO** (Cursos, Recursos). Todos "Próximamente" salvo Cursos/Recursos (funcionales) y Dashboard — esa parte (qué módulos ya tienen contenido real) sí se mantuvo vigente mucho más tiempo que el agrupamiento visual.
- **Topbar (v1.3, 2026-09-06):** de izquierda a derecha — campana de **Notificaciones** (`btnNotificaciones`, abre `panel-notificaciones`, placeholder "próximamente", el contenido real llegará cuando existan Solicitudes/Diagnósticos), engrane de **Configuración** (`btnConfig`, abre `panel-config`), switch de tema tipo píldora (`.admin-theme-switch` — sol/luna con contraste real: el ícono del tema activo se pinta con `var(--text)`/naranja `#f5a623`, el inactivo queda en `var(--text-dim)`, nunca ambos igual de apagados como en la v1.2 original), separador, avatar "AP" + nombre "Alfredo Piña" (fijo) + correo real (`/.auth/me`), ícono de logout. Tanto Configuración como Notificaciones viven **fuera del sidebar** (solo se llega por sus íconos) y por eso cada una trae su propio botón **"← Volver"** (`configBack`/`notifBack`) que regresa a `ultimoPanel` — la variable que `activarPanel(key)` actualiza cada vez que se entra a un panel que no es config/notificaciones. Sin este botón, v1.2 dejaba a Alfredo sin salida de Configuración (bug reportado y corregido el mismo día).
- **Panel Configuración (`panel-config`):** shell con 4 secciones placeholder — Perfil, Notificaciones (preferencias, no la campana), Accesos, Plantillas — cada una "PRONTO" con nota de que se diseña más adelante en el proyecto, cerca del cierre. Idioma descartado (sitio 100% en español). **Snapshot histórico (v1.3, 2026-09-06), YA NO VIGENTE en cuanto a Accesos/Tarifas/Respaldos — ver el rediseño a acordeón del 2026-09-10 más abajo** (Plantillas ya se eliminó del todo, ver Cotizaciones/pdfmake; Accesos se reemplazó por Actividad, con contenido real).
- **Tema claro:** tinte azul sutil en `--bg`/`--panel-2`/`--line` en vez de gris plano — a Alfredo le gustó tal cual quedó.
- **Dashboard — Productos completo (v1.3, 2026-09-06):** pestañas estilo "hoja de Excel" (`.dash-etab`, mismo look que `.excel-tab` de `recursos.html`), pestaña "Producto" renombrada a **Productos**, en el mismo orden que el sidebar (Comercial, Operación, Cierre, Productos). Contenido de Productos, de arriba a abajo:
  1. 4 tarjetas KPI (`.dkpi-card`, tooltip nativo `title`): Herramientas (6, fijo), Cursos (temarios estándar publicados), Temas (temas publicados), Proyectos (proyectos publicados) — todo de `getCatalogoCursosAdmin`.
  2. Gráfico de barras por herramienta (`.dbar-*`) con cursos publicados, de mayor a menor, animado al entrar a la pestaña (carga perezosa: los datos se piden una sola vez y se cachean, la animación se repite en cada visita).
  3. **6 anillos de % de completitud** (`.dring-card`, SVG con `stroke-dashoffset` animado) — colores exactos elegidos por Alfredo por su deuteranomalía: rojo oscuro `#b91c1c` si <50%, amarillo `#facc15` si 50–99%, verde `#4ade80` solo si 100%, gris neutro si la herramienta no tiene ningún curso publicado (evita leer "sin datos" como si fuera "mal"); el % siempre va en texto blanco al centro del anillo, el color nunca es la única señal.
  4. **Drill-down**: clic en un anillo o en su fila del gráfico de barras (ambos disparan lo mismo, `toggleDrilldown(toolId)`) expande debajo la lista de cursos de esa herramienta — nombre, temas, proyecto(s) que desbloquea (puede ser más de uno) y si tiene manual cargado, con un check verde en los completos. Clic de nuevo cierra.
  Todo sale de `getCatalogoCursosAdmin` + la Function nueva `getResumenCompletitudAdmin`.
- **Vínculo Cursos↔Recursos — construido (v1.3):** el Curso de Recursos (tabla `Cursos`, la del código de descarga) ahora tiene un campo `temarioId` opcional que apunta al `rowKey` de un Temario Estándar de la misma herramienta (`api/crearCurso`, `api/adminListCursos`). En el admin de Recursos, el form de edición de un curso trae un **dropdown "Vincular con Temario Estándar"** poblado con los temarios *publicados* de esa herramienta (por nombre, no por id — Alfredo no tiene que escribir ni hacer coincidir nada a mano). La Function `getResumenCompletitudAdmin` (v3 clásico, protegida) cruza `TemariosEstandar`+`Proyectos` (módulo Cursos) con `Cursos`+`Recursos` (módulo Recursos) vía ese campo.
- **Regla de "curso completo" — simplificada el mismo día (v1.3.1):** la primera versión exigía ≥3 temas + ≥1 proyecto desbloqueado + ≥1 recurso en cada una de las 5 categorías. Alfredo la simplificó horas después de verla: en la práctica un curso solo lleva **un** manual, así que la regla real es **completo = tiene ≥1 recurso tipo `manual` cargado** — punto. Temas y proyectos se siguen calculando y se muestran en el drill-down como info de contexto, pero ya no determinan el check verde. Importante: esto significa que un curso **no necesita estar vinculado a un Temario Estándar** para contar como completo (el manual se cuenta por `herramienta_cursoId`, no por el temario) — el vínculo sigue sirviendo para mostrar temas/proyecto en el drill-down, pero ya no es requisito de completitud.
- **Pendiente, mencionado pero no retomado:** la propuesta de renombrar "Temario Estándar"→"Curso" y "Curso" (Recursos)→"Ficha de Recursos" para quitar la ambigüedad de nombres — Alfredo no la entendió bien y prefirió posponerla, retomar con más ejemplos. Posible pestaña nueva de Dashboard a futuro: **Bitácora** (log de qué se publicó/cambió y quién — cobra sentido con 2 administradores).
- **Cómo se le da el rol `admin` a Alfredo — IMPORTANTE, no repetir el error:** se intentó primero con una Azure Function (`api/GetRoles`, `rolesSource` en `staticwebapp.config.json`) que asignaba el rol automáticamente por correo. **Esa función requiere el plan Standard (de paga) de Azure Static Web Apps y tumbó todo el sitio en producción** al desplegarse en el plan Free que se usa aquí (error: `The 'auth' configuration in staticwebapp.config.json is only supported on the Standard SKU`). Se revirtió y se borró `api/GetRoles/`. La alternativa que sí funciona en Free es manual: Alfredo genera una **liga de invitación** desde el portal de Azure (recurso `Web-AlfredoPina` → "Role management" → "Invite"), eligiendo proveedor "Azure Active Directory" y rol `admin`, y la abre una vez iniciando sesión con su cuenta de M365 — eso asocia su cuenta con el rol de forma permanente, sin tocar código. Si en el futuro se necesitan más admins o roles dinámicos por correo, ahí sí se justificaría subir a Standard.
- **Confirmado funcionando en producción** (2026-09-04): Alfredo generó su invitación de rol desde el portal (Role management → Invite → Azure Active Directory → rol `admin`), la aceptó iniciando sesión con `alfredo.pina@lifezen.com.mx`, y entrar a `/admin` ya lo reconoce y lo deja pasar. El candado real solo se puede probar así, desplegado — local solo sirve para revisar el shell visual.

**Publicado y en producción — Recursos, backend real completo (lectura + escritura):**
- Primer módulo del sitio con una **Storage Account de Azure de verdad** — no existía ninguna antes (Agenda usa un link .ics, no Storage). Cuenta creada: **`apcwebrecursos`**, grupo `GR_AlfredoPina`, región Central US, Standard/LRS, tipo StorageV2. Contenedor Blob `recursos` (nivel de acceso "Blob" — acceso anónimo de solo lectura por archivo, no se puede listar el contenedor completo; requirió habilitar "Permitir el acceso anónimo en contenedores individuales" a nivel cuenta, que no viene activado por default). Dos tablas: `Cursos` (PartitionKey=herramienta, RowKey=curso id, propiedades `nombre`/`codigo`/`estado`/`orden`) y `Recursos` (PartitionKey=`<herramienta>_<cursoId>`, propiedades `tipo`/`titulo`/`texto`/`url`/`blobPath`/`orden`). La cadena de conexión vive en la Application Setting `RECURSOS_STORAGE_CONNECTION` del Static Web App (en el portal nuevo esto vive bajo **"Variables de entorno"**, ya no bajo "Configuración") — nunca en el repo, mismo patrón que `OUTLOOK_ICS_URL`. La misma connection string sirve tanto para Table como para Blob Storage (`api/src/recursos-tables.js` expone `getCursosTable`, `getRecursosTable` y `getRecursosContainer`).
- **Lectura (pública):** `api/getRecursos` recibe herramienta+curso+código por query string, valida contra `Cursos` (código Y `estado === "publicado"`) y si coincide regresa los recursos de `Recursos` agrupados por tipo. `api/getCatalogoRecursos` regresa, para las 6 herramientas, la lista de cursos publicados (solo id+nombre) — `recursos.html` ya no hardcodea qué herramientas/cursos existen, arma el nivel 1 (grid) y el nivel 2 (pestañas) dinámicamente con esta llamada al cargar, y solo muestra "Próximamente" en las herramientas sin ningún curso publicado.
- **Escritura (protegida, rol `admin`):** `api/crearCurso` (upsert de un curso — también sirve para "rotar código", es guardar el mismo curso con un código distinto, no hay Function aparte), `api/eliminarCurso`, `api/uploadRecurso` (sube archivo a Blob + fila en `Recursos`), `api/editarRecurso` (upsert de Skills/Contenido Complementario, que no llevan archivo, y edición de título/texto de los que sí sin tocar el archivo), `api/eliminarRecurso`, `api/updateOrden` (genérico — reordena `Cursos` o `Recursos` según qué tabla se le mande, para el drag & drop de pestañas y de recursos), `api/listCursosAdmin` (lista todos los cursos de una herramienta sin importar estado, con código visible) y `api/getRecursosAdmin` (recursos de un curso sin agrupar y con `rowKey`, sin exigir código ni que esté publicado).
- `admin/index.html` → sección Recursos: espejo del sitio público en 3 niveles (Herramientas → pestañas de curso con indicador de estado, ícono+texto nunca solo color por la deuteranomalía de Alfredo, con drag & drop de orden → 5 secciones de recursos con alta/edición inline/borrado/reordenar). SortableJS (CDN) para el drag & drop. Incluye switch de tema claro/oscuro (recordado en `localStorage`, default oscuro) — se agregó porque el contraste original de las etiquetas (`--text-faint`) no se leía bien.
- **Nuevo campo `estado`** (`borrador`/`publicado`) en `Cursos` — un curso nace en borrador y no es alcanzable desde el sitio público hasta que Alfredo lo publica desde el admin (el switch **no** exige que ya tenga recursos cargados — si lo publicas vacío, cualquiera con el código ve las 5 secciones en blanco). Se agregó también `orden` (curso, para el orden de pestañas) junto al `orden` (recurso) que ya existía.
- **Confirmado funcionando de punta a punta en producción** (2026-09-04/05): Alfredo subió el manual real (~15MB) del curso `excel-bi` desde el admin sin problema — con esto queda cerrada la duda de si las Functions "administradas" de Static Web Apps aceptan archivos de ese tamaño (sí, mandados como base64 dentro de JSON, ver bug de abajo).
- **Límite real de tamaño de archivo, ya probado:** Alfredo confirmó por prueba directa que archivos de hasta **~21MB suben bien** y **~22MB+ ya no dejan** subir por `uploadRecurso`. Cuadra con la teoría: base64 infla el archivo ~33%, así que 21MB de archivo real son ~28MB de cuerpo JSON — el límite real parece estar en el tamaño del *request* (~28-30MB), no en el archivo en sí, probablemente un límite del proxy/edge de Static Web Apps más que de la Function. **Regla práctica: mientras el archivo pese menos de ~20MB, se sube sin problema.** Si algún día Alfredo necesita subir algo más pesado, ahí sí se justifica cambiar a subir directo a Blob con un SAS token (bypassa esta Function y su límite de cuerpo).
- **TODO — cosmético:** el nombre de prueba quedó como "Basico-Intermedio" sin acento en el curso `excel`/`excel-bi` (código `EXCEL2026`) — corregirlo desde el admin junto con publicarlo cuando Alfredo termine de cargarle contenido real.
- **Nombre de archivo al descargar:** `uploadRecurso` manda `blobContentDisposition` al subir (`attachment; filename="..."`, con variante `filename*=UTF-8''...` para acentos/ñ). **Esto solo no bastó** — Azure Blob Storage solo regresa `Content-Disposition` en la respuesta si la petición trae un `x-ms-version` moderno; las peticiones anónimas normales (como la de cualquier navegador al hacer clic en "Descargar") no mandan ese header y Azure las atiende con una versión de API de **2009**, de antes de que existiera esa propiedad, así que la omitía por completo — se veía como si el fix no sirviera, aunque el blob sí tenía el nombre bien guardado. Se corrigió configurando `DefaultServiceVersion: 2021-08-06` en la Storage Account `apcwebrecursos` (`az storage account blob-service-properties update --account-name apcwebrecursos --resource-group GR_AlfredoPina --default-service-version 2021-08-06`, corrido por Alfredo desde Azure Cloud Shell) — con eso, las peticiones sin versión especificada usan ese default en vez de 2009, y `Content-Disposition` ya se ve en cualquier descarga normal, **incluyendo archivos ya subidos antes del fix** (es una propiedad de la cuenta, no algo que haya que rehacer por archivo).
- **`sessionStorage` de `recursos.html` puede mostrar datos viejos:** una vez que alguien desbloquea un curso en una pestaña, esa pestaña guarda la respuesta de `getRecursos` y no la vuelve a pedir (para no repetir el código al cambiar de tab). Si Alfredo agrega/edita recursos desde el admin mientras esa misma pestaña sigue abierta y ya desbloqueada, no va a ver los cambios hasta cerrar y reabrir la pestaña (o borrar datos del sitio) — no es un bug, es el trade-off de esa decisión de diseño.

**Publicado y en producción — Cursos (Temarios), backend real completo (lectura + escritura):**
- Módulo independiente de Recursos a propósito — no comparte tablas ni referencia cruzada con la tabla `Cursos` de Recursos (código de descarga), aunque conceptualmente ambos usan la palabra "curso". Reusa el mismo Storage Account (`apcwebrecursos`, misma Application Setting `RECURSOS_STORAGE_CONNECTION`) solo por infraestructura, no por dato compartido.
- **3 tablas nuevas**, PartitionKey=herramienta en las 3: `Temas` (RowKey=temaId slug; `nombre`/`descripcion`/`horas`/`nivel` 1-3=Básico/Intermedio/Avanzado/`estado`/`orden` — banco compartido usado tanto por el constructor "Personalizar Temario" como por los Temarios Estándar), `TemariosEstandar` (RowKey=temarioId; `nombre`/`objetivo`/`dirigido`/`tags` opcional coma-separado/`temaIds` JSON-array ordenado/`estado`/`orden` — horas totales y rango de nivel tipo "Básico · Intermedio" se calculan al vuelo, no se guardan), `Proyectos` (RowKey=proyectoId; `nombre`/`objetivo`/`temaIds` JSON-array de temas requeridos/`estado`/`orden`). **A diferencia de `Cursos`/`Recursos` (creadas a mano en el portal), estas 3 se auto-crean solas la primera vez que se escribe** (`tableClient.createTable()` en `api/src/cursos-tables.js`, ignorando el error 409 si ya existe) — no hizo falta que Alfredo tocara el portal de Azure para este módulo.
- **"Proyecto"** reemplaza el viejo concepto difuso de "caso práctico" (que antes vivía como texto suelto pegado a un tema): ahora es una entidad propia con nombre + objetivo + lista de temas que requiere. Se "desbloquea"/muestra únicamente cuando **TODOS** sus temas requeridos están cubiertos (seleccionados en el constructor, o incluidos en un Temario Estándar) — no basta con uno solo. En el constructor personalizado, el bloque de Proyectos activos solo se calcula una vez que la selección llega al mínimo cotizable (12 hr / 3 temas, mismo umbral `MIN_HOURS` de siempre) — si una herramienta tiene pocos temas cargados todavía, es normal que un Proyecto no aparezca aunque sus temas ya estén seleccionados, hasta llegar a ese mínimo.
- **12 Functions nuevas** (v3 clásico, mismo patrón `authLevel:"anonymous"` + protección real vía `staticwebapp.config.json` que ya usaba Recursos): `getCatalogoCursos` (pública, `?herramienta=` → temas/estándar/proyectos publicados, con temas de un Temario Estándar resueltos sin importar su propio estado individual) y 11 protegidas — `getCatalogoCursosAdmin` (conteos para el grid del admin), `listTemasAdmin`/`crearTema`/`eliminarTema`, `listTemariosAdmin`/`crearTemario`/`eliminarTemario`, `listProyectosAdmin`/`crearProyecto`/`eliminarProyecto`, `updateOrdenCursos` (genérico, copia de `updateOrden` pero **separado a propósito** para no acoplar con Recursos). `eliminarTema` bloquea con error explícito si el tema sigue referenciado por algún Temario o Proyecto. Ninguna ruta empieza con "admin" (ver bug ya conocido en Recursos).
- `admin/index.html` → sección Cursos nueva (antes de Recursos en el sidebar): grid de 6 herramientas → 2 tabs por herramienta, "Temarios Estándar" (tabs por temario + form grande con selector de temas, mismo patrón visual que los cursos de Recursos) y "Personalizar Temario" (dos sub-secciones con filas + form inline: Temas y Proyectos). Reusa las clases CSS `.radm-*` de Recursos (genéricas, sin nada específico de ese módulo) pero con su **propio IIFE de JS e IDs propios prefijo `cadm`** — los helpers (`apiGet`/`apiPostJson`/`toast`/`TOOL_META`) están duplicados a propósito, viven encapsulados en el IIFE de cada sección y no vale la pena acoplarlos.
- **`cursos.html` público**: los 6 `curso-panel` hardcodeados se reemplazaron por un solo mount point (`#cursoPanel`) que hace fetch a `/api/getCatalogoCursos` la primera vez que se activa cada herramienta (con cache en memoria). El color de acento (`--c`/`--c-bright`, con `color-mix()`) ahora sigue la paleta de marca de la herramienta activa en vez de estar fijo en verde-Excel como antes. Sin nada publicado en una herramienta, cae automáticamente en la tarjeta "PRÓXIMAMENTE" ya existente. El "ref" decorativo tipo celda de Excel (`B2`, `C3`...) del constructor se calcula en el cliente agrupando por nivel — ya no es un dato capturado a mano.
- **Migración del contenido real** (Excel completo: 9 temas + 2 temarios + 3 proyectos; Power BI completo: 10 temas + 2 temarios + 10 proyectos) hecha pegando un script en la consola del navegador estando logueado en `/admin` — usa la cookie de sesión ya autenticada, sin pedir ningún connection string. Approach reutilizable para futuras migraciones. El **nivel** (Básico/Intermedio/Avanzado) de cada tema de Power BI fue inferido por Claude a partir del badge de rango que ya traía cada temario (el dato original no era granular por sesión) — Alfredo lo revisó y lo dejó así.
- **Secuencia de deploy usada** (para replicar en módulos futuros de riesgo similar): 1) push de backend + admin + `staticwebapp.config.json`, dejando `cursos.html` público sin tocar; 2) verificar en producción con datos de prueba; 3) migrar el contenido real vía consola; 4) confirmar en el admin; 5) recién ahí, commit aparte del cutover de `cursos.html` a data-driven. Así nunca hubo una ventana en la que el sitio público perdiera el temario real de Excel/Power BI.
- **Confirmado funcionando de punta a punta en producción** (2026-09-05): Alfredo ya cargó contenido real de Power Apps (2 temas + 1 temario + 1 proyecto) desde el admin nuevo, además de la migración de Excel/Power BI — verificado que el Proyecto se desbloquea solo en el Temario Estándar y en el constructor personalizado.

**Publicado y en producción — Diplomas, backend real completo (Fase 0-3 del módulo, todo en una sesión el 2026-09-07):**
- Primer módulo del proyecto en **Azure SQL** — base nueva `apcweb-backoffice` en servidor `apcweb-sql-server`, dentro de `GR_AlfredoPina`, tier **serverless con la oferta gratis de Azure** (100,000 seg. de vCore + 32GB/mes gratis de por vida de la suscripción, "Auto-pause the database until next month" activado para no arriesgar cargos). Se auto-pausa por inactividad — el retraso exacto **no está confirmado ni es configurable** (2026-09-09: Alfredo revisó "Proceso y almacenamiento" en el portal y no existe ahí un control de minutos de pausa; lo único visible es el comportamiento al agotar el cupo mensual, que es un concepto distinto — Alfredo observó pausas reales a los 20-30 min, más corto de lo que se asumía originalmente "~1h"). La primera consulta después de una pausa tarda en "despertarla", por eso `api/src/backoffice-db.js` usa `connectionTimeout`/`requestTimeout` de 60s (el default de 15s de `mssql` daba timeout real), y el admin (Clientes/Solicitudes/Tarifas) más `encuesta.html` ya tienen reintento automático con pausas crecientes para esto. Usuario admin del servidor no puede ser algo genérico tipo "admin" (Azure lo bloquea por seguridad). Cliente/Alumno/Diploma nacen aquí como semilla del esquema completo — cuando se construya Cotizaciones/Grupos (Fase 1/2), se EXTIENDEN estas mismas tablas, no se crean nuevas (script `sql/001_backoffice_diplomas.sql`, más `sql/002_diploma_horas.sql` para la columna `horas` que faltó en el diseño original).
- Cadena de conexión en la Application Setting `BACKOFFICE_SQL_CONNECTION`, formato simple `Server=...;Database=...;User Id=...;Password=...` que `backoffice-db.js` parsea a mano (no depende del parser de cadenas de `mssql`, más robusto entre versiones).
- **Folio**: `AP_{HERRAMIENTA}_{código cliente}_{AA}-{consecutivo}` — el consecutivo es por Cliente+Año sin importar herramienta, calculado leyendo los folios ya usados de ese cliente (no hay columna de contador separada). "No Aprobado" consume folio pero no genera PDF.
- **Functions** (v3 clásico, protegidas rol admin, ninguna con nombre que empiece "admin"): `buscarClientes`, `generarDiplomas` (genera el lote completo — alta de Cliente/Alumno, folio, PDF, sube a blob, inserta fila; un error por alumno no tumba el resto del lote), `getDiplomaPdf` (streaming desde el blob privado, `Content-Disposition: inline` para previsualizar en vez de forzar descarga), `listDiplomas` (filtros: alumno/cliente/herramienta/curso/fechas/resultado/estatus), `anularDiploma` (cambia estatus, nunca borra — la corrección real es generar un diploma nuevo desde "Crear Diplomas", opcionalmente anotando el folio viejo en `corrige_a_folio`, campo que existe en la tabla pero **todavía no está conectado a la UI**).
- **PDF con pdf-lib** (`api/src/diploma-pdf.js`) — NO Puppeteer/navegador headless, no es confiable en managed functions. Página del mismo tamaño en puntos que la plantilla en píxeles (hoy 1437x1078) para usar las coordenadas del PNG directo sin conversión. Texto dinámico con **fuente Inter real** (`api/assets/fonts/`, vía `@pdf-lib/fontkit` — `pdfDoc.registerFontkit(fontkit)` hay que llamarlo en **cada** `PDFDocument` nuevo que use fuentes custom, se les olvidó una vez en `generarGuiaCoordenadas` y tronó). Posición/tamaño de cada campo dinámico vive en un arreglo `CAMPOS` compartido entre `generarDiplomaPdf` y `generarGuiaCoordenadas`, para que nunca se desalineen.
- **Plantilla y firmas — NO son archivos fijos del repo, viven en Blob Storage** (contenedor privado `plantillas`, mismo Storage Account `apcwebrecursos`): `fondo.png` (el diseño de fondo) y `firmas/{slug-instructor}.png` (una por instructor). `generarDiplomas` las descarga UNA vez por lote (no por alumno). Si un instructor no tiene firma cargada, el diploma se genera igual, solo sin firma — no bloquea. Formato requerido de la plantilla: PNG horizontal, 1437×1078px exactos (otra medida se estira), sin texto de datos (eso se dibuja encima), sí puede traer decoración/logo/texto fijo que nunca cambia (ej. "REG STPS").
- **Instructores — lista dinámica**, ya no hardcodeada: vive en `instructores.json` dentro del mismo contenedor `plantillas`, con 2 nombres de semilla ("Ing. Alfredo Piña", "Lic. Sergio Moreno") si el archivo todavía no existe. Functions `getInstructores`, `agregarInstructor` (nombre + firma opcional en un solo paso), `eliminarInstructor` (borra de la lista y su firma; diplomas ya generados con ese nombre no cambian), `eliminarFirma` (borra solo la firma, el instructor se queda).
- `admin/index.html` → sección **Diplomas**, 3 pestañas:
  1. **Crear Diplomas** — formulario de lote (Instructor y Nivel son `<select>`, Herramienta también, resto texto/número/fecha, todos obligatorios marcados con `*`) + grid pegable desde Excel (Ctrl+V reparte solo, orden real de la hoja de Alfredo: Nombre, Participación, Asistencia, Proyecto, Resultado; Enter en cualquier celda baja de fila y la crea si hace falta; Resultado también tiene `<datalist>` para capturar a mano sin pegar) + resultados con botón "Ver" (preview inline) + "Descargar todo (.zip)" armado en el navegador con JSZip, nombre `Diplomas_{código}_{herramienta}_{consecutivo min}_{max}`.
  2. **Consultar Diplomas** — filtros (alumno/empresa/herramienta/curso/fechas/resultado/estatus) + botones Buscar/Limpiar filtros/Ver todos (ícono+texto+tooltip) + tabla con ver PDF y anular.
  3. **Plantilla** (singular) — 3 pasos numerados en tarjetas: (1) plantilla de fondo con vista previa + notas de formato + botón "Descargar guía de coordenadas" (PDF con la plantilla actual y un recuadro punteado/etiqueta en cada punto donde se dibuja un dato — para usar de referencia al diseñar una plantilla nueva en Canva/PowerPoint, avisa que marca el *punto central*, no una caja de tamaño fijo, porque varios campos se centran solos según el largo del texto), (2) firmas por instructor con "+ Agregar instructor" y basura para eliminar instructor/eliminar solo firma, (3) generar PDF de prueba con datos ficticios (no toca la base ni consume folio).
- **Autocompletado de Empresa** — componente reutilizado (`crearAutocomplete`) en Crear Diplomas y en el filtro de Consultar: escribes, sugiere clientes existentes por nombre/código; si no hay match, deja capturar nombre+código nuevo (el código es el alias corto que arma el folio, ej. "ACME" — se bloquea el campo si eliges un cliente existente, se habilita si es nuevo).
- **Bug propio corregido, no de plataforma:** un `onerror` en la vista previa del fondo **reemplazaba** el `<img>` por un `<span>` cuando no había plantilla, perdiendo su `id` — el botón de "Subir plantilla" ya no lo encontraba después y tronaba `Cannot set properties of null`. Patrón correcto: ocultar con `hidden`, nunca reemplazar/quitar del DOM un elemento al que se le va a seguir apuntando por id.
- Ícono nativo del `<input type="date">` es invisible en tema oscuro por default — se corrige con `input[type=date]::-webkit-calendar-picker-indicator{ filter:invert(1) }`.
- **Pendiente para Alfredo (no requiere a Claude):** subir la plantilla nueva (rediseño con marca alfredopina.ai en vez de LifeZen) usando la guía de coordenadas como referencia, y cargar el backlog real de 7-8 grupos sin diploma emitido.

**Publicado y en producción — Encuestas, backend real completo (Fase 3.1, construida el 2026-09-07):**
- Reusa por completo la infraestructura que dejó Diplomas — misma base `apcweb-backoffice`, mismo `api/src/backoffice-db.js`, mismos timeouts de 60s por el auto-pause. **3 tablas nuevas** (`sql/003_encuestas.sql`): `EncuestaPregunta` (banco de preguntas, `seccion` fija `"Instructor"`/`"Curso y Materiales"`, `tipo` `"escala"`/`"texto"`, `orden`, `activa`), `EncuestaRespuesta` (cabecera — nombre opcional, `cliente_id` FK a `Cliente`, curso, instructor, fecha) y `EncuestaRespuestaDetalle` (una fila por pregunta respondida). Sembrada con 10 preguntas de escala (5 por sección) + 1 de texto ("Comentarios para tu instructor") con textos de ejemplo — **Alfredo los ajusta desde el admin → Encuestas → Preguntas en cuanto tenga los definitivos**, no hace falta tocar el script otra vez.
- **`pregunta_id` en `EncuestaRespuestaDetalle` es una referencia SIN FK a propósito** (documentado en el script): `eliminarPregunta` hace un borrado real, y las respuestas históricas que ya usaron esa pregunta se conservan tal cual — con FK, borrar una pregunta usada fallaría o (con CASCADE) se llevaría el historial, ninguna de las dos es lo que se quiere. `listRespuestasEncuesta` hace LEFT JOIN y muestra `(pregunta eliminada)` cuando ya no la encuentra.
- **Decisión de arquitectura tomada sin pausar a preguntar (documentar bien porque toca seguridad):** `buscarClientes` y `getInstructores` nacieron protegidas (rol `admin`) para Diplomas. Encuestas necesita reusarlas tal cual desde `encuesta.html`, que es una página **pública sin login** (mismo patrón "oculta" que `agenda.html`) — protegidas, esa página nunca las hubiera podido llamar (redirect a login de Microsoft). Se optó por **quitarlas de `staticwebapp.config.json`** (se volvieron públicas) en vez de duplicar Functions, porque el dato que exponen (nombre/código de cliente; nombre de instructor) ya es de baja sensibilidad y, en el caso de instructores, literalmente ya sale impreso en cada diploma. Si en algún momento eso deja de ser aceptable, la alternativa es duplicar una versión pública separada en vez de volver a proteger las que ya usa el admin.
- **8 Functions nuevas** (v3 clásico): `getPreguntasEncuesta` (pública, preguntas activas ordenadas, para `encuesta.html`), `listPreguntasAdmin`/`crearPregunta`/`eliminarPregunta`/`reordenarPreguntas` (protegidas, banco de preguntas), `enviarRespuesta` (pública, dentro de una `sql.Transaction` inserta cabecera + detalle completos o nada; da de alta el Cliente si no existe, mismo patrón `resolverCliente` que `generarDiplomas` pero **duplicado a propósito** en vez de compartido, mismo criterio que ya usa el proyecto), `listRespuestasEncuesta` (protegida, agrupa filas planas en una respuesta por cabecera con su detalle resuelto) y `getContadorRespuestas` (protegida, `COUNT(*)` simple).
- **Sin protección anti-duplicados/anti-spam en `enviarRespuesta`** — intencional: el link se comparte por QR en vivo durante la sesión de cierre, no es un link público difundido.
- `encuesta.html` — página pública oculta (sin login, sin liga en el menú, meta `noindex`, igual que `agenda.html`), con nav y footer **completos** del sitio (Conóceme/Cursos/Hablemos + el footer grande "Yo invito el café" con fórmula de contacto) — no es una página huérfana, cualquiera puede seguir explorando el sitio desde ahí. Trazabilidad (Nombre, Empresa con autocompletado — sin campo de código visible, se deriva del nombre al vuelo si la empresa es nueva, ver `codigoDesdeNombre`, Curso, Instructor, Fecha) + preguntas agrupadas por sección (**Curso y Materiales primero, Instructor después** — orden de despliegue fijo en el front, independiente del `orden` que se administra por sección en el admin) + la pregunta de texto pegada al final del bloque Instructor, sin repetir título + línea de privacidad + botón de envío con la barra `fx =ENVIAR_ENCUESTA()` (mismo lenguaje visual que `=AGENDAR()`/`=CALIFICAR_INSTRUCTOR()`) + pantalla de agradecimiento con check animado y un `fx =GRACIAS()` de cierre.
  - **Control de escala** (`.xcell` — data-bar de Excel): además de tocar una zona, ahora es un slider real — `pointerdown` fija el valor de inmediato y `pointermove` mientras se arrastra lo va actualizando (con soporte de teclado, flechas ← →, vía `role="slider"`). Número+palabra siempre visibles, nunca solo el color, por la deuteranomalía de Alfredo. **Bug real encontrado y corregido probando el drag**: `setPointerCapture` puede tronar `NotFoundError` según el dispositivo — como no estaba en un try/catch, eso abortaba el resto del handler y la calificación ni se registraba. Se corrigió llamando `setValue()` **antes** de intentar `setPointerCapture` (envuelto en try/catch, ya no es crítico si falla).
- `admin/index.html` → sección **Encuestas** (le quita el tag "PRONTO" al sidebar), 3 pestañas:
  1. **Preguntas** — nota fija arriba explicando qué hace cada control (Activa/Inactiva = soft, no pierde historial; lápiz = editar; bote de basura = borrado real con confirmación), dos listas fijas (Instructor / Curso y Materiales), drag & drop de orden (`reordenarPreguntas`, con un offset de `orden` reservado por sección — 0-999 Instructor, 1000+ Curso y Materiales — para que nunca se entrelacen), alta/edición inline, switch Activa/Inactiva. **El botón "+ Agregar" se deshabilita mientras carga la lista** — sin ese candado, agregar una pregunta antes de que el fetch inicial terminara (la base puede tardar hasta 60s en despertar) calculaba mal el `orden` contra un banco todavía vacío; bug real encontrado y corregido antes de dar el módulo por bueno.
  2. **QR / Link** — reencuadrado en dos tarjetas tipo `.dip-paso` (mismo lenguaje visual que la pestaña Plantilla de Diplomas) en vez de la grilla suelta original: "Link y QR" (link + copiar, QR con `qr-code-styling` centrado, botones Ver encuesta/Descargar QR/Descargar tarjeta) y "Respuestas en vivo" (2 tarjetas KPI — **total** y **de hoy**, esta última útil para ver cuántos del grupo ya la llenaron durante la sesión; "hoy" se calcula contra `fecha_envio` en UTC, puede haber un desfase de horas cerca de medianoche por la diferencia con hora de México, aceptable para un contador de referencia). La tarjeta imprimible sigue dibujada con **Canvas 2D nativo** (no una librería de captura de HTML), reusando el mismo QR ya generado en la pestaña.
     - **El contador se auto-actualiza cada 20s mientras esa pestaña esté realmente a la vista** (más un botón ↻ para forzar de inmediato) — pero con un candado importante: el `setInterval` sigue vivo de fondo, pero cada tick se autofiltra en `cargarContador()` contra 3 condiciones (`encQrTabActiva`, el panel Encuestas siga siendo el activo, `!document.hidden`) y no hace ninguna llamada de red si alguna falla. Sin este candado, dejar el admin abierto en otra sección mantendría la base SQL serverless despierta sin necesidad (cada consulta le resetea el auto-pause), gastando su cupo gratis de vCore-segundos/mes más rápido de lo que conviene — encontrado y corregido a partir de una pregunta directa de Alfredo sobre el costo del polling, antes de subir el cambio. Probado end-to-end simulando el paso del tiempo: confirma llamadas mientras la pestaña QR está activa, cero llamadas nuevas al cambiar a Preguntas aunque pasen 20s+, y retoma al volver a QR.
  3. **Resultados** — filtros (empresa/curso/instructor/fechas) + botones Buscar/Limpiar filtros/Ver todos (mismo patrón que Consultar Diplomas) + tabla con **columnas dinámicas**: una por cada texto de pregunta distinto que aparezca en el resultado filtrado, sin acciones ni PDF — es solo consulta de datos crudos.
- Probado de punta a punta en local sirviendo el repo con un servidor HTTP estático temporal (PowerShell `HttpListener`, esta máquina no tiene Node) + `window.fetch` mockeado en consola, en dos rondas (construcción inicial y la ronda de ajustes de UX): envío completo de encuesta con empresa nueva y con empresa existente, validación de "falta calificar" antes de dejar enviar, el slider de escala con eventos `PointerEvent` sintéticos, alta/edición/borrado de preguntas con reflejo inmediato en la lista, pestaña QR (la librería externa sí carga y dibuja bien) y tarjeta imprimible (capturado el canvas antes de descartarlo para confirmar visualmente que coincide con el mockup), Resultados armando columnas correctas, y el candado de polling del contador (`document.hidden` siempre da `true` en este navegador de pruebas automatizado — se sobreescribió con `Object.defineProperty` solo para poder probar las otras 2 condiciones del candado).
- **Nav estandarizado (2026-09-08):** `agenda.html`, `recursos.html` y `encuesta.html` ahora muestran los mismos 3 links (Conóceme / Cursos / Recursos) — `agenda.html` no traía "Recursos" (quedó pendiente de cuando se agregó ese link a `index.html`/`cursos.html`, nunca se hizo el mismo ajuste ahí); `agenda.html` y `encuesta.html` además muestran su propio badge de "página oculta" (Agenda / Encuesta respectivamente), `recursos.html` no necesita badge porque sí está enlazada en el menú principal.
- **`encuesta.html` sin footer de contacto** — a pedido de Alfredo ("podría distraer aquí"), se quitó la sección "Yo invito el café"/fórmula de contacto; queda solo un footer mínimo con el copyright. El botón "Hablemos" del nav apunta a `index.html#contacto` en vez de un ancla local.
- **Reintento automático al cargar la encuesta** — `pedirDatosEncuesta()` en `encuesta.html`: si `getPreguntasEncuesta`/`getInstructores` fallan (típicamente porque la base SQL serverless estaba dormida y el primer intento se cae por timeout antes de que termine de despertar), reintenta solo hasta 3 veces con pausas crecientes (4s, 8s, 15s) antes de mostrarle a quien esté contestando el "no se pudo cargar" — cubre con margen el peor caso documentado de ~60s de auto-pause. Probado end-to-end: con reintentos exitosos y con el camino de agotar los 3 (confirmado que sí muestra el error final y no se cuelga, esperando ~40s reales).
- **Tip operativo para Alfredo (no requiere a Claude):** la pestaña QR/Link del admin ya dispara una consulta real a la base al abrirla (el contador de "Respuestas en vivo") — abrirla uno o dos minutos antes de proyectar el QR al grupo (o darle clic al ↻ apenas se entra) ya la deja despierta para cuando la gente escanee, sin necesidad de nada adicional.
- **Pendiente para Alfredo (no requiere a Claude):** reemplazar los 11 textos de pregunta de ejemplo por los definitivos desde el admin → Encuestas → Preguntas, y mostrar el QR en la próxima sesión de cierre para probar el flujo real.

**Publicado y en producción — Respaldos automáticos de Table Storage (2026-09-08, hallazgo Alto de la auditoría de resiliencia):**
- Cursos/Recursos/Temas/TemariosEstandar/Proyectos no tenían ningún mecanismo de backup — Table Storage no ofrece soft-delete ni point-in-time restore, así que esto es la única red de seguridad real contra un borrado o bug que corrompa el catálogo.
- `api/src/backup-tables.js` (`generarRespaldo(origen)`) lee las 5 tablas completas y sube un JSON a un contenedor Blob **privado** nuevo `respaldos` (mismo Storage Account `apcwebrecursos`, se crea solo con `createIfNotExists`, sin pasos de portal). Nombre del blob: `{YYYY-MM-DD}_{HHmm}_{origen}.json`, hora de México fija (mismo cálculo de offset -6 que ya usa `availability-logic.js` para Agenda, reutilizado, no reinventado).
- **El disparador automático NO es un timer de Azure Functions — no funciona en este hosting** (ver "Ya resueltos": Static Web Apps managed functions solo soportan `httpTrigger`). `api/respaldoAutomatico` es una Function HTTP **pública** (sin rol admin, sin entrada en `staticwebapp.config.json`) protegida por un secreto compartido en el header `x-backup-secret` contra la Application Setting `BACKUP_CRON_SECRET`; la dispara `.github/workflows/respaldo-semanal.yml` (cron lunes 06:00 UTC = 00:00 México, más `workflow_dispatch` para correrlo a mano desde la pestaña Actions). `respaldarAhora` (protegida, admin) sigue siendo el manual desde el botón del panel; `listRespaldos`/`getRespaldo` (protegidas) alimentan la lista y la descarga.
- Admin → Configuración → **Respaldos** (ya no es "PRONTO"): botón "Respaldar ahora" + lista de los últimos 20 (ícono reloj/click según origen — nunca solo color — y botón de descarga por fila).
- Nuevas pruebas locales sin red (mismo patrón que `test-availability.js`): `api/test-backup.js` (formato del nombre de archivo / hora de México) y `api/test-rate-limit.js` (ver siguiente punto) — ambas conectadas a `npm test`, que ya corre en CI desde el lote de correcciones de este mismo día.
- **Candado de intentos generalizado**: la lógica de rate limiting que vivía hardcodeada en `getRecursos` se extrajo a `api/src/rate-limit.js` (parametrizada por `table`+`partitionKey`, sin saber nada de "cursos") para que Diagnóstico (Fase 1.4 del roadmap) la reuse cuando le toque construirse, en vez de copiarla a mano. `getRecursos` la consume sin cambios de comportamiento. De paso se corrigió un bug de UX real: `recursos.html` no distinguía un 429 (bloqueado) de cualquier otro error — ya muestra el mensaje real del backend en ese caso.
- **3 bugs reales encontrados y corregidos en el rollout, no en el diseño original** (mismo día, ver Historial): (1) `getRespaldosContainer()` creaba el contenedor con `{access:"none"}` — valor inválido para Azure (solo acepta `"container"`/`"blob"`), tronaba toda la familia de Functions de Respaldos; corregido quitando la opción (sin ella, el contenedor ya es privado por default). (2) el `timerTrigger` de la primera versión (ver arriba). (3) `listRespaldos` extraía el "origen" del nombre del archivo con `/_(\w+)\.json$/` — como `\w` incluye `_`, capturaba desde el primer guion bajo (ej. `"0434_auto"`) en vez de solo `"auto"`, así que un respaldo automático nunca comparaba igual a `'auto'` y siempre se mostraba como "Manual"; corregido matcheando el valor literal `(auto|manual)`.
- **Confirmado funcionando de punta a punta en producción** (2026-09-08): Alfredo generó el secreto, lo configuró en Azure y GitHub, corrió el workflow a mano (`workflow_dispatch`) sin esperar al lunes, y confirmó que el respaldo ya se etiqueta correctamente "Automático" tras el fix del punto (3).

**En construcción — Clientes/Contactos/Tarifas, base de Fase 1 Comercial (construido 2026-09-08, rediseñado 2026-09-09 tras feedback real de Alfredo, pendiente confirmar en producción):**
- **ACTUALIZACIÓN 2026-09-10 — el layout maestro-detalle descrito abajo ya no es de una sola vista:** se separó en pestañas "Consultar Clientes" (el maestro-detalle de siempre, pero ahora 100% solo lectura) / "Crear Cliente" (el formulario de alta/edición + gestión de Contactos, antes vivían dentro del panel de detalle). Ver el reacomodo de navegación (Prompt 2) en "Pendientes de Alfredo" y el Historial de esa fecha — el resto de este bloque (Functions, esquema, criterios de orden/búsqueda) sigue vigente tal cual.
- Primer módulo de Fase 1. Extiende la base `apcweb-backoffice` que ya usan Diplomas/Encuestas — `sql/005_comercial.sql` (corrido ya por Alfredo) agrega `notas` a `Cliente`, crea `Contacto`/`TarifaHerramienta`/el esquema completo de `Solicitud`/`Cotizacion`; `sql/006_comercial_ajustes.sql` (nuevo, **pendiente de correr**) agrega `planta` a `Contacto` y `cliente_desde` (año, INT) a `Cliente`. Solicitud/Cotizacion siguen sin ninguna Function ni UI todavía, a propósito: Fase 1.1/1.2 solo van a tener que construir encima, sin volver a tocar el modelo de datos.
- `temas_json` en Solicitud/Cotizacion es una **foto** de los temas al momento de crear el registro (snapshot `[{temaId,nombre,horas,nivel}]`), no una referencia viva a `Temas`/`TemariosEstandar` (Table Storage) — evita JOINs entre las dos bases.
- **10 Functions** (v3 clásico, protegidas rol admin): `listClientesAdmin` (ahora con `?sort=nombre|antiguedad|ultima_cotizacion` y la búsqueda `?q=` incluye nombre de Contacto vía `EXISTS`, no solo Cliente), `crearCliente`/`editarCliente` (validan `cliente_desde` como año 1990–actual si viene, Cliente nunca se borra), `listContactosAdmin`/`crearContacto`/`editarContacto`/`eliminarContacto` (ahora con `planta`), `listTarifasAdmin`/`actualizarTarifa`, y la nueva **`getResumenClientesAdmin`** (scorecard: total Clientes, total Contactos, Clientes con ≥1 Cotización este año — para el % que pinta el admin). `crearContacto`/`editarContacto` siguen usando `sql.Transaction` para el candado de "un solo contacto principal por cliente". `ultima_cotizacion` y el conteo de "clientes activos" ya están conectados a la tabla `Cotizacion` real (hoy vacía, Fase 1.2 sin construir) — cuando esa fase empiece a insertar filas, estos números y el orden por "Última cotización" reflejan la realidad solos, sin tocar código.
- `admin/index.html` → sección **Clientes** — **rediseño completo 2026-09-09** (reemplaza el layout de acordeón del día anterior, que dejaba mucho espacio muerto a la derecha): layout maestro-detalle (`.cli-layout`, grid 340px+1fr, colapsa a una columna en <900px) — lista compacta a la izquierda con buscador (ahora también busca por nombre de contacto) + 3 botones de orden (Nombre/Última cotización/Antigüedad, pill toggle) + "+ Nuevo Cliente"; a la derecha el detalle del cliente seleccionado (nombre/código/antigüedad/notas + sus Contactos con alta/edición/borrado inline). Cada fila de la lista muestra tags de antigüedad ("X años", calculado de `cliente_desde`) y última cotización ("Sin cotizar" hasta que exista Fase 1.2). Scorecard de 3 KPIs arriba de todo (`.dkpi-card`, mismo componente que Dashboard/Encuestas). El form de Contacto ahora incluye **Planta** (para clientes con más de una ubicación física) junto a Área/Correo/Teléfono/WhatsApp/Principal.
- **Tarifas por hora — guardado deliberado, no automático (pedido explícito de Alfredo: la toca ~1 vez al año, prefiere precaución sobre conveniencia):** el auto-guardado al perder el foco de la v1 se reemplazó por un botón **"Actualizar"** por fila, deshabilitado hasta que el precio cambia, que pide confirmación (`confirm()` con el precio anterior y el nuevo) antes de escribir en la base. Cada tarjeta lleva un borde/glow del color de marca de su herramienta (`--tc` por fila, mismos hex que Cursos/Recursos/Dashboard — verde Excel, amarillo Power BI, magenta Power Apps, cian Power Automate, morado IA, naranja Ofimática).
- **Configuración — limpieza 2026-09-09:** sub-sección **Plantillas** eliminada (documentos de cotización/diploma ya no viven aquí, se decide después). **Tarifas por hora** y **Respaldos** ahora son colapsables (botón "Desplegar"/"Ocultar", contenido oculto por default) en vez de cargar sus datos automáticamente cada vez que se abre Configuración — evita pegarle a la base SQL serverless (Tarifas) o a Table Storage (Respaldos) sin que Alfredo realmente vaya a usar esa sección, mismo criterio ya aplicado al polling del contador de Encuestas.
- **Bug propio encontrado y corregido el 2026-09-08, antes de dar el módulo por bueno:** el formulario inline de "+ Agregar contacto"/editar contacto no se cerraba después de guardar exitosamente — `pintarContactos()` reemplaza el `innerHTML` de la LISTA de contactos, pero el formulario vivía como hermano después de la lista (`listaEl.after(form)`), así que redibujar la lista nunca lo tocaba. Corregido con `form.remove()` explícito en el handler de guardar, encontrado probando el flujo real en el navegador.
- Probado de punta a punta en ambas sesiones con el mismo patrón (`npx http-server` vía `.claude/launch.json` + `window.fetch` mockeado en memoria simulando las Functions): alta/edición de cliente con antigüedad y notas, 2 contactos con planta marcando ambos como principal (confirmado que el candado se refleja en la UI), búsqueda por nombre de contacto, los 3 botones de orden (antigüedad confirmado nulls-al-final), scorecard con números reales, el flujo de Tarifas con `confirm()` mockeado en ambos sentidos (aceptar guarda y actualiza `data-original`; cancelar no toca la base y deja el botón habilitado), y el colapsable de Respaldos (oculto por default, dispara la carga solo al desplegar la primera vez).
- **Segunda ronda de pulido visual, mismo día 2026-09-09**, tras ver el rediseño ya desplegado: scorecard separado del resto con una línea (`border-bottom`) para que no se sienta pegado al buscador; toolbar de Buscar/Ordenar/Nuevo Cliente reorganizado en una tarjeta propia de 2 filas (buscar+nuevo arriba, ordenar abajo con su propia línea divisoria) en vez de un solo renglón que se sentía revuelto; **orden ahora es bidireccional** — clic en un criterio ya activo alterna ascendente/descendente (antes solo alternaba el criterio, nunca la dirección, lo que hacía parecer que "ordenar por Nombre no funcionaba" porque ya era el default) — con una flecha ↑/↓ junto al criterio activo; y la fila seleccionada en la lista y el panel de detalle ahora comparten el mismo acento azul en el borde izquierdo, para que se lea como "estos contactos pertenecen a esta fila". `listClientesAdmin` ahora recibe `?dir=asc|desc` además de `?sort=` (mismo query parametrizado con `CASE WHEN`, sin SQL dinámico).
- **2 bugs reales de CSS encontrados y corregidos en esta ronda — ambos por lectura + prueba en el navegador, no evidentes solo leyendo el código:**
  1. `border-color:var(--x)` (shorthand de las 4 esquinas) combinado con una sobreescritura de un solo lado en la MISMA regla (ej. `border-left-color:var(--x)` justo después) hace que el navegador calcule los otros 3 lados como inválidos/vacíos en vez de heredar `var(--x)` — confirmado con `document.styleSheets` mostrando los long-hands realmente vacíos en el CSSOM parseado, no solo un problema de repintado. Pasaba en `.cli-row.active` y `.cli-detail.seleccionado` (el borde de selección nunca se pintaba). **Regla: si un lado necesita un grosor/color distinto a los otros 3, usar el shorthand completo `border:...; border-left:...;` en ambas reglas (base y estado), nunca mezclar `border-color` con un long-hand de un solo lado.**
  2. Un selector descendiente compuesto (`.padre.activo .hijo{ transform:... }`) tampoco se aplicaba de forma confiable en esta hoja de estilos grande, aunque `querySelector`/`.matches()` confirmaban que sí calzaba y la regla en el CSSOM tenía el valor correcto — pasaba con el chevron de "Desplegar" en Configuración (nunca rotaba). **Corregido evitando el selector compuesto por completo:** la clase de estado (`expanded`) se pone directamente en el elemento que cambia (el ícono), no en un ancestro, y el CSS queda como selector simple `.cfg-toggle-ico.expanded`. Si un componente futuro necesita reaccionar al estado de un ancestro, preferir poner la clase directo en el elemento afectado en vez de depender de un selector `.ancestro.estado .descendiente`.
- **Pendiente para Alfredo (no requiere a Claude):** correr `sql/006_comercial_ajustes.sql` en el Query Editor; confirmar en producción el flujo completo (cliente con antigüedad, contacto con planta, orden en ambas direcciones, búsqueda por contacto, scorecard, y el nuevo guardado de tarifas con confirmación).

**En construcción — Solicitudes, Fase 2 Comercial (2026-09-09, pendiente confirmar en producción):**
- **ACTUALIZACIÓN 2026-09-10 — reacomodo de navegación (Prompt 2):** la vista única descrita abajo (tabla + formulario que se expande arriba) se separó en pestañas "Consultar Solicitudes" (tabla, filtros/orden, el desglose expandible por fila ahora también trae `participantes`/`modalidad`/`ciudad_sede`/`fecha_tentativa`, no solo los temas) / "Crear Solicitud" (el mismo formulario de siempre, ahora con modo edición unificado — nueva acción "Editar" por fila, Cliente queda de solo lectura al editar, mismo criterio que ya usaba `editarSolicitud` desde Cotizaciones). Ver el Historial de esa fecha — el resto de este bloque (Functions, TemarioSelector, esquema) sigue vigente tal cual.
- Extiende `Solicitud`, tabla que ya existía desde `sql/005_comercial.sql` (Fase 1) sin ninguna Function todavía. Sin botón "Convertir a Cotización" a propósito (Cotización no existe como Function hasta Fase 3) ni cambio automático a estatus "Cotizada" — por ahora el estatus se cambia a mano desde la tabla.
- **`sql/007_solicitud_datos_cotizacion.sql`** (nuevo, pendiente de correr): agrega `fecha_tentativa` (texto libre, ej. "últimas dos semanas de septiembre" — no es un DATE real), `ciudad_sede`, `participantes` y `modalidad` (estos 2 últimos con CHECK, mismos valores fijos que ya usa el formulario público de contacto en `cursos.html`) — Alfredo los necesita para armar la Cotización después. El campo "Comentarios" del form público se guarda en `notas` (ya existía) — no se duplicó un campo de texto libre casi idéntico.
- **`temas_json` ahora siempre guarda el desglose COMPLETO de temas** (id/nombre/**descripción**/horas/nivel), tanto para "personalizado" (los temas marcados) como para "estándar" (los temas que trae ese temario, resueltos desde `getCatalogoCursos`) — antes "estándar" solo guardaba el nombre del temario. Alfredo lo pidió explícitamente para poder desglosar la Cotización con el mismo detalle sin volver a consultar el catálogo.
- **4 Functions** (v3 clásico, protegidas rol admin): `listSolicitudesAdmin` (filtros estatus/cliente/fecha + `?sort=fecha|cliente|horas&dir=asc|desc`, mismo patrón `CASE WHEN` que `listClientesAdmin`; JOIN a Cliente/Contacto para mostrar nombres), `crearSolicitud` (mismo patrón `resolverCliente` que `generarDiplomas`/`enviarRespuesta`, duplicado a propósito), `actualizarEstatusSolicitud` (valida los 5 estatus válidos en JS, sin CHECK constraint en la tabla), y la nueva **`getResumenSolicitudesAdmin`** (scorecard: solicitudes Manuales, del Sitio, y % con estatus distinto de "Nueva").
- `admin/index.html` → sección **Solicitudes**: scorecard de 3 KPIs arriba (separado con línea) + una sola tarjeta `.sol-toolbar` (mismo patrón que `.cli-toolbar` de Clientes) con los filtros arriba y, tras una línea divisoria, **orden bidireccional** (Fecha/Cliente/Horas, ícono ↑/↓) + "+ Nueva Solicitud" — antes filtros y orden flotaban sueltos sobre el fondo del panel sin una tarjeta que los agrupara, y se sentía desordenado. Formulario de alta (`.radm-form`): Cliente (copia propia de `crearAutocomplete` con `onChange` para cargar Contactos) → Contacto (`<select>`, deshabilitado hasta elegir cliente EXISTENTE — clic real en la sugerencia del autocompletado, no basta con escribir el nombre) + "+ Nuevo contacto" (mini-form inline) → Herramienta → selector de temario → Fecha tentativa/Ciudad-sede (texto libre) → Participantes/Modalidad (`.sol-pill-group`) → Comentarios. **Tabla con columna "Horas" separada** (numérica, para usar directo en la Cotización) de "Temario / Temas" (ahora muestra el nombre real o los primeros temas, con un botón que despliega el desglose completo — cada tema con nivel, horas y descripción, tirando de `temas_json`).
- **2 bugs reales de CSS encontrados y corregidos** (mismo patrón que el de `border-color` de la sesión de Clientes, ver "Ya resueltos" arriba para el detalle completo): `.radm-btn-add:disabled` no tenía NINGÚN estilo propio (un botón deshabilitado se veía idéntico a uno habilitado) — así se sentía "+ Nuevo contacto" como roto cuando en realidad estaba deshabilitado a propósito; y el `background:color-mix(...)` que se le agregó a `.radm-btn-add` en la pasada anterior resolvía a transparente del todo en esta hoja de estilos (mismo tipo de falla que `border-color`, corregido igual: shorthand → long-hand, `background-color`). El fix de `background-color` aplica a TODO el admin, no solo Solicitudes.
- Probado de punta a punta en el navegador: botón deshabilitado con opacidad visiblemente distinta al habilitado, tinte de fondo correcto tras el fix, columna Horas separada, desglose de temas expandiéndose con descripción y el chevron rotando, más lo ya probado en la pasada anterior (ambos tipos de temario, pills, scorecard, orden).
- **Pendiente para Alfredo (no requiere a Claude):** correr `sql/007_solicitud_datos_cotizacion.sql`; confirmar el flujo completo en producción, sobre todo "+ Nuevo contacto" con un cliente existente elegido de verdad desde la lista.

**En construcción — Cotizaciones, Fase 3 Comercial (2026-09-09, pendiente confirmar en producción):**
- **ACTUALIZACIÓN 2026-09-10 — reacomodo de navegación (Prompt 2):** el panel gana una segunda pestaña, "Ver Cotizaciones" (filtros Cliente/Estatus/fechas, carga perezosa solo al abrir la pestaña — mismo criterio de no despertar la base SQL sin necesidad que ya usa Tarifas/Respaldos) junto a "Crear Cotización". "Recotizar" ahí reusa tal cual `precargarFormDesdeCotizacion` (la misma función de "Nueva versión" en Tracking Comercial, sin duplicar lógica). `listCotizacionesAdmin` ganó `?estatus=` (exacto) para poder filtrar cualquier estatus individual, no solo las 4 categorías amplias de `?vista=`. Ver el Historial de esa fecha.
- **ACTUALIZACIÓN 2026-09-10 — el PDF se reescribió sobre pdfmake, ya construido:** reemplaza por completo lo que decía este bloque de `pdf-lib` + plantilla de fondo — ver el detalle real (ya no el plan) más abajo, en el bloque "PDF con pdfmake". El resto de este bloque (Functions de datos/tracking, flujo Lista/Detalle/Editar, TemarioSelector) sigue vigente tal cual, solo cambió cómo se genera el archivo PDF.
- La pieza más grande del bloque Comercial hasta ahora: PDF, pantalla de creación, tracking/scorecard (implementa tal cual el mockup ya aprobado en una sesión anterior) y plantilla. Extiende `Cotizacion`, tabla que ya existía completa desde `sql/005_comercial.sql` (Fase 1, sin Functions todavía) — `sql/008_cotizacion_datos.sql` (nuevo, pendiente de correr) le agrega los mismos 4 campos que ya tiene `Solicitud` (`fecha_tentativa`/`ciudad_sede`/`participantes`/`modalidad`), mismo patrón `GO` + idempotente que `sql/007` tras el bug de batch de esa sesión (ver "Ya resueltos").
- **PDF con pdfmake** (`api/src/cotizacion-pdf.js`, reescrito de punta a punta el 2026-09-10) — a diferencia de Diplomas (pdf-lib + plantilla PNG diseñada en Canva/coordenadas x/y a mano), aquí el diseño completo del documento vive en código: sin imagen de fondo que Alfredo tenga que diseñar/subir, sin coordenadas que ajustar. 3 páginas — Portada (logo+folio, franja de acento degradada herramienta→azul, marcas de esquina, glow decorativo tras el título, badge de herramienta, bloque cliente, callout "Dirigido a/Objetivo" — se omite entero si ambos vienen vacíos, caso del temario Personalizado —, pastilla `fx =COTIZAR(...)`), Temario (barra de datos proporcional a horas por tema, paginación 100% automática si el temario es largo) y Propuesta (tabla de servicio, grid de datos, tarjeta de precio, 6 términos y condiciones completos, bio+foto+certificaciones, pastilla `fx =CONTACTAR(...)`). Fuentes: `Inter` (ya existía, Diplomas), `Space Grotesk` y `JetBrains Mono` nuevas en `api/assets/fonts/` (Google Fonts, licencia OFL, TTF estáticos por peso — **no** las variables `[wght].ttf` que reparte el repo oficial de Google Fonts, esas no traen un peso fijo embebible tal cual). Imágenes `firma-ap.png`/`alfredo-work-photo.png` copiadas a `api/assets/img/` (las Functions no pueden leer fuera de `api/` al desplegar).
- **Colores por herramienta — tabla fija en el código** (`TOOL_COLORS`): color de marca para fills/barras + una versión de texto con contraste real sobre fondo claro (varios de los 6 colores, sobre todo el amarillo de Power BI, son ilegibles como texto tal cual) — ver la tabla completa en CLAUDE.md → Identidad de marca. El fondo "soft" de pills/badges se calcula mezclando el color de marca con blanco al 13% (`tint()`), no con `color-mix()` de CSS (esto es PDF, no hay CSS).
- **Limitaciones de pdfmake vs. el mockup aprobado, aceptadas a propósito** (documentadas en el propio código): sin blur real (el "glow" tras el título es un óvalo de color plano, no difuminado), sin conic-gradient (el marco de la foto en "Sobre mí" es un borde sólido de 2pt del color de la herramienta, no un anillo degradado), sin textura de cuadrícula de fondo (se omitió, poco impacto visual para el código que costaba).
- **Sin sistema de firmas aquí** (a diferencia de Diplomas) — decisión explícita: solo Alfredo firma cotizaciones, y no hay plantilla donde hornear una firma — si algún día la quiere en el PDF, habría que agregarla como imagen fija en el código.
- **Correo de contacto — Application Setting, no texto fijo:** `COTIZACION_CONTACT_EMAIL` (Alfredo no tiene correo `@alfredopina.ai` activo todavía; configurarla mientras tanto con `alfredo.pina@lifezen.com.mx`) — si falta, `generarCotizacionPdf` truena con un mensaje claro en vez de generar un PDF con un contacto roto. El teléfono `(811) 725 5937` sí está fijo en código (es real).
- **2 bugs reales de pdfmake encontrados y corregidos probando en el navegador** (ver "Ya resueltos" — patrón nuevo para este proyecto, primera vez que se usa pdfmake): (1) una tabla con `absolutePosition` muy cerca del borde inferior de la página fuerza una página en blanco extra en vez de solo dibujarse ahí; (2) el resumen "Horas totales" del temario podía quedar huérfano solo en una página casi en blanco cuando el temario llenaba la página anterior casi exacto — corregido agrupándolo (`unbreakable`) junto con el último tema.
- **Blob**: contenedor **`cotizaciones`** (`api/src/cotizaciones-storage.js`, sin cambios) para los PDFs finales — privado, autocreado con `createIfNotExists()` sin la opción `access` (mismo patrón que `respaldos`, ver bug ya documentado). El contenedor `plantillas` ya no tiene nada de Cotización (solo lo de Diplomas, que se queda igual).
- **Folio y precio**: mismo folio `AP_{HERRAMIENTA}_{código}_{AA}-{consecutivo}` que Diplomas/Solicitudes (consecutivo por Cliente+Año, ahora leyendo `Cotizacion.folio`). `precio_sugerido = horas × TarifaHerramienta.precio_hora`; descuento sugerido de 10% si `participantes` es "Solo yo" o "5 a 10" — **siempre editable**, `descuento_pct`/`precio_final` que mande el front pisan la sugerencia sin problema.
- **6 Functions de Cotización** (v3 clásico, protegidas rol admin — bajó de 9 a 6 el 2026-09-10 al eliminar las 3 de plantilla): `previsualizarCotizacion` (mismo espíritu que "generar PDF de prueba" de Diplomas — recibe cliente/contacto ya como texto del front en vez de resolverlos, usa un folio placeholder fijo, y **no toca la base ni el blob permanente**, ni siquiera para leer), `crearCotizacion` (resuelve folio, calcula precio, sube el PDF final, inserta la fila en Borrador; si viene `reemplaza_a_folio` pasa esa cotización vieja a "Reemplazada"; si viene `solicitud_id` esa Solicitud pasa a "Cotizada"), `listCotizacionesAdmin` (`?vista=activas|ganadas|perdidas|todas` mapea a los 4 tabs del mockup; calcula en la consulta `dias_abierta` y `vencida` — vencida es **solo informativa**, nunca cambia el estatus real solo por vencerse), `getResumenCotizacionesAdmin` (scorecard — sin columna `fecha_cierre` en el esquema, "del mes"/"tiempo de cierre" se aproximan con `fecha_creacion → ahora` para las que ya están Ganada/Perdida y se crearon este mes, simplificación documentada en el código), `actualizarEstatusCotizacion` (pone `fecha_envio` a ahora si el nuevo estatus es Enviada y no la tenía), y `getCotizacionPdf` (streaming desde el blob privado). Tanto `crearCotizacion` como `previsualizarCotizacion` reciben `dirigido_a`/`objetivo` ya resueltos por el front (ver abajo) y los pasan tal cual a `generarCotizacionPdf` — no se guardan en la base, son solo para el PDF.
- **`editarSolicitud`** (nueva, v3 clásico, protegida rol admin, sesión del mismo día tras el rediseño de Crear Cotización) — UPDATE de una Solicitud existente (contacto/herramienta/temario/fecha tentativa/ciudad/participantes/modalidad/notas; `cliente_id`/`canal_origen` no editables a propósito). Mismas validaciones que `crearSolicitud`. `actualizarEstatusSolicitud` gana el estatus **`Descartada`** (el "eliminar" de la lista de Crear Cotización — Solicitud nunca se borra de verdad, mismo criterio que Cliente/Diploma).
- **Selector de temario compartido — `window.TemarioSelector`** (nuevo, en `admin/index.html`, insertado antes del IIFE de Solicitudes): extrae la lógica que antes vivía solo dentro de Solicitudes (herramienta → tabs Estándar/Personalizado → dropdown o checkboxes con suma de horas en vivo, contra `getCatalogoCursos`) a un componente reusable — `TemarioSelector.crear({modoWrapEl, bodyEl})` regresa `{cargarHerramienta, getPayload, getModo, preseleccionar}`, con el caché de catálogo por herramienta compartido a nivel de módulo entre ambos paneles. El IIFE de Solicitudes se refactorizó para consumirlo (se probó que produce el mismo payload exacto que antes, regresión limpia) — cumple el pedido explícito de Alfredo de "no repetir el componente desde cero". Cotizaciones abre una SEGUNDA instancia (`temarioSelEdit`) solo dentro de la vista "Editar" de Crear Cotización — la vista de solo lectura no necesita selector, solo muestra el `temas_json` ya guardado. `preseleccionar({temarioId}|{temaIds})` es lo que usa esa vista de edición para precargar el temario ya guardado — como Solicitud solo guarda el NOMBRE del temario estándar (no su id), la precarga resuelve el id por nombre contra el catálogo cacheado (`TemarioSelector.cargarCatalogo`, también expuesto) antes de llamar `preseleccionar`.
- **Tarifas — carga perezosa, no al entrar al panel:** `cargarTarifasCache()` (usada para el cálculo de precio sugerido) solo se dispara la primera vez que se abre la pestaña Crear Cotización (y de nuevo, sin red si ya cargó, al abrir el detalle de una Solicitud), no automáticamente al abrir Cotizaciones — mismo criterio que Tarifas/Respaldos en Configuración, para no despertar la base SQL serverless sin necesidad. Ya usaba `apiGetConReintento` desde el diseño original (no es el mismo bug que `buscarClientes`, ver "Ya resueltos"), el ajuste fue solo de *cuándo* se dispara.
- **`dirigido_a`/`objetivo` se resuelven en el front, no en el backend** (`resolverDirigidoObjetivo(sol)` en `admin/index.html`): si la Solicitud es de un Temario Estándar, busca el temario por nombre en el catálogo ya cacheado por `TemarioSelector.cargarCatalogo` (mismo patrón que ya usa `precargarTemarioEdit` para resolver el id del temario) y lee sus campos `dirigido`/`objetivo` (ya existían en `TemariosEstandar`, ver Cursos); si es Personalizado, regresa ambos `null` y el PDF simplemente omite esa caja. Se llama antes de armar el payload tanto en "Vista previa" como en "Crear".
- `admin/index.html` → sección **Cotizaciones** (le quita el tag "PRONTO" al sidebar), **2 pestañas** (bajó de 3 el 2026-09-10 al quitar Plantilla — ya no hay nada que subir para el PDF):
  1. **Seguimiento** (default) — scorecard de 5 KPIs (pipeline activo/ganado/perdido/tasa de conversión/tiempo promedio de cierre, este último resaltado con gradiente), alerta ámbar de cotizaciones con ≥10 días sin respuesta (nombra hasta 2 clientes + "y N más"), tabs Activas/Ganadas/Perdidas/Todas, tabla con semáforo de días (mismos 3 colores del Dashboard — `#4ade80`/`#facc15`/`#b91c1c`, siempre con el número de días en texto, nunca solo color) y 3 acciones por fila: **Enviar** (marca Enviada, abre el PDF en pestaña nueva para adjuntar a mano, y abre un `mailto:` prellenado — un link no puede adjuntar archivos), **Nueva versión** (precarga Crear Cotización con los datos de esa fila y `reemplaza_a_folio` listo), **Correo de seguimiento** (mismo mecanismo `mailto:`, cuerpo tipo "solo dando seguimiento...").
  2. **Crear Cotización** — **rediseñada el mismo día tras el primer vistazo de Alfredo: toda Cotización nace de una Solicitud, no de captura libre de cliente/herramienta/temario.** 3 vistas dentro de la misma pestaña (`mostrarVistaCrear('lista'|'detalle'|'editar')`):
     - **Lista** (default) — todas las Solicitudes activas para cotizar (`listSolicitudesAdmin` sin filtro, excluyendo Ganada/Perdida/Descartada en el cliente), con 3 acciones por fila: **Cotizar** (abre el detalle), **Editar** (lápiz), **Descartar** (basura — no borra, pasa la Solicitud a estatus nuevo `Descartada`, mismo criterio "nunca borrar de verdad" que Cliente/Diploma).
     - **Detalle** — resumen de SOLO LECTURA de la Solicitud elegida (cliente, contacto, código, herramienta, temario+desglose de temas, horas, fecha tentativa, ciudad, participantes, modalidad) + botón "Editar solicitud" (abre la vista de edición sin perder el lugar) + tarjeta de precio con el cálculo en vivo (horas × tarifa, descuento de grupo chico aplicado si corresponde) y Descuento %/Precio final editables (con vista formateada en moneda) + Fecha de vigencia (default hoy+15 días) → botones Vista previa/Crear.
     - **Editar** — formulario con los mismos campos que Solicitudes (contacto, herramienta + selector de temario compartido, fecha tentativa, ciudad, participantes, modalidad, comentarios), llama a la Function nueva `editarSolicitud` (UPDATE, cliente_id/canal_origen no editables a propósito) y regresa a donde estaba (lista o detalle).
     "Nueva versión" desde Seguimiento salta directo a la vista Detalle con los datos de esa Cotización (sin pasar por la lista) — si tiene `solicitud_id` puede editarse desde ahí, si no (cotizaciones viejas sin origen), el botón "Editar solicitud" avisa que no hay Solicitud ligada en vez de fallar en silencio.
- **Bug real de CSS encontrado y corregido esta sesión — familia nueva, no exactamente la de `border-color`/`background` ya documentada:** la alerta de "≥10 días sin respuesta" (`.cot-alert`) se quedaba visible vacía aunque tuviera el atributo `hidden` y el código nunca le quitara ese atributo — causa: `.cot-alert{ display:flex; ... }` tiene la MISMA especificidad CSS que el selector `[hidden]` del user-agent stylesheet, y un selector de autor siempre gana sobre el user-agent sin importar el orden, así que `display:flex` ganaba y el `hidden` quedaba sin efecto. Encontrado probando en el navegador (visualmente el banner aparecía vacío en pestañas donde nunca debería mostrarse). **Corregido con un override explícito `.cot-alert[hidden]{ display:none; }`.** Regla ampliada para el resto del proyecto: cualquier elemento que se oculte con el atributo `hidden` y cuya clase base fije su propio `display` (no `none`) necesita ese mismo override `[hidden]{ display:none; }` — si no, el atributo `hidden` puede quedar sin efecto sin ningún error visible en consola.
- Probado en local con `.claude/launch.json` + `window.fetch` mockeado (mismo patrón de siempre), dos rondas (construcción inicial y el rediseño de Crear Cotización): scorecard/alerta/semáforo/badges con datos variados, los 4 tabs de vista de Seguimiento, la lista de Solicitudes activas (Cotizar/Editar/Descartar), el detalle de solo lectura con el desglose completo de temas, editar una Solicitud desde la lista y desde dentro del detalle (confirmado que precarga el temario estándar correcto resolviéndolo por nombre), Descartar marcando el estatus sin borrar, cálculo de precio con y sin descuento de grupo chico, Crear armando el payload completo (folio, solicitud_id, temas) y regresando a la lista, "Nueva versión" saltando directo al detalle con los datos de una Cotización existente (con y sin Solicitud ligada — el segundo caso avisa en vez de fallar), y regresión completa de Solicitudes tras el refactor a `TemarioSelector` (mismo payload exacto que antes de tocar el código).
- **Pendiente para Alfredo (no requiere a Claude):** correr `sql/008_cotizacion_datos.sql`; configurar la Application Setting `COTIZACION_CONTACT_EMAIL`; confirmar el flujo completo en producción — ver el checklist detallado en "Pendientes de Alfredo" al inicio del documento.

**En construcción — Diagnóstico, Fase 2 Operación (2026-09-10, diseño + construcción en la misma sesión, pendiente confirmar en producción):**
- Reemplaza el Google Forms actual de Alfredo — cada pregunta ancla una imagen de referencia real (tabla/hoja) a 4 respuestas, evaluando método/criterio en vez de memorización. Solo Excel y Power BI, 15 preguntas por herramienta (5 por nivel). Se comparte en vivo por QR durante la sesión de cierre/arranque de grupo, mismo patrón operativo que Encuestas — sin anti-spam/rate-limit a propósito, mismo criterio.
- **`sql/009_diagnostico.sql`** (nuevo, pendiente de correr): 3 tablas — `DiagnosticoPregunta` (banco, `opcion_correcta` nunca se manda al público), `DiagnosticoRespuesta` (cabecera — nombre + `cliente_id` FK a `Cliente` + herramienta) y `DiagnosticoRespuestaDetalle` (una fila por pregunta respondida, `pregunta_id` **sin FK a propósito**, mismo criterio que `EncuestaRespuestaDetalle` — una pregunta borrada no tumba el historial, `listRespuestasDiagnosticoAdmin` la muestra como "(pregunta eliminada)"). `fue_correcta` y `nivel` se guardan como **foto al momento de responder** en el detalle — si la pregunta se edita o borra después, las respuestas ya guardadas no se recalculan.
- **Contenedor Blob nuevo `diagnostico`** (`api/src/diagnostico-storage.js`, mismo Storage Account `apcwebrecursos`) — a diferencia de `respaldos`/`cotizaciones` (privados, `createIfNotExists()` sin `access`), este necesita ser **público** (`access:'blob'`, explícito) porque las imágenes de referencia se sirven directo por su URL, sin Function de streaming — mismo criterio que `recursos`, no como los PDF privados de Diplomas.
- **9 Functions nuevas** (v3 clásico, ninguna con nombre que empiece "admin"): `getPreguntasDiagnostico` (pública, filtra `opcion_correcta` antes de responder — nunca llega al cliente ni en la respuesta cruda de red), `listPreguntasDiagnosticoAdmin` (protegida, sí incluye `opcion_correcta`), `crearPreguntaDiagnostico` (imagen obligatoria, sube a Blob con un `crypto.randomUUID()` como nombre) / `editarPreguntaDiagnostico` (imagen opcional — si no llega, conserva la `imagen_url` existente), `eliminarPreguntaDiagnostico` (borrado real de la fila + su imagen del Blob), `reordenarPreguntasDiagnostico` (dentro de herramienta+nivel — **sin el truco de offset que usa Encuestas**: aquí nivel ya es una columna propia, la consulta pública ordena `(nivel, orden)`, así que el `orden` solo importa relativo a las demás preguntas del MISMO nivel), `enviarDiagnostico` (pública, `sql.Transaction`, calcula `fue_correcta` comparando contra `opcion_correcta` en ese momento — nunca confía en lo que mande el cliente para esto), `listRespuestasDiagnosticoAdmin` (protegida, tallies por nivel calculados sobre el total de preguntas realmente contestadas en esa banda, no hardcodeado a 5) y `getContadorDiagnosticoAdmin` (protegida, total + desglose Excel/Power BI).
- `admin/index.html` → sección **Diagnósticos** (le quita el tag "PRONTO" al sidebar), 3 pestañas:
  1. **Preguntas** — grid de 2 tarjetas de herramienta (Excel/Power BI, reusa `.radm-tool-card`) → tabs por nivel (Básico/Intermedio/Avanzado) → lista con drag & drop de orden (SortableJS) + alta/edición inline (texto, imagen con vista previa inmediata vía `URL.createObjectURL` antes de subir nada, 4 opciones + radio para marcar la correcta, switch Activa/Inactiva) + borrado con confirmación (mismo texto de advertencia que Encuestas: el historial sobrevive, solo pierde texto/imagen).
  2. **QR / Link** — dos tarjetas lado a lado (Excel/Power BI, cada una con su color de marca), cada una con su propio link+QR+copiar+"Ver diagnóstico"+descargar PNG, y una tarjeta **compartida** de "Respuestas en vivo" abajo con 3 KPIs (Total/Excel/Power BI) — mismo candado de polling de 20s que Encuestas (`diagQrTabActiva` + panel activo + `!document.hidden`).
  3. **Resultados** — filtros (Herramienta/Empresa/fechas) + tabla (Nombre, Empresa, Herramienta, Fecha, Básico X/Y, Intermedio X/Y, Avanzado X/Y, Total X/Y) + fila expandible (mismo patrón `sol-temario-toggle`/`sol-desglose-row` que ya usa Solicitudes, reusado tal cual) con el detalle de las 15 preguntas — opción elegida + ícono **y texto** "Correcta"/"Incorrecta" (nunca solo color, regla de siempre por la deuteranomalía de Alfredo). El nivel final interpretativo NO se calcula aquí — es una síntesis que arma Alfredo aparte, esto solo guarda/muestra el dato crudo.
- `diagnostico.html` (nuevo) — página pública oculta (sin login, sin liga en el menú, `noindex`, nav+badge "página oculta", igual que `agenda.html`/`encuesta.html`, sin footer de contacto). Lee `?h=excel|powerbi` de la URL; si falta o es inválido, muestra un error claro en vez de una página en blanco. Identificación = Nombre + Empresa (autocompletado vía `buscarClientes`, ya pública) — sin Instructor/Curso, a diferencia de Encuestas. Las 15 preguntas se muestran todas con scroll (no una por una), agrupadas Básico→Intermedio→Avanzado, cada una con imagen de referencia arriba + badges (nivel + contador `fx =PREGUNTA(n,15)`) + texto + 4 opciones como filas (letra A-D cuadrada mono + texto + checkbox que se comporta como radio — un solo valor por pregunta). **Regla no negociable ya implementada:** el estado seleccionado usa siempre `--blue-bright`, nunca el color de la herramienta ni ningún tono que pueda leerse como "correcto", y en ningún momento (selección, envío, pantalla final) se revela si la respuesta fue correcta. Validación de "faltan preguntas por responder" antes de dejar enviar, con scroll a la primera sin contestar (mismo patrón que Encuestas). Pantalla de agradecimiento con el motivo `fx` de cierre, sin mostrar puntaje ni nivel.
- Probado con `.claude/launch.json` + `window.fetch` mockeado en consola: en `diagnostico.html` — las 15 preguntas renderizando agrupadas por nivel con el contador correcto (1/15…15/15), el checkbox comportándose como single-select (clic en otra opción quita la selección anterior), validación de "faltan preguntas" contando exactamente las que faltan, envío completo y pantalla de gracias sin revelar nada, y el estado de error claro sin `?h=` o con un valor inválido; en el admin — alta de pregunta con validaciones (texto/opciones/imagen faltante bloquean el guardado en el orden correcto), edición sin volver a subir imagen (opciones se prellenan correctamente), borrado con confirmación, cambio de nivel dentro de una herramienta, los 2 QR generándose con sus links correctos, el candado de polling del contador compartido, y Resultados con tallies + detalle expandible mostrando correcto/incorrecto con texto.
- **Pendiente para Alfredo (no requiere a Claude):** correr `sql/009_diagnostico.sql`; cargar el banco real de 30 preguntas (15 Excel + 15 Power BI) con sus imágenes de referencia desde el admin → Diagnósticos → Preguntas; confirmar el flujo completo en producción, sobre todo el primer envío real desde `diagnostico.html` y que las imágenes se vean bien sirviéndose del contenedor Blob público nuevo.

**Configuración — rediseño a acordeón + Actividad reemplaza a Accesos (2026-09-10/11, pendiente confirmar en producción):**
- Alfredo pidió explícitamente que las secciones dejaran de verse como texto suelto — cada una (Perfil, Notificaciones, Actividad, Tarifas por hora, Respaldos) ahora es una tarjeta con borde (`.cfg-card`) cuyo encabezado COMPLETO es el disparador del acordeón (clic en cualquier parte del renglón, no un botón "Desplegar" aparte) — el chevron rota, sin texto "Ocultar"/"Desplegar" adicional (se quitó a propósito, más simple). Perfil/Notificaciones siguen siendo solo una nota "Pendiente" adentro, pero ya conviven visualmente igual que las demás.
- **Accesos se reemplaza por Actividad — decisión tomada tras cuestionar la utilidad real de la sección, no solo un cambio de nombre:** Alfredo preguntó directamente "no le encuentro la funcionalidad, dime qué no estoy viendo" sobre una tarjeta de Accesos puramente informativa (pasos + estado estático) — la respuesta honesta fue que tenía razón: Azure Static Web Apps (plan Free) no expone ningún API para ver o administrar roles desde dentro de la app (por eso `GetRoles` tronó el sitio hace meses, ver "Ya resueltos" — esa función solo existe en el plan Standard), así que cualquier versión de "Accesos" iba a ser decoración sin funcionalidad real. En vez de forzar algo ahí, se buscó qué SÍ es construible y útil: un conteo real de accesos al panel por administrador, que Alfredo pidió por separado en la misma conversación ("¿podemos ver cuántas veces entra cada quien?").
- **`api/src/auth.js`** (nuevo) — decodifica el header `x-ms-client-principal` que Azure ya inyecta en cada request a una ruta protegida (base64 → JSON) para leer el correo real del lado del servidor. Deliberado: nunca se confía en un correo que mande el propio cliente (alguien podría inflar el contador de otra persona), esto es lo que Azure ya verificó.
- **`api/src/actividad-storage.js`** (nuevo) — tabla `AdminActividad` en `apcwebrecursos` (self-creando, mismo patrón que `IntentosCodigo`), un renglón por correo con `veces` (leer-incrementar-escribir, sin transacción — con 2-3 administradores el riesgo de carrera es despreciable) y `ultimaVez`.
- **2 Functions nuevas:** `registrarAccesoAdmin` (protegida, POST, sin body — el correo sale del header, no del cliente) y `listActividadAdmin` (protegida, GET, ordenada por `veces` descendente). `admin/index.html` llama a `registrarAccesoAdmin` una vez por cada carga completa de `/admin` (justo junto al fetch de `/.auth/me` que ya existía) — cuenta "veces que se conectó" como "veces que cargó el panel", no como logins OAuth estrictos (Azure no expone esos eventos a la app de todas formas).
- **Botón "Despertar" — reintento automático agregado** (ver también "Deuda técnica"): Alfredo reportó que el primer clic a veces daba tachita roja pero un segundo clic inmediato después sí funcionaba — se confirmó que es el comportamiento real de Azure SQL serverless (el timeout de conexión de 60s a veces gana la carrera contra el arranque en frío, que de todas formas sigue completándose del lado de Azure aunque el cliente ya se haya rendido). Se agregó un reintento automático de 1 vez dentro del propio click handler, sin que Alfredo tenga que volver a dar clic a mano.
- **Analítica de tráfico del sitio público — pedida, explícitamente pospuesta:** Alfredo preguntó también "¿puedo ver cuántas veces visitan mi sitio, o quién?" — se explicó que "quién" en el sentido de identidad real nunca es posible (nadie inicia sesión para ver el sitio público) y se presentaron 3 caminos (contador propio en el admin, servicio externo tipo GoatCounter, Application Insights de Azure). Alfredo confirmó que le interesa pero no ahora — queda en "Pendientes de Alfredo" para retomar después, sin construir nada todavía.
- Probado con `.claude/launch.json` + `window.fetch` mockeado en consola: las 5 tarjetas abriendo/cerrando por clic en el encabezado completo con el chevron rotando, Tarifas/Respaldos cargando sus datos igual que antes tras el rediseño (sin regresión), Actividad mostrando 2 administradores de prueba con avatar/iniciales, correo, conteo y última vez formateada, y confirmado por red que `registrarAccesoAdmin` se dispara solo una vez al cargar `/admin`. Botón Despertar probado en sus 3 casos: falla-luego-éxito (2 llamadas, termina en palomita), éxito directo, y falla-falla (2 llamadas, termina en tachita sin quedarse colgado).
- **Pendiente para Alfredo:** confirmar en producción — que Viridiana aparezca en Actividad en cuanto entre por primera vez con su cuenta nueva, y que el botón Despertar ya no necesite el segundo clic manual con la base realmente dormida.

**En construcción — Grupos, primer módulo de Fase 2 Operación (2026-09-11, diseño y construcción el mismo día, pendiente confirmar en producción):**
- Reemplaza el Excel de control que Alfredo lleva hoy — un Grupo es una instancia real de curso entregada a un Cliente. Extiende `apcweb-backoffice` (misma base que Clientes/Solicitudes/Cotizaciones/Diplomas/Encuestas/Diagnóstico), reusando `Cliente`/`Contacto`/`Cotizacion` sin volver a crearlas. `cliente_id` es quien contrató, `cliente_final_id` es opcional (FK a `Cliente` también) — solo se llena en el caso de reventa/intermediario (ej. Capacitanet vende, Clarios recibe el curso). `herramientas`/`niveles` se guardan como JSON array de texto (mismo criterio de simplicidad que `temas_json` en Solicitud/Cotizacion) en vez de tablas de unión. `instructor` es texto plano sin FK, igual que Diplomas. **`eliminarGrupo` — ÚNICA excepción en todo el proyecto al criterio de "nunca borrar de verdad" (2026-09-12):** Alfredo lo pidió explícito para corregir errores de captura o limpiar pruebas mientras carga el backlog real de 2026 — ver "Ronda de pulido 3" más abajo para el detalle.
- **`sql/010_grupos.sql`** (corrido y confirmado 2026-09-11) — tabla `Grupo` completa (ver diccionario de campos en "Pendientes de Alfredo" → Grupos), envuelta en `IF NOT EXISTS` (mismo patrón idempotente que `sql/009`).
- **3 Functions nuevas** (v3 clásico, protegidas rol admin, ninguna con nombre que empiece "admin"): `crearGrupo`/`editarGrupo` (mismo `resolverCliente` que ya usan Solicitud/Cotización/Diploma/Encuesta — duplicado a propósito en cada uno — para Cliente Y Cliente final, ambos aceptan `{clienteId}` o `{nombre,codigo}` para dar de alta uno nuevo al vuelo) y `listGruposAdmin` (filtros `clienteId`/`herramienta`/`estatusCurso`/`estatusCierre`/`desde`/`hasta` sobre `fecha_inicio`, orden `fecha|cliente` con `dir=asc|desc`, mismo patrón `CASE WHEN` que `listSolicitudesAdmin`). `contacto_id` (si viene) se valida en el backend contra el Cliente correcto (el final si existe, si no el que contrató) — nunca se confía en lo que mande el front para esto, aunque el front ya aplica la misma regla al armar el `<select>` de Contacto.
- **A diferencia de Solicitudes, aquí Cliente y Cliente final SÍ son editables en modo edición** — decisión deliberada: un Grupo es control operativo propio de Alfredo, no hay el mismo riesgo de "cambiar de empresa a una venta ya cerrada" que justificó el candado de `editarSolicitud`. El formulario de edición prellena el autocompletado ya "seleccionado" (nuevo método `set()` en la copia de `crearAutocomplete` de este panel) pero deja el campo editable por si hace falta corregirlo.
- `admin/index.html` → sección nueva **Grupos** (grupo Operación del sidebar, junto a Diagnósticos/Encuestas/Diplomas), 2 pestañas mismo patrón `dash-etabs`/`dip-tabpanel` que el resto del admin:
  1. **Consultar Grupos** (default) — filtros (Cliente/Herramienta/Estatus del curso/Estatus de cierre/fechas) + orden bidireccional (Fecha de inicio/Cliente) + tabla (Cliente, Cliente final, Grupo/Código con toggle de detalle, Herramientas como badges de color por marca, Nivel, Fechas, Instructor, Estatus del curso, Estatus de cierre, Editar). Fila expandible (reusa `.sol-desglose-row`/`.sol-desglose` de Solicitudes) con Fotos RS/Correos ML/Pagado (ícono **y** texto siempre, nunca solo color, regla de siempre por la deuteranomalía de Alfredo), la Cotización de origen como link directo al PDF (`getCotizacionPdf?folio=`) si existe, nombre del curso y notas.
  2. **Crear Grupo** — formulario unificado alta/edición (`abrirFormGrupo(g)`, mismo patrón `abrirFormSolicitud`): Cliente + Cliente final (2 autocompletados independientes, cada uno con su propio código para dar de alta un cliente nuevo), Contacto (`<select>` que se recarga contra el Cliente final si está lleno, si no contra el Cliente — con "+ Nuevo contacto" inline igual que Solicitudes), Modalidad, Grupo/código, Herramientas y Nivel (pills multi-select — **a diferencia de las pills single-select que ya usaba el proyecto en Solicitudes/Cotizaciones, aquí cada pill se alterna sola sin limpiar a sus hermanas**, mismo componente visual `.sol-pill`/`.sol-pill-group`, comportamiento nuevo), Nombre del curso, Horas/Sesiones, Instructor (reusa `getInstructores`), Estatus del curso (default "Por iniciar")/Estatus de cierre, Cotización de origen (`<select>` que se llena solo al elegir el Cliente, vía `listCotizacionesAdmin?vista=todas&clienteId=`), Fotos RS/Correos ML/Pagado (`.radm-switch`, mismo componente que "Tiene WhatsApp"/"Contacto principal" de Contacto), Fecha de cierre, Notas.
- Probado de punta a punta con `.claude/launch.json` + `window.fetch` mockeado en consola (mismo patrón de siempre): alta de Grupo con Cliente Y Cliente final nuevos-por-nombre (escenario Capacitanet→Clarios del roadmap), confirmado que el Contacto se recarga contra el Cliente final en cuanto se elige y contra el Cliente si se limpia; validaciones de herramientas/nivel vacíos bloqueando el guardado con el mensaje correcto; edición precargando Cliente/Cliente final/Contacto/Cotización/fechas/pills/switches/notas exactos (incluyendo que las fechas `DATE` no sufren el corrimiento de un día que da `toISOString()` — se resuelve recortando los primeros 10 caracteres del ISO que regresa `mssql`, sin pasar por el huso horario del navegador); filtros y orden armando la URL de `listGruposAdmin` correctamente; fila expandible mostrando el link a la Cotización de origen. `node --check` limpio en las 3 Functions nuevas y sintaxis del `<script>` completo del admin verificada con Node.
- **Pendiente para Alfredo (no requiere a Claude):** confirmar el flujo completo en producción — sobre todo el caso de reventa/intermediario con un Cliente final real, y que el link a la Cotización de origen abra el PDF correcto.
- **Ronda de pulido (2026-09-11, segunda sesión), tras la primera prueba real de Alfredo — 6 pedidos concretos, todos iterados en texto antes de tocar código:**
  1. **Toggle "¿Hay Cliente final?"** — antes los campos de Cliente final siempre estaban visibles; ahora un switch los oculta por default (alta) o los muestra automáticamente si el Grupo que se edita ya tenía uno. Apagado, el campo se ignora por completo al guardar (nunca se manda `cliente_final` aunque queden datos viejos escritos en los inputs).
  2. **Bug real corregido — "+ Nuevo contacto" no se activaba con un Cliente nuevo:** el botón solo se habilitaba cuando `onChange` del autocompletado disparaba con un cliente YA existente (clic real en una sugerencia) — capturar nombre+código de un cliente nuevo sin dar ese clic nunca refrescaba el estado del Contacto. Se agregó `datosClienteEfectivoContacto()`/`refrescarEstadoContacto()` (con listeners de `input` en los 4 campos de texto de Cliente/Cliente final, no solo en `onChange`) y una Function nueva, **`contacto_nuevo`** en `crearGrupo`/`editarGrupo`: cuando el cliente todavía no existe, el mini-formulario de contacto no llama a `crearContacto` (no hay `cliente_id` real todavía) — guarda los datos en memoria y los manda como `contacto_nuevo:{nombre,correo,telefono}` junto con el Grupo; el backend crea Cliente, Contacto y Grupo en la misma transacción, o no crea ninguno.
  3. **Bug real de CSS encontrado en el camino — mismo patrón ya documentado del proyecto:** al esconder la fila de Cliente final con el atributo `hidden`, la fila seguía viéndose — `.radm-form-row{ display:flex }` (regla de autor) le ganaba a `[hidden]` (regla del user-agent, misma especificidad). Corregido con `.radm-form-row[hidden]{ display:none; }` explícito — ver "Ya resuelto", es la 2ª vez que aparece esta familia de bug en el proyecto (antes fue `.cot-alert`).
  4. **"Grupo / Código" → "Grupo"**, con hint `Ej. Grupo1, Grupo2...` — confirmado contra `sql/010_grupos.sql` que `grupo_codigo` no es `UNIQUE` ni lo referencia ninguna FK, es 100% de referencia libre para Alfredo, no hacía falta ningún cambio de esquema.
  5. **Herramientas/Nivel — pills reemplazadas por un dropdown colapsable con checkboxes** (`crearMultiselect()`, nuevo, genérico dentro del IIFE de Grupos): cerrado muestra un resumen de lo elegido (ej. "Excel, Power BI"), clic abre la lista de checkboxes, clic afuera cierra. Reemplaza `valoresPill`/`setPills`, que ya no se usan en este panel.
  6. **Campos "Correos ML" y "Pagado" eliminados por completo** — Alfredo confirmó que no aplican a su operación real. Se optó por tirar las columnas de verdad de la tabla (no dejarlas huérfanas sin usar) — `sql/012_grupo_quita_columnas.sql` (**corrido y confirmado 2026-09-11**) tira primero los `DEFAULT CONSTRAINT` autogenerados de ambas columnas (SQL Server no deja tirar una columna con default sin tirar antes su constraint) y luego las columnas, todo idempotente. Quitados de `crearGrupo`/`editarGrupo`/`listGruposAdmin` y del admin (formulario, prefill, payload, fila expandible).
  7. **Cotización de origen "sin datos" — no era un bug**: Alfredo confirmó que no había dado clic en la sugerencia del autocompletado al elegir el Cliente (mismo mecanismo de fondo que el punto 2) — sin cambios de código para este punto específico.
  - Probado en el navegador local (`.claude/launch.json` + `window.fetch` mockeado, capturando el payload real enviado a `crearGrupo` en cada corrida): alta con cliente nuevo sin contacto (payload limpio, sin `correos_ml`/`pagado`), alta con cliente nuevo + contacto nuevo (`contacto_nuevo` armado correctamente, `contacto_id` en `null`), edición de un Grupo con Cliente final ya existente (toggle prendido solo, multiselects de Herramientas/Nivel precargados con el resumen correcto, Cotización de origen precargada). `node --check` limpio en `crearGrupo`/`editarGrupo`/`listGruposAdmin`, sintaxis del `<script>` del admin verificada con Node.
  - **Pendiente para Alfredo:** confirmar en producción, sobre todo el flujo de cliente nuevo + contacto nuevo junto con la creación real del Grupo.
- **Segunda ronda de pulido (2026-09-12), 6 pedidos más — iterados en texto primero, con 2 preguntas resueltas vía `AskUserQuestion` antes de tocar código:**
  1. **Sesiones deja de ser un conteo (`INT`) y pasa a texto libre** (ej. "5 Sesiones Jueves de 2 a 6 pm") — `sql/013_grupo_sesiones_texto.sql` (**corrido y confirmado 2026-09-12**, `ALTER COLUMN` a `NVARCHAR(150)`, sin pérdida de datos) + `crearGrupo`/`editarGrupo` ya no validan que sea un entero.
  2. **Toggle "¿Más de 1 Grupo?"** — revela el campo Grupo (antes siempre visible); "el Grupo es consecutivo cuando nos contratan más de 1 grupo a la vez", confirmado que no es llave de nada (ver ronda anterior).
  3. **Estatus de cierre — ya no dice "(vacío)"**: el `<select>` nace deshabilitado y solo se activa cuando Estatus del curso = Terminado, arrancando en "Proyecto" (la misma lectura que ya hacía `derivarFase` para un cierre vacío) en vez de quedar en blanco. Se descartó agregar un estado nuevo "En Proceso" — hubiera chocado de nombre con el valor "En proceso" que ya usa Estatus del curso, dos cosas distintas. Sin cambios de esquema (el `CHECK` constraint ya permitía `NULL`).
  4. **Nombre del curso — datalist de sugerencias**: Alfredo pidió explícitamente que la fuente sea el propio historial de `Grupo.nombre_curso` ("aún no hay pero habrá", crece solo conforme se den de alta Grupos) — no el catálogo de Cursos/TemariosEstandar. Function nueva `listNombresCursoAdmin` (`SELECT DISTINCT nombre_curso`), sigue siendo texto 100% libre, el datalist solo ayuda a no repetir el mismo nombre con variaciones de espacios/acentos.
  5. **4 notas fijas al final del formulario** (mismo lugar donde ya vivía la única nota de antes, no arriba — Alfredo prefirió no moverla, y aclaró que rediseñará las notas de Encuestas después con un criterio distinto): un solo bloque `.radm-hint` con salto de línea (`<br>`) entre cada una, en vez de una nota-por-campo repartida por el formulario.
  6. **Los 4 toggles (Cliente final / Más de 1 Grupo / Tiene Cotización / Curso histórico) se agruparon en una sola tarjeta al inicio del formulario**, antes de cualquier campo — "Tiene Cotización" revela Cotización de origen (ya sin el "(opcional)" del label) y "Curso histórico" revela Fecha de cierre. Confirmado con Alfredo: para un Grupo nuevo, Fecha de cierre **no debe verse en este formulario en absoluto** — se llena sola cuando la fase avanza a "Cerrado" vía el stepper (`avanzarFaseGrupo`, ya construido); "Curso histórico" es solo para el backlog real de 2026 que Alfredo va a cargar con la fecha de cierre ya conocida.
  - **Bug de CSS encontrado de paso, mismo patrón ya documentado dos veces — ahora en `.radm-field`:** al esconder campos individuales (Grupo/Cotización/Fecha de cierre, a diferencia de la ronda anterior que escondía la fila `.radm-form-row` completa), `[hidden]` volvía a perder contra `.radm-field{ display:flex }`. Corregido con `.radm-field[hidden]{ display:none; }` — mismo criterio ya anotado en "Ya resuelto": cualquier clase de este admin con `display` propio necesita su override `[hidden]` desde que se escribe.
  - En modo edición, los 3 toggles nuevos se prellenan según si el Grupo ya tiene dato (`grupo_codigo`/`cotizacion_id`/`fecha_cierre`) — si ya existe, el toggle nace prendido y el campo visible.
  - Probado en el navegador local (`.claude/launch.json` + `window.fetch` mockeado): los 4 toggles revelando/ocultando sus campos, Estatus de cierre bloqueado en alta y desbloqueándose a "Proyecto" al cambiar Estatus del curso a Terminado, el datalist de Nombre del curso poblado desde el mock, y la nota de 4 líneas confirmada en el DOM. `node --check` limpio en `crearGrupo`/`editarGrupo`/`listNombresCursoAdmin`, sintaxis del `<script>` del admin verificada con Node.
  - **Pendiente para Alfredo:** correr `sql/013_grupo_sesiones_texto.sql`; confirmar en producción, sobre todo el datalist de Nombre del curso una vez que ya haya un par de Grupos reales guardados.
- **Ronda de pulido 3 (2026-09-12), la más grande de las tres — reestructura Consultar Grupos y separa Editar/Mover en su propia pestaña:**
  1. **Toggles horizontales sin paréntesis** en Crear/Modificar Grupo (antes apilados verticales con explicación larga) y **notas con viñeta cuadrada** (`<ul style="list-style-type:square">`, antes `<br>` sueltos) — mismo lugar de siempre, al final del formulario.
  2. **Filtro de Cliente ya buscaba por código también** — `buscarClientes` ya hacía `WHERE nombre LIKE @q OR codigo LIKE @q` desde que se construyó, solo hacía falta el label ("CLIENTE O CÓDIGO"); cero cambios de backend.
  3. **Filtros Desde/Hasta eliminados**, botones Buscar/Ver todos eliminados — **búsqueda automática**: los `<select>` disparan `buscarGrupos()` en su evento `change`, el campo de texto lo hace con un debounce de 350ms (para no lanzar una búsqueda por cada tecla) vía `buscarGruposDebounced()`. "Limpiar filtros" quedó como ícono solo (embudo tachado) dentro de la misma fila de filtros, y al limpiar ya dispara la búsqueda sola (mostrando todo).
  4. **Filtrar y Ordenar, cada uno en su renglón dentro de la misma tarjeta con borde** (`.sol-toolbar`, patrón que YA existía) — se reemplazó el texto "ORDENAR" y el ícono de lupa de "Buscar" por un ícono de embudo (filtrar) y uno de flechas arriba/abajo (ordenar), sin texto. **3 sort nuevos: Herramienta/Instructor/Fase** — el backend (`listGruposAdmin`) solo ordena por `fecha`/`cliente`; con el volumen real de Grupos (decenas, no miles) fue más simple reordenar estos 3 en el cliente (`ordenarListaGrupos()`, usa el array `FASES` ya existente para el orden de fase) que replicar la regla en SQL — sin cambios de backend.
  5. **Tabla: "Grupo / Código" → "Grupo"**; **Estatus del curso + Estatus de cierre → una sola columna "Fase"** reusando tal cual `window.FaseStepper.render({fase, variant:'mini'})`, el mismo componente que ya pinta Tracking Operación — barato, cero componente nuevo; **columna Horas agregada** (el dato ya lo regresaba `listGruposAdmin`, solo faltaba mostrarlo).
  6. **Acciones separadas en 3 — Editar (solo el formulario, sin el stepper) / Mover (solo el stepper, sin el formulario) / Eliminar** — nueva pestaña sin botón propio en la barra, **"Modificar Grupo"** (`gruTab-modificar`, se llega solo desde una fila, nunca navegando directo, "para no mezclar" alta con edición). Como el formulario de Crear y el de Modificar comparten los mismos ids fijos (`gruClienteNombre`, etc.), `abrirFormGrupo(g, targetWrapId)` ahora recibe a qué wrap pintar y **limpia el otro** (`gruFormWrap`/`gruModificarWrap`) para que nunca haya 2 copias del mismo id en el DOM a la vez. `abrirStepperGrupo(g)` es la vista nueva de solo el stepper (mismo `window.FaseStepper` interactivo que antes vivía arriba del formulario de edición, ahora aislado).
  7. **Navegación de regreso consciente del origen** — `gruOrigenModificar` ({panel, tab}) se fija en `abrirModificarGrupo(g, modo, origen)` antes de abrir Editar/Mover; "Cancelar", "← Volver" (del stepper) y el guardado exitoso llaman a `volverDeModificar()`, que manda a Consultar Grupos O a Tracking Operación según de dónde vino — Tracking Operación ganó su propia acción "Mover" (antes solo tenía "Editar") para que esto tuviera sentido desde ahí también.
  8. **`eliminarGrupo` — Function nueva, ÚNICA excepción del proyecto a "nunca borrar de verdad":** Alfredo lo pidió explícito para corregir capturas erróneas o limpiar pruebas mientras carga el backlog real de 2026. Borra `GrupoFaseHistorial` del grupo primero (FK sin `ON DELETE CASCADE`, ver `sql/011` — se diseñó asumiendo que Grupo nunca se borraría) y luego el `Grupo`, en una transacción. Confirmación con `window.confirm()` en el front (mismo patrón ya usado en el proyecto para avanzar fase/actualizar tarifas) antes de llamar a la Function — nombra el Grupo y el Cliente, avisa que es borrado real e irreversible.
  9. **Bug real encontrado y corregido de paso — Tracking Operación "no se actualizaba":** KPIs/alerta/tabla solo se cargaban UNA vez, al abrir `/admin` (`cargarKpisGrupos(); buscarGruposTracking();` sueltas al final del IIFE) — navegar ahí desde el sidebar después de cambiar algo en otro panel seguía mostrando los números viejos, porque nada volvía a llamarlas. Corregido con un listener en el botón del sidebar que refresca ambas cada vez que se hace clic ahí.
  - Probado en el navegador local (`.claude/launch.json` + `window.fetch`/`window.confirm` mockeados, contando llamadas de red para verificar comportamiento, no solo lo visual): el filtro de texto no dispara ninguna búsqueda hasta 350ms después de la última tecla (3 cambios seguidos → 0 búsquedas a media espera, 1 al final); un `<select>` sí dispara de inmediato; orden por Herramienta reacomoda la tabla correctamente; Editar abre solo el formulario (sin stepper) y "Cancelar" regresa a Consultar Grupos; Mover abre solo el stepper y "← Volver" regresa a Consultar Grupos; desde Tracking Operación, Mover regresa a Tracking Operación (con sus KPIs/tabla ya refrescados); Eliminar dispara el `confirm()` con el texto correcto; clic directo en "Tracking Operación" del sidebar dispara exactamente 1 llamada a `getResumenGruposAdmin` y 1 a `listGruposAdmin`. `node --check` limpio en `eliminarGrupo`, sintaxis del `<script>` del admin verificada con Node.
  - **Pendiente para Alfredo:** confirmar en producción — sobre todo el flujo completo Editar/Mover/Eliminar desde ambos orígenes (Consultar Grupos y Tracking Operación), y que el fix de refresco de Tracking Operación ya se sienta al día.

**En construcción — Tracking Operación + stepper de fase con historial (2026-09-11, segunda sesión constructora el mismo día que Grupos, pendiente confirmar en producción):**
- Extiende Grupos (no lo reemplaza) con una "fase" operativa de 6 paradas — Por iniciar → En curso → Proyecto → Calificaciones → Diplomas → Cerrado — derivada de `estatus_curso`/`estatus_cierre` (no es una columna nueva). Nueva tabla **`GrupoFaseHistorial`** (`sql/011_grupo_fase_historial.sql`, corrido y confirmado 2026-09-11, FK real a `Grupo` — nunca huérfana, porque Grupo nunca se borra) guarda cuándo se cruzó cada parada.
- **`api/src/grupo-fase.js`** — única fuente de verdad de la regla, usada por los 3 lugares que tocan fase, ninguno la reimplementa: `derivarFase(estatusCurso, estatusCierre)`, `estatusDeFase(fase)` (mapeo inverso), `registrarCambioFase(transaction, grupoId, faseNueva, faseAnterior)` (inserta solo si cambió, fecha = ahora) y `sembrarHistorialInicial(...)` (solo para alta — ver regla de abajo).
- **Regla de siembra al crear un Grupo — con un matiz importante encontrado al diseñarla:** si el Grupo nace en "Por iniciar" (el caso normal, curso que no ha arrancado) se siembra un solo renglón con la fecha REAL de alta (ahora) — nunca con `fecha_inicio`, que casi siempre es una fecha futura y hubiera sido una fecha inventada. Si nace ya avanzado (carga retroactiva de un Excel viejo — `estatus_curso`/`estatus_cierre` ya reflejan una fase más allá de "Por iniciar") sí se infieren las fechas reales de cada parada ya cruzada: `fecha_inicio` para Por iniciar/En curso, `fecha_fin` para Proyecto, `fecha_cierre` para Cerrado; Calificaciones/Diplomas no tienen un campo de fecha propio en `Grupo`, así que si la fase inicial las rebasa se OMITEN por completo (sin fila), nunca se inventa un "ahora" para una parada que en realidad ya pasó hace tiempo.
- **`crearGrupo`/`editarGrupo`** ahora envuelven su INSERT/UPDATE junto con la llamada a `grupo-fase.js` en la misma `sql.Transaction` — o quedan ambos (el Grupo y su historial), o ninguno. `editarGrupo` calcula la fase con los datos viejos (un SELECT antes del UPDATE) y con los nuevos, y si cambió, la loguea — así corregir el Grupo directo desde los `<select>` de estatus también deja el historial al día, no solo el stepper interactivo.
- **`avanzarFaseGrupo`** (nueva Function, protegida, ligera a propósito) — lo que dispara el stepper interactivo: recibe `{grupoId, faseNueva}`, valida en el backend que sea la fase INMEDIATA siguiente (nunca confía en que el front ya solo deje clicar esa), traduce a `estatus_curso`/`estatus_cierre` reales, y si la fase nueva es "Cerrado" y no hay `fecha_cierre` capturada todavía, la llena sola con hoy — así el stepper es un atajo real, no solo cosmético.
- **`listGruposAdmin`** ahora regresa 2 campos calculados por fila: `fase` (siempre vía `derivarFase`, nunca reimplementada en SQL) y `dias_en_fase` (subconsulta correlacionada `MAX(fecha)` contra `GrupoFaseHistorial`). Ganó también `?vista=activos|cerrados` (opcional — Consultar Grupos sigue sin mandarlo) filtrando por `estatus_cierre`, no por la fase derivada — se documentó en el código por qué esa condición es equivalente.
- **Nueva Function `getHistorialFaseGrupo`** (`?grupoId=`) — carga perezosa, solo se pide la primera vez que se expande una fila en Consultar Grupos (mismo criterio que `listContactosAdmin`/`getRecursosAdmin`); alimenta la variante "expandido con fechas" del stepper.
- **Nueva Function `getResumenGruposAdmin`** — a diferencia de `getResumenCotizacionesAdmin` (que tuvo que aproximar su "tiempo de cierre" porque `Cotizacion` no tiene fecha de cierre real), aquí el cálculo es exacto: `AVG(DATEDIFF(day, fecha_fin, fecha_cierre))` sobre los ya Cerrados. "Activos"/"Cierre pendiente" se calculan con una condición simple sobre `estatus_cierre` (no reimplementando la tabla de reglas en SQL) — documentado en el código por qué es equivalente a la fase derivada.
- **`window.FaseStepper`** (nuevo componente compartido, mismo criterio que `window.TemarioSelector`) — `render({fase, historial, variant})` pinta las 3 variantes del mockup (cuadrados conectados por línea, nunca círculos): `mini` (fila de la tabla de Tracking Operación — sin fechas, solo posición + nombre de fase en texto debajo), `expandido` (fecha por parada, fila expandible de Consultar Grupos), `interactivo` (el nodo inmediato siguiente es clicable, borde punteado + tooltip "Toca para avanzar"). `wireInteractivo(containerEl, onAvanzar)` engancha el clic tras insertar el HTML — nunca llama la API él mismo, deja que quien lo use decida qué hacer tras el `confirm()`.
- **Nuevo panel Tracking Operación** (reemplaza el placeholder "PRONTO" del sidebar, ahora con `.admin-nav-live-dot` pulsando — ya tiene datos reales) — **vive en el MISMO IIFE que Grupos**, mismo criterio ya establecido por Tracking Comercial/Cotizaciones (reusa TOOL_META/NIVEL_LABEL/escapeHtml/apiGet*/`abrirEdicionGrupo` sin duplicar nada; `toast()` se volvió panel-aware, igual que ya lo era en Cotizaciones, para no quedar oculto en el panel no activo):
  1. Scorecard de 5 KPIs (`.cot-kpis`/`.cot-kpi`, reusado) — Grupos activos, En curso ahora, Por iniciar, Cierre pendiente (resaltado en ámbar), Tiempo promedio de cierre.
  2. Alerta ámbar (`.cot-alert`, reusado) — grupos en Proyecto/Calificaciones/Diplomas con `dias_en_fase > 21`, nombra hasta 2 + "y N más".
  3. Tabs Activos (default)/Cerrados/Todos (`.radm-tabs`, reusado) + filtros Cliente/Herramienta/fechas.
  4. Tabla: Cliente (+ Cliente final si aplica), Grupo, Herramientas (badges), **Fase** (stepper mini + nombre de fase en texto debajo — nunca solo la posición del stepper), **Semáforo** (`.cot-semaforo`, reusado — verde `<10`/ámbar `10-21`/rojo `>21` días, celda vacía si la fase no es de cierre, nunca un color sin significado), Instructor, Editar (llama a `abrirEdicionGrupo(g)` de Grupos — cambia de panel y abre el mismo formulario con el stepper interactivo arriba).
- **Editar Grupo** — el stepper interactivo vive arriba del formulario completo (solo en modo edición), con un tag "X días en esta fase". Al avanzar, refleja el cambio de inmediato en pantalla (stepper + `<select>` de estatus + tag) sin tener que recargar el Grupo completo — usa una copia en el front de `estatusDeFase` (`FASE_A_ESTATUS`) solo para esto.
- Probado de punta a punta con `.claude/launch.json` + `window.fetch`/`window.confirm` mockeados en consola (espejo fiel de la lógica real de `grupo-fase.js`, incluyendo la rama de siembra retroactiva): alta normal (fase "Por iniciar", 1 fila de historial con "ahora", no con `fecha_inicio`); alta retroactiva a "Proyecto" (3 filas con fechas reales de `fecha_inicio`/`fecha_fin`) y a "Cerrado" (4 filas, Calificaciones/Diplomas omitidas sin fecha inventada); `editarGrupo` logueando un cambio de fase real y NO logueando nada cuando la fase no cambia; clic en el stepper interactivo con `confirm()` aceptado (avanza, loguea, actualiza pantalla) y cancelado (no llama la API); el candado del backend rechazando un salto de fase no inmediato; los 3 colores del semáforo y su ausencia en Por iniciar/En curso/Cerrado; scorecard con números exactos verificados a mano; alerta con el cliente correcto; los 3 tabs filtrando bien; navegación cruzada Tracking Operación → Editar → panel Grupos con el stepper interactivo ya cargado. `node --check` limpio en las 4 Functions nuevas/tocadas y en `src/grupo-fase.js`, sintaxis del `<script>` completo del admin verificada con Node.
- **Pendiente para Alfredo (no requiere a Claude):** confirmar el flujo completo en producción — sobre todo el stepper interactivo avanzando un Grupo real, y que el semáforo/alerta de Tracking Operación reflejen datos reales una vez que haya Grupos con más historia.

**Publicado y en producción — Pendientes, captura rápida de notas (2026-09-11, mismo día que Grupos/Tracking Operación):**
- Herramienta personal de Alfredo para no perder ideas/pendientes que hoy anota en Excels sueltos o libreta — mockup aprobado tal cual: `https://claude.ai/code/artifact/39cc5d78-bd6f-4750-9505-975df610c4bd`. **Deliberadamente en Table Storage, no en la base SQL del backoffice**: el punto de esta herramienta es capturar al instante, sin el auto-pause de 20-60s que sí se tolera en el resto del backoffice (Diplomas/Encuestas/Solicitudes/Cotizaciones/Diagnóstico/Grupos).
- Nueva tabla **`Pendientes`** en la cuenta ya existente `apcwebrecursos` (misma `RECURSOS_STORAGE_CONNECTION` que ya usan Cursos/Recursos/Temas), auto-creada por código (`api/src/pendientes-tables.js`, mismo patrón `ensureTable`/`isTableNotFound` que `cursos-tables.js`) — Alfredo no tocó el portal para este módulo. `PartitionKey` = una de 5 categorías fijas (`comercial`/`operacion`/`productos`/`marca`/`sitio`, sin más por ahora), `RowKey` = `crypto.randomUUID()`, propiedades `texto`/`fecha_creacion`/`archivado`/`fecha_archivado`.
- **4 Functions** (v3 clásico, protegidas rol admin): `listPendientes` (trae las 5 particiones completas de un jalón, activas + archivadas — el volumen es mínimo, el front separa por categoría y ordena por `fecha_creacion` descendente), `crearPendiente`, `editarPendiente` (solo actualiza `texto`), `archivarPendiente` (togglea `archivado` — booleano, no siempre `true`, así "ver archivados" también desarchiva con el mismo endpoint; al pasar a `true` guarda `fecha_archivado` = ahora, al pasar a `false` la limpia a `null`). **Sin `eliminarPendiente` a propósito** — mismo criterio de "nunca borrar de verdad" que ya usa todo el proyecto (Cliente/Diploma/Solicitud/Grupo), archivar (reversible) basta.
- `admin/index.html` → ítem nuevo **Pendientes** en el sidebar, suelto (sin grupo Comercial/Operación/Productos), al mismo nivel que Dashboard/Tracking Comercial/Tracking Operación, justo debajo de Productos — ícono de notepad. Panel calcado del mockup: grid responsivo de 5 contenedores fijos (`auto-fit, minmax(215px,1fr)`), cada uno con ícono+nombre+contador de activas; tarjeta con texto+fecha (mono, discreta)+checkbox cuadrado (clic → archiva/desarchiva de inmediato, sin `confirm()` — reversible, no necesita esa fricción)+lápiz de editar que solo aparece al hover; clic en el texto o el lápiz convierte la tarjeta en un `<textarea>` inline con Guardar/Cancelar; "+ Agregar" al fondo abre el mismo tipo de textarea inline, la nota nueva aparece arriba de la lista; "ver archivados (N)" expande la lista de archivadas debajo (mismo diseño de tarjeta, checkbox ya marcado); estado vacío ("Sin pendientes por ahora.") cuando una categoría no tiene notas activas. Todo el HTML de las 5 columnas se construye en JS desde una sola fuente (`CATEGORIAS`, con nombre+ícono por categoría) — nada hardcodeado por separado.
- **Sin panel de filtros/orden ni scorecard** — a propósito, esto no es un módulo de reportes, es una libreta; la única "vista" es la rejilla completa siempre visible.
- **Resiliencia agregada sin que el prompt lo pidiera explícitamente:** si `listPendientes` falla al cargar (ej. problema de red pasajero), el panel ya NO se queda bloqueado con un mensaje de error sin salida — se cae a una rejilla vacía y usable (se puede seguir agregando notas normalmente) más un toast avisando del problema. Encontrado revisando el flujo de error mientras se probaba con `window.fetch` mockeado — antes de este ajuste, cualquier fallo real de red hubiera dejado el panel completamente inutilizable hasta recargar la página.
- Probado de punta a punta con `.claude/launch.json` + `window.fetch` mockeado en consola (mismo patrón de siempre): alta de una nota en cada una de las 5 categorías, edición de una existente (confirmado que el texto se actualiza y el modo edición se cierra) y su cancelación (confirmado que descarta el cambio sin llamar a la API), archivar una nota (desaparece de la lista activa, sube "ver archivados", el checkbox queda marcado en la lista de archivadas) y desarchivarla desde ahí (regresa a la lista activa, `fecha_archivado` se limpia), y el estado vacío de una categoría sin notas. `node --check` limpio en las 4 Functions nuevas y en `src/pendientes-tables.js`, sintaxis del `<script>` completo del admin verificada con Node.
- **Pendiente para Alfredo (no requiere a Claude):** confirmar el flujo completo en producción — no hay ningún script SQL que correr para este módulo, la tabla se crea sola en cuanto se guarda la primera nota.
- **Ronda de pulido, mismo día, ya con Alfredo probando el módulo:** 3 pedidos concretos — el checkbox casi no se veía en ningún tema, quería poder arrastrar las notas para priorizar (la más urgente arriba de cada contenedor) y un botón de ordenar por fecha. Alfredo cerró el bloque después de esto ("me gusta como quedó, me es muy útil").
  - **Checkbox:** el borde usaba `var(--line)` sobre `var(--panel-2)` — casi el mismo tono en ambos temas, prácticamente invisible sin marcar. Corregido a `border:2px solid var(--text-dim)` sobre `var(--panel)`, con hover a `--blue-bright` — el estado marcado (fondo `--blue-bright` + palomita) ya tenía buen contraste y no se tocó.
  - **Drag & drop, mismo patrón ya probado del proyecto (SortableJS, `handle`+`onEnd` reindexando a `0,10,20…`, igual que Cursos/Temas/Proyectos/Recursos):** cada tarjeta activa suma un grip de 6 puntos a la izquierda (visible solo al hover, como el lápiz de editar) que es el `handle` — arrastrar reordena SOLO dentro de la misma columna, nunca entre categorías. Nuevo campo `orden` en la tabla `Pendientes` y Function nueva `updateOrdenPendientes` (mismo patrón que `updateOrden`/`updateOrdenCursos`, pero acotada a una categoría por llamada). `crearPendiente` siembra `orden:-Date.now()` para que la nota nueva siga apareciendo arriba por default (mismo comportamiento de siempre) hasta que Alfredo la reordene a mano — el primer drag en una columna reindexa TODAS sus tarjetas de una vez, así que las notas viejas sin este campo (o con el valor negativo por default) quedan bien ordenadas en cuanto se arrastra cualquiera. El front resuelve el campo `orden` ausente (notas ya creadas antes de este cambio) como `-fecha_creacion`, mismo efecto.
  - **Botón "ordenar por fecha":** ícono nuevo en el encabezado de cada columna (solo aparece con 2+ notas activas) que alterna asc/desc y persiste el resultado como `orden` — reusa el mismo endpoint `updateOrdenPendientes` del drag, no es un modo de vista aparte ni un campo de estado nuevo que mantener.
  - Aprovechado el mismo cambio para agregar el separador que Alfredo pidió por separado (ver abajo) entre Productos y Pendientes en el sidebar.
  - Probado en el navegador local (`.claude/launch.json` + `window.fetch` mockeado): contraste del checkbox en ambos temas, el grip de arrastre apareciendo solo al hover, `Sortable.get()` confirmando que la instancia se reengancha en cada `render()`, y el botón de ordenar alternando correctamente (con su toast "más antiguo primero"/"más reciente primero"). El drag físico en sí no se pudo simular de forma confiable con el mouse automatizado del navegador de pruebas (limitación conocida de SortableJS con clics sintéticos) — se validó por inspección de que usa exactamente el mismo patrón `Sortable.create({handle, onEnd})` ya en producción en Cursos/Recursos, no por una corrida real de arrastre.
  - **Separador de sidebar (pedido aparte, mismo momento):** el botón "Pendientes" vivía pegado justo debajo de "Recursos" sin ningún corte visual, así que se leía como un módulo más de Productos aunque conceptualmente es un ítem suelto (ver arriba, "no pertenece a ningún pilar"). Se agregó una clase `.admin-nav-standalone` (línea + espacio arriba, mismo lenguaje que ya usa el pie del sidebar) al botón de Pendientes — sin tocar el resto del sidebar.
  - **Pendiente para Alfredo:** confirmar en producción, sobre todo el drag real con mouse/touch (la prueba automatizada no lo cubrió) y que el botón de ordenar se sienta bien en las columnas con más de 2-3 notas.

**Publicado y en producción — Modelo, visor del esquema completo del sitio (2026-09-11):**
- Panel nuevo dentro de `/admin` (ítem suelto del sidebar, debajo de Pendientes) — nace del diagrama publicado primero como Artifact aparte (`https://claude.ai/code/artifact/64be899d-0c26-43a8-b9a8-7ce1cea32279`), embebido en el propio sitio para que Alfredo no dependa de un link externo a claude.ai. Nace de que Alfredo pidió verlo dentro del admin "como la vista de Modelo de Power BI".
- **Decisión 2026-09-11 — el panel del admin es la ÚNICA fuente que se mantiene de aquí en adelante:** Alfredo confirmó explícitamente que solo le interesa el de `/admin` — el Artifact público queda congelado tal como está (no se vuelve a actualizar cuando se agreguen tablas nuevas, y puede quedar desactualizado respecto al esquema real sin que eso sea un problema). Cualquier sesión futura que agregue tablas/relaciones al modelo debe tocar solo el arreglo `TABLES`/`RELATIONS`/`ZONES` dentro del IIFE "PANEL MODELO" de `admin/index.html` — no hace falta replicar el cambio en el Artifact ni mantenerlo sincronizado.
- **Las 23 tablas del modelo real** (15 en Azure SQL `apcweb-backoffice`, 8 en Table Storage `apcwebrecursos`) con sus campos/tipos/PK/FK exactos y las **30 relaciones** entre ellas, arrastrables e interactivas (zoom, pan, buscador, clic para resaltar relaciones, leyenda de los 3 tipos de línea — FK real/sin FK/foto-snapshot). Reusa los tokens de color del sitio (solo 5 variables `--mdl-*` nuevas, una por "pilar" del diagrama) e IDs/clases prefijadas `mdl-` para no chocar con el resto del admin, que ya es un archivo grande con muchos paneles.
- **Botón de actualizar (pedido explícito de Alfredo, no estaba en el Artifact original):** Function nueva `getModeloEstadisticas` (protegida, rol admin) trae filas + última fecha de cada tabla — un solo `UNION ALL` para las 15 tablas SQL, `listEntities()` para las 8 de Table Storage (sin `COUNT` nativo ahí, hay que recorrer las entidades). Las 6 tablas SQL sin ninguna columna de fecha en su esquema (Cliente, TarifaHerramienta, Alumno, EncuestaPregunta, EncuestaRespuestaDetalle, DiagnosticoRespuestaDetalle) muestran solo el conteo, nunca una fecha inventada; Table Storage reporta el `Timestamp` de sistema como "última actividad" (no "última fecha" — puede ser una edición, no forzosamente una fila nueva).
- **Deliberado: las estadísticas no se cargan solas al abrir el panel** — solo al pulsar el ícono de refrescar (mismo look que el botón "Despertar" del topbar, reusa `.admin-icon-btn`/`.despertando`). Mismo criterio que Tarifas/Respaldos en Configuración: evita despertar la base SQL serverless o recorrer Table Storage completo solo por entrar a ver el diagrama.
- Sin ningún script SQL que correr — la Function solo lee, no crea ni modifica ninguna tabla.
- **Pendiente para Alfredo (no requiere a Claude):** confirmar en producción — el diagrama se ve/arrastra igual que en el Artifact, y "Actualizar" trae números reales.

**Pendiente / roadmap:** ver la sección "Modelo del negocio y roadmap estratégico" arriba para Diagnóstico, backoffice/CRM y todo el plan de fases — es la versión vigente, reemplaza cualquier plan anterior (incluidos los documentos `Memoria_...` del Project de claude.ai en lo que toque a fases/orden). Los pendientes puntuales (contenido, marketing, deuda técnica) viven todos en "Pendientes de Alfredo" al inicio del documento — un solo lugar, no repetido aquí.

**Ya resueltos** (no reabrir salvo pedido explícito):
- **SQL — un `ALTER TABLE ... ADD columna` y un `CHECK` que referencia esa misma columna necesitan estar en batches distintos (`GO` de por medio):** `sql/007_solicitud_datos_cotizacion.sql` agregaba 4 columnas y luego, en el MISMO batch, un `ALTER TABLE ADD CONSTRAINT CHECK (modalidad IN (...))` — Alfredo lo corrió en el Query Editor de Azure y tronó con `Invalid column name 'modalidad'`, aunque la sintaxis era correcta: dentro de un mismo batch, SQL Server no reconoce todavía una columna agregada por un `ALTER TABLE` anterior en ese mismo batch. Se corrigió separando las 4 `ALTER TABLE...ADD columna` de las 2 `ALTER TABLE...ADD CONSTRAINT CHECK` con un `GO` de por medio. De paso, como el batch de columnas ya se había ejecutado antes de fallar, el script se reescribió para ser **idempotente** (`IF COL_LENGTH(...) IS NULL` / `IF NOT EXISTS (SELECT 1 FROM sys.check_constraints WHERE name = ...)`) — seguro de volver a correr aunque ya haya avanzado a medias. Regla para scripts SQL futuros de este proyecto: si un `ALTER TABLE ADD` y algo que referencia la columna nueva (`CHECK`, un `UPDATE`, etc.) van en el mismo script, sepáralos con `GO`.
- **CSS — no mezclar el shorthand `border-color` con un long-hand de un solo lado (`border-left-color`, etc.) en la misma regla:** en el panel Clientes, `.cli-row.active`/`.cli-detail.seleccionado` combinaban `border-color:var(--x)` (4 lados) con `border-left-color:var(--x)` justo después para dar más grosor al lado izquierdo — el navegador terminaba calculando los otros 3 lados como inválidos/vacíos (confirmado inspeccionando `document.styleSheets`, no era solo un problema de repintado) y el borde de selección nunca se pintaba. Corregido usando el shorthand completo en ambos lados: `border:...; border-left:...;` en la regla base Y en la de estado — nunca `border-color` + un long-hand de un solo lado en la misma regla.
- **CSS — el shorthand `background` con `color-mix(in srgb, var(--x) N%, transparent)` también puede colapsar a transparente del todo, igual que el caso de `border-color` de arriba — usar `background-color` en su lugar:** `.radm-btn-add` (usado en "+ Nuevo Cliente", "+ Nueva Solicitud", "+ Nuevo contacto", etc.) llevaba `background:color-mix(in srgb, var(--c) 12%, transparent)` — en la hoja de estilos real (no en una prueba aislada, que sí funcionaba) el navegador calculaba `background-color` como vacío/transparente del todo, y combinado con que `.radm-btn-add:disabled` no tenía NINGÚN estilo propio (mismo color y opacidad que habilitado), el botón deshabilitado "+ Nuevo contacto" de Solicitudes se veía idéntico al habilitado — Alfredo lo reportó como "no funciona" cuando en realidad estaba deshabilitado a propósito (sin cliente existente seleccionado) pero invisible-mente así. Corregido en dos partes: (1) `background` → `background-color` en `.radm-btn-add` y su `:hover`, arreglando el tinte en TODO el admin, no solo Solicitudes; (2) agregado `.radm-btn-add:disabled{ opacity:.4; cursor:default; background-color:transparent; }`, que no existía. Regla ampliada: cualquier `border`/`background` shorthand que mezcle `var()` con `color-mix()` es sospechoso en esta hoja de estilos — preferir siempre el long-hand específico (`border-color`+`border-width`+`border-style` por separado, o `background-color`).
- **CSS — cualquier panel que use `.radm-btn`/`.radm-btn-add` necesita declarar `--c` (ej. `style="--c:var(--blue-bright)"` en el `<div class="admin-panel">`), o esos botones pierden el borde/color por completo:** `panel-config` nunca declaró `--c` (a diferencia de Encuestas/Diplomas/Clientes, que sí lo hacen) — los botones "Desplegar" de Tarifas/Respaldos y "Respaldar ahora" se veían sin borde, "como una palabra suelta" (reporte textual de Alfredo). Corregido agregando `--c` al panel completo. Revisar esto primero si un botón nuevo en un panel se ve sin borde/color.
- **CSS — un selector descendiente compuesto (`.padre.estado .hijo`) puede no aplicarse de forma confiable en una hoja de estilos grande, aunque `querySelector`/`.matches()` confirmen que sí calza:** pasó con el chevron animado de "Desplegar" en Configuración (`.cfg-toggle-btn.expanded .cfg-toggle-ico{ transform:rotate(180deg) }` nunca rotaba, pese a que el CSSOM tenía la regla correcta y el elemento sí matcheaba el selector). Corregido evitando el selector compuesto: la clase de estado se pone directo en el elemento que cambia, no en un ancestro (`.cfg-toggle-ico.expanded` simple, controlado por JS con `icoEl.classList.toggle('expanded', ...)` en vez de `botonEl.classList.toggle(...)`). Si un componente nuevo necesita reaccionar al estado de un ancestro, preferir poner la clase directo en el elemento afectado.
- **CSS — un `<button>` no hereda color/fuente del sitio a menos que se le diga explícitamente:** en el admin, `.radm-tool-card` (las tarjetas de herramienta) no traía `color`/`font` propios, así que el navegador usaba su gris de sistema para el texto en vez del blanco del sitio — se veía casi ilegible. Cualquier componente nuevo del admin hecho con `<button>` necesita `color`/`font` explícitos (o `font:inherit;color:inherit;`) si no va a fijar su propio color — no asumir que hereda solo porque un `<div>` sí lo haría.
- **Bug de plataforma — no volver a nombrar una Function `admin<Algo>`:** las Functions `adminListCursos` y `adminGetRecursos` regresaban 404 real (no un error nuestro) SOLO cuando se llamaban ya autenticado como admin — sin sesión, la regla de seguridad redirigía normal (302) a login, lo cual despistó bastante porque parecía que la Function sí existía. Con sesión, en cambio, Azure nunca lograba encontrar la Function de verdad. La causa más probable: el nombre de la ruta empezaba con la palabra "admin", la misma que usa la regla `/admin/*` de `staticwebapp.config.json` para proteger el panel — aunque técnicamente viven en `/api/`, no en `/admin/`, algo en el motor de rutas de Azure las confundía. Se resolvió renombrando las rutas a `listCursosAdmin` y `getRecursosAdmin` (la palabra "Admin" al final, no al inicio) — nunca más nombrar una ruta de API empezando con "admin".
- **Bug de plataforma — el cuerpo binario crudo no llega como `Buffer` en managed functions:** `uploadRecurso` se diseñó al inicio para recibir el archivo como cuerpo binario crudo (`"dataType": "binary"` en `function.json`, `fetch(url, {body: file})`) para no pagar el costo de convertirlo a base64. En el runtime de las "managed functions" de Static Web Apps, `req.body` llegaba como `string`, no como `Buffer`, y tronaba al subirlo a Blob Storage. Se corrigió mandando el archivo como base64 dentro de un JSON normal (mismo patrón que ya usan todas las demás Functions) — `Buffer.from(fileBase64, "base64")` del lado del servidor. ~33% más pesado el payload, pero confiable; así se subió sin problema el manual real de ~15MB.
- **Bug de plataforma — `timerTrigger` no soportado en Functions "administradas" de Static Web Apps:** al construir el respaldo automático semanal de Table Storage (2026-09-08), una Function con `timerTrigger` en su `function.json` hizo que Oryx rechazara el build completo del deploy ("the file 'respaldoSemanal/function.json' has specified an invalid trigger of type 'timerTrigger'... Currently, only httpTriggers are supported"). El tier Free/managed de Static Web Apps solo soporta `httpTrigger` — nunca usar `timerTrigger` en este proyecto. Para tareas programadas, el patrón correcto es: Function HTTP pública (`authLevel:"anonymous"`, sin entrada en `staticwebapp.config.json`) protegida por un secreto compartido en un header (nunca por rol admin, un disparador automatizado no puede iniciar sesión), disparada por un `schedule: cron` de GitHub Actions — ver `api/respaldoAutomatico` y `.github/workflows/respaldo-semanal.yml`.
- Recursos Fase 1 (frontend de prueba con contenido y códigos hardcodeados en `recursos.html`) — completamente superada por el backend real (lectura + escritura, ver arriba); ya no queda código de esa fase en el repo
- Favicon (ícono "fx" en 16x16/32x32/apple-touch-icon), meta tags Open Graph básicas y `.gitignore` — listos
- Símbolo SVG fantasma de piña (`pineappleMark`, nunca usado) — eliminado de `index.html` y `cursos.html`
- Bug de scroll: los links del menú aterrizaban con el título tapado por las barras sticky (nav + barra "Conóceme") — corregido con `scroll-margin-top` por breakpoint en `style.css`
- **`pdf-lib` — `registerFontkit` es por `PDFDocument`, no global:** al agregar una segunda función que genera PDF con fuentes custom (`generarGuiaCoordenadas`), se les olvidó llamar `pdfDoc.registerFontkit(fontkit)` en ella (sí estaba en `generarDiplomaPdf`) — tronaba "no `fontkit` instance was found". Cada `PDFDocument.create()` nuevo que vaya a usar `embedFont` con una fuente TTF propia necesita su propio `registerFontkit`.
- **JS — nunca reemplazar un elemento al que se le sigue apuntando por `id`:** un `onerror` en una `<img>` de vista previa (plantilla de Diplomas) hacía `this.replaceWith(...)`, perdiendo el `id` — el código que después intentaba actualizar esa imagen (`document.getElementById(...).src = ...`) encontraba `null` y tronaba. Patrón correcto: ocultar con el atributo `hidden` y un elemento hermano de fallback, nunca quitar del DOM el elemento original.
- **JS — mezclar `getUTC*()` con `get*()` (hora local) al construir/leer la misma fecha rompe todo fuera de UTC+0:** `test-availability.js` construía una fecha con `setUTCHours`/`setUTCDate` pero la leía de vuelta con `getFullYear`/`getMonth`/`getDate` (hora local de la máquina) — en cualquier máquina con offset negativo (como la de Alfredo), el calendario se recorría un día y la prueba comparaba contra el día equivocado, fallando 6 verificaciones sin que el código real (`getAvailability`/`availability-logic.js`) tuviera ningún problema. Solo se descubrió al correr la prueba de verdad por primera vez (2026-09-08, ya con Node instalado) — nunca se hubiera visto solo leyendo el código. Regla: una vez que se elige trabajar en UTC para una fecha, usar los getters/setters UTC de punta a punta, nunca mezclar con los locales.
- **CSS — el atributo `hidden` pierde contra una clase de autor que fija su propio `display` (misma especificidad, y el autor siempre gana sobre el user-agent stylesheet):** `.cot-alert{ display:flex; ... }` (banner de alerta de Cotizaciones) se quedaba visible y vacío aunque tuviera `hidden` puesto y el JS nunca se lo quitara — el navegador aplicaba `display:flex` de mi propia regla por encima del `display:none` que el navegador le da a `[hidden]` por default, porque ambas reglas tienen la misma especificidad (una clase) y la de autor gana siempre, sin importar el orden en la hoja. Encontrado probando en el navegador (visible en una pestaña donde nunca debería aparecer). Corregido con un override explícito `.cot-alert[hidden]{ display:none; }`. Regla general: cualquier elemento que se oculte con el atributo `hidden` y cuya clase base fije su propio `display` (no `none`) necesita ese mismo override `[hidden]{ display:none; }` a propósito, o el atributo queda sin efecto sin ningún error en consola. **Reapareció 2026-09-11** con `.radm-form-row` (fila de Cliente final en Grupos, oculta tras el toggle nuevo) — mismo fix, `.radm-form-row[hidden]{ display:none; }`. Dado que ya son 2 casos, cualquier clase nueva de este admin que use `display:flex`/`grid`/etc. y alguna vez vaya a ocultarse con `hidden` debería llevar su override desde que se escribe, no esperar a que se rompa.
- **Un panel que carga sus datos solo UNA VEZ (al abrir `/admin`) parece "no actualizarse" si algo cambia en otro panel y luego se navega ahí por el sidebar:** Tracking Operación llamaba `cargarKpisGrupos()`/`buscarGruposTracking()` solo al final del IIFE (arranque de la página) — el clic genérico del sidebar (`adminNav`) solo hace `activarPanel(key)`, que cambia qué `.admin-panel` se ve pero no vuelve a pedir datos. Reportado por Alfredo como "se siente medio bugeado, no se actualiza eficazmente" (2026-09-12). Corregido con un listener extra en el botón del sidebar de ese panel (`.admin-nav-item[data-panel="tracking-operacion"]`) que refresca KPIs+tabla en cada clic, ADEMÁS del genérico de `adminNav` (ambos listeners conviven sin problema). Regla para el proyecto: cualquier panel nuevo cuyo contenido pueda quedar desactualizado por una acción hecha en OTRO panel (no solo por sus propios filtros/acciones internas) necesita este mismo refresco-al-navegar, no asumir que cargar una vez al inicio basta.
- **Cualquier buscador/autocompletado que le pegue a la base SQL serverless necesita `apiGetConReintento`, no `apiGet` a secas — si no, una base dormida lo hace fallar en silencio, sin ningún error visible:** el autocompletado de Cliente en Crear Cotización usaba `apiGet` directo dentro de su `catch(e){ /* silencioso */ }` (copiado tal cual del patrón de Diplomas/Solicitudes, que tienen el mismo hueco pero nunca se topó en la práctica) — si la base estaba dormida, la búsqueda se caía y el dropdown de sugerencias simplemente nunca aparecía. Reportado por Alfredo en producción justo después de correr `sql/008`. Corregido cambiando a `apiGetConReintento` + un `toast` visible si de plano falla tras los reintentos. Esta instancia puntual del buscador de cliente quedó **eliminada** horas después en el mismo día, al rediseñarse Crear Cotización para nacer siempre de una Solicitud — pero la regla general se queda: cualquier código nuevo que llame a `buscarClientes` (o cualquier otra consulta a `apcweb-backoffice`) desde el admin debe usar `apiGetConReintento`, nunca `apiGet` a secas.
- **pdfmake — una tabla con `absolutePosition` cerca del borde inferior de la página dispara una página en blanco extra, en vez de solo dibujarse ahí:** al construir la pastilla `fx =COTIZAR(...)` del pie de la portada de Cotización (`api/src/cotizacion-pdf.js`), colocarla en `absolutePosition:{x, y: PAGE_H - 66}` (bastante cerca del borde) hacía que pdfmake generara una página 2 en blanco antes de la de Temario — confirmado aislando el caso: pdfmake sí mide la altura natural de una tabla (no de un `canvas`, que no tiene este problema) contra el espacio "restante" de la página incluso cuando el elemento va `absolutePosition`, y si no cabe la manda entera a la página siguiente en vez de solo posicionarla ahí. No depende de cuánto contenido de flujo haya antes. Corregido dándole más margen al borde inferior (`PAGE_H - 100` en vez de `PAGE_H - 66`). Regla para este proyecto: cualquier tabla/pill con `absolutePosition` cerca de un borde de página necesita margen de sobra (≥80-100pt) del lado en que puede desbordar, no solo el mínimo visual — verificarlo contando páginas del PDF resultante (`PDFDocument.load(buf).getPageCount()` con `pdf-lib`), no solo viéndolo.
- **pdfmake — un resumen corto al final de una lista larga (`temas`) puede quedar huérfano solo en una página casi en blanco, si la lista llena la página anterior casi exacto:** el bloque "Horas totales" al final del Temario de Cotización, al no estar agrupado con nada, podía terminar siendo el ÚNICO contenido de una página nueva cuando los temas de arriba llenaban toda la página física anterior — no es un bug de "corta contenido a la mitad" (eso ya estaba resuelto con `unbreakable` por tema), es un desperdicio de una página completa por una sola línea. Corregido agrupando (mismo `unbreakable`) el resumen de horas totales junto con el ÚLTIMO tema de la lista, para que si no caben juntos en la página actual, se muevan juntos a la siguiente — nunca solos. Regla: cuando un resumen/total cierra una lista de longitud variable, agrúpalo con el último elemento de esa lista, no lo dejes como bloque independiente.
- **Un `toast()`/aviso compartido entre 2 secciones deja de verse si esas 2 secciones se separan en `admin-panel`s distintos:** al mover el tablero de Seguimiento de Cotizaciones a su propio panel "Tracking Comercial" (reacomodo de navegación, 2026-09-10), el `<div id="cotToast">` se quedó físicamente dentro de `#panel-cotizaciones` — y como `.admin-panel:not(.active){ display:none }`, cualquier `toast()` disparado por una acción de fila en Tracking Comercial (ej. "Enviar") quedaba oculto de verdad (su panel padre invisible), aunque el código de `toast()` le quitara el atributo `hidden` sin ningún error en consola. Corregido dándole a Tracking Comercial su propio `#cotToastTracking`, y haciendo que `toast()` elija cuál usar según qué panel esté `.active` en ese momento. Regla para este proyecto: cuando un componente compartido (toast, modal, etc.) se reparte entre 2 secciones que antes eran una sola y ahora vive en 2 `admin-panel`s separados, revisar que cualquier elemento de UI que ese componente controle (no solo los datos) también se haya movido o duplicado — un `getElementById` sigue "funcionando" sin error aunque el elemento esté enterrado en un panel invisible.
- **Mover HTML de una pestaña a un panel de sidebar propio puede dejar vivo un `querySelector`/`.click()` que apuntaba al botón de esa pestaña vieja:** el mismo reacomodo rompió 2 flujos que antes navegaban entre pestañas del MISMO panel Cotizaciones con `document.querySelector('#cotTabs .dash-etab[data-cottab="..."]').click()` — "Nueva versión" (llamado desde Tracking Comercial) intentaba activar la pestaña "Crear Cotización" sin primero cambiar de `admin-panel` visible (el usuario se quedaba viendo Tracking Comercial mientras el contenido cambiaba invisible debajo), y el handler de "Crear" intentaba hacer clic en el botón "Seguimiento" que ya no existe (`.click()` sobre `null`, `TypeError` real tras cada creación exitosa). Corregido llamando `activarPanel('cotizaciones')`/`activarPanel('tracking-comercial')` explícitamente en cada caso, más `buscarCotizaciones()` para refrescar la tabla al llegar. Regla: al mover contenido de pestaña a panel de sidebar, buscar TODOS los `querySelector(...).click()` que apunten a los botones de esa pestaña vieja — cambiar de pestaña ya no equivale a cambiar de panel visible.
- **Un dominio ya "reclamado" dentro de un tenant de Microsoft 365 no admite crear una cuenta Microsoft personal libre con ese mismo dominio — nunca de portal, tema de acceso, no de código:** al invitar a Viridiana al rol `admin` (2026-09-11), el primer intento fue tratar `viridiana.garza@lifezen.com.mx` como si pudiera convertirse en una cuenta Microsoft personal nueva (mismo supuesto que ya había funcionado para `alfredo.pina@lifezen.com.mx` en su momento) — dio "contraseña incorrecta" en el link de invitación. Causa real: `lifezen.com.mx` ya está registrado dentro del tenant de Microsoft 365 de LifeZen (porque Alfredo tiene licencia ahí), así que Microsoft nunca ofrece "crear cuenta" para ese dominio — cualquier correo `@lifezen.com.mx` solo puede ser un usuario real DENTRO de ese tenant. Resultó que Viridiana ya estaba dada de alta ahí (licencia gratuita "Microsoft Fabric (Free)", quedó de algún alta anterior sin relación) pero nunca había iniciado sesión, así que no tenía contraseña activa. Se resolvió sin tocar código: Microsoft 365 admin center → Users → Active users → su usuario → **Reset password**, ella completó su primer login con la contraseña temporal, y de ahí sí pudo aceptar la invitación al rol `admin`. Regla para el futuro: si alguien con correo de un dominio ya usado en un tenant M365 no puede entrar a `/admin`, primero revisar si ya existe como usuario en ese tenant (Active users) antes de intentar crearle una cuenta Microsoft nueva — casi seguro ya existe y solo le falta el primer login/contraseña.
- **Esta máquina (la de Alfredo) no tiene ImageMagick ni Python — `convert` resuelve al `convert.exe` de Windows (la utilidad de conversión FAT→NTFS), no a ImageMagick:** descubierto al necesitar recortar una foto nueva (2026-09-11). `python`/`python3` tampoco funcionan de verdad (son alias de Windows que solo abren la Microsoft Store). Lo que sí funciona: Node.js (ya instalado) + la librería `sharp`, instalada en una carpeta temporal fuera del proyecto (`npm init`+`npm install sharp` en un directorio de trabajo aparte) para no ensuciar `api/package.json` con una dependencia que no usa el sitio en producción. Regla para el futuro: cualquier tarea de recorte/procesamiento de imagen en esta máquina, usar Node+`sharp` en un scratch aparte — nunca asumir que `convert`/`python` funcionan aquí.

## Historial de sesiones

Movido a HISTORIAL.md (2026-09-12) para no cargarlo completo en cada turno de Claude Code — CLAUDE.md se reinyecta entero en cada mensaje, este archivo aparte no. Consultarlo solo cuando se pida explícitamente ver el historial o haga falta el detalle de una decisión vieja que no quedó ya resumida en "Estado del proyecto"/"Ya resueltos" de este documento.

## Reglas de trabajo con Alfredo

- Está aprendiendo Azure/GitHub/Claude Code activamente — explica brevemente el "por qué" de decisiones técnicas no triviales, no solo el "qué".
- Prefiere que se le proponga con 2-3 opciones y trade-offs antes de que se ejecute algo con impacto de diseño o arquitectura grande.
- No asumas requerimientos nuevos — si algo no está en este documento ni fue pedido explícitamente en la sesión, pregunta antes de construir.
- El Project de claude.ai (chat separado) tiene documentos `Memoria_...md` con el detalle completo de decisiones, historia y razones — este archivo es el resumen operativo, no el reemplazo de esos documentos.
