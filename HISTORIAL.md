# HISTORIAL.md — alfredopina.ai

Bitácora completa, sesión por sesión, movida aquí desde `CLAUDE.md` el 2026-09-12 para que las conversaciones de Claude Code no tengan que cargar todo este relato en cada turno (`CLAUDE.md` sí se reinyecta completo en cada mensaje; este archivo no). Solo se lee cuando alguien lo pide explícitamente ("revisa el historial", "¿qué pasó en la sesión de tal fecha?") o cuando hace falta el detalle de una decisión vieja que no quedó resumida en `CLAUDE.md` → "Estado del proyecto"/"Ya resueltos".

Formato de cada entrada: `Fecha Módulo: Acciones` — un título corto por sesión de trabajo, con el detalle en bullets debajo. Agregar una entrada nueva (más reciente arriba) al cerrar cada sesión.

### 2026-09-12 alfredo.pina: Grupos — Consultar Grupos con búsqueda automática, columna Fase, Editar/Mover/Eliminar separados (ronda de pulido 3)

Tercera y más grande ronda de depuración de Grupos, mismo patrón de las dos anteriores: Alfredo trae una lista larga de pedidos concretos sobre Consultar Grupos, se iteran en texto las 2 dudas reales (semántica de Estatus de cierre, fuente de las sugerencias de Nombre del curso) antes de tocar código, y se construye todo de una vez. Detalle técnico completo en `CLAUDE.md` → "Estado del proyecto" → Grupos, bloque "Ronda de pulido 3", no repetido aquí.

- **Consultar Grupos simplificado**: fuera Desde/Hasta y los botones Buscar/Ver todos — búsqueda automática al cambiar cualquier filtro (debounce de 350ms en el texto). Filtrar y Ordenar quedan cada uno en su línea, dentro de la misma tarjeta con borde, con un ícono al inicio en vez de texto ("ORDENAR"/lupa de "Buscar"). 3 sorts nuevos (Herramienta/Instructor/Fase), resueltos en el cliente porque el backend solo ordena por fecha/cliente.
- **Tabla**: "Grupo / Código" → "Grupo"; Estatus del curso + Estatus de cierre se unifican en una sola columna **Fase**, reusando tal cual el stepper mini que ya usa Tracking Operación (pregunta de Alfredo: "¿qué tan complejo sería?" — resultó barato, cero componente nuevo); columna Horas agregada.
- **Acciones separadas en 3**: Editar (solo el formulario) / Mover (solo el stepper) / Eliminar — pestaña nueva "Modificar Grupo" sin botón propio en la barra de pestañas, solo alcanzable desde una fila. El regreso (Cancelar/Volver/Guardar) recuerda si la acción se disparó desde Consultar Grupos o desde Tracking Operación y vuelve al lugar correcto — Tracking Operación ganó su propia acción "Mover" para que esto tuviera sentido también desde ahí.
- **`eliminarGrupo` — única excepción de todo el proyecto a "nunca borrar de verdad"**, pedida explícita por Alfredo para corregir capturas erróneas o limpiar pruebas del backlog 2026. Borra `GrupoFaseHistorial` primero (la FK no tiene `ON DELETE CASCADE`) y luego el Grupo, en una transacción, con confirmación `window.confirm()` en el front.
- **Bug real encontrado y corregido de paso**: Tracking Operación no se refrescaba al navegar ahí por el sidebar (solo cargaba sus datos una vez, al abrir `/admin`) — Alfredo lo reportó como "se siente medio bugeado". Corregido con un listener extra en el botón del sidebar.
- Probado en el navegador local contando llamadas de red reales (no solo lo visual): debounce del filtro de texto, disparo inmediato de los `<select>`, orden por Herramienta, los 3 flujos de Editar/Mover/Eliminar desde ambos orígenes, y exactamente 1 refresco de KPIs+tabla al hacer clic en Tracking Operación. `node --check` limpio en `eliminarGrupo`.
- **Pendiente para Alfredo:** confirmar en producción, sobre todo Editar/Mover/Eliminar desde los dos orígenes y que Tracking Operación ya se sienta al día.

### 2026-09-12 alfredo.pina: Grupos — segunda ronda de pulido (Sesiones a texto libre, Estatus de cierre bloqueado, datalist de Nombre del curso, toggles agrupados)

Segunda ronda de depuración de Grupos, mismo día que la primera (ver entrada de abajo) — 6 pedidos más, con `AskUserQuestion` para las 2 decisiones reales (semántica de "Estatus de cierre", fuente del datalist de Nombre del curso). Detalle técnico completo en `CLAUDE.md` → "Estado del proyecto" → Grupos, bloque "Ronda de pulido" (segunda), no repetido aquí.

- **Sesiones deja de ser un conteo (`INT`) y pasa a texto libre** (ej. "5 Sesiones Jueves de 2 a 6 pm") — `sql/013_grupo_sesiones_texto.sql` (corrido y confirmado 2026-09-12, `ALTER COLUMN` a `NVARCHAR(150)`, sin pérdida de datos).
- **Toggle "¿Más de 1 Grupo?"** — revela el campo Grupo, antes siempre visible.
- **Estatus de cierre ya no dice "(vacío)"**: se bloquea hasta que Estatus del curso = Terminado y arranca en "Proyecto" al desbloquearse — se descartó un estado nuevo "En Proceso" por chocar de nombre con el valor "En proceso" de Estatus del curso.
- **Nombre del curso** gana un datalist de sugerencias — a pedido explícito de Alfredo, sacado del propio historial de `Grupo.nombre_curso` (Function nueva `listNombresCursoAdmin`), no del catálogo de Cursos/TemariosEstandar.
- **Los 4 toggles (Cliente final/Más de 1 Grupo/Tiene Cotización/Curso histórico) se agrupan al inicio del formulario** — "Curso histórico" revela Fecha de cierre, que para un Grupo nuevo no debe verse en absoluto (se llena sola al avanzar la fase a Cerrado vía el stepper).
- Bug de CSS ya conocido (`[hidden]` perdiendo contra `display:flex`) reaparece por 3ª vez, ahora en `.radm-field` — mismo fix de siempre.
- **Pendiente para Alfredo:** confirmar en producción, sobre todo el datalist de Nombre del curso una vez que ya haya un par de Grupos reales guardados.

### 2026-09-11 alfredo.pina: Grupos — ronda de pulido tras la primera prueba real (toggle Cliente final, contacto con cliente nuevo, dropdown de Herramientas/Nivel, quita Correos ML/Pagado)

Alfredo empieza a cargar datos y probar en vivo el proyecto casi terminado — el rol de la sesión cambia de construir módulos nuevos a depurar/pulir los ya construidos, arrancando por Grupos. Llegó con 6 pedidos concretos y pidió explícitamente "iteremos dudas antes de arreglar" — antes de tocar código se investigó cada punto (leyendo `sql/010_grupos.sql`, `crearGrupo`/`editarGrupo`, `listCotizacionesAdmin`) y se usó `AskUserQuestion` para las 4 decisiones que sí eran del usuario (crear Cliente+Contacto juntos vs. dejarlo como estaba; diagnóstico del bug de Cotización de origen; tirar las columnas `correos_ml`/`pagado` de la base o solo dejar de usarlas; diseño del dropdown colapsado vs. checkboxes siempre visibles). Detalle técnico completo ya integrado en "Estado del proyecto" → Grupos, bloque "Ronda de pulido", no repetido aquí.

- **Toggle "¿Hay Cliente final?"** — antes esos campos siempre estaban visibles, ahora un switch los oculta por default.
- **Bug real corregido** — "+ Nuevo contacto" solo se activaba con un cliente EXISTENTE (clic real en la sugerencia del autocompletado); capturar nombre+código de un cliente nuevo nunca refrescaba ese estado. Resuelto de fondo, no con un parche: `contacto_nuevo` nuevo en `crearGrupo`/`editarGrupo` — cuando el cliente todavía no existe, el contacto se manda como datos sueltos y el backend crea Cliente+Contacto+Grupo en una sola transacción.
- **Bug de CSS encontrado en el camino, 2ª vez que aparece en el proyecto**: `[hidden]` perdía contra `.radm-form-row{ display:flex }` (mismo patrón ya documentado con `.cot-alert` en Cotizaciones) — corregido con `.radm-form-row[hidden]{ display:none; }`.
- **"Grupo / Código" → "Grupo"** — confirmado contra el esquema real que no es llave de nada, solo referencia libre de Alfredo.
- **Herramientas/Nivel**: pills reemplazadas por un dropdown colapsable con checkboxes y resumen ("Excel, Power BI") cuando está cerrado.
- **"Correos ML" y "Pagado" eliminados de verdad** — Alfredo prefirió tirar las columnas de la base en vez de dejarlas huérfanas; `sql/012_grupo_quita_columnas.sql` (tira primero los `DEFAULT CONSTRAINT` autogenerados, luego las columnas) **corrido y confirmado el mismo día**.
- El bug reportado de "Cotización de origen sin datos" resultó no ser un bug — mismo mecanismo de fondo que el punto del contacto (Alfredo confirmó que no había dado clic en la sugerencia), sin cambios de código ahí.
- Probado en el navegador local (`.claude/launch.json` + `window.fetch` mockeado, capturando el payload real enviado a `crearGrupo`): alta con cliente nuevo sin y con contacto nuevo, edición con Cliente final ya existente y multiselects precargados. `node --check` limpio en las 3 Functions tocadas.
- **Pendiente para Alfredo:** confirmar en producción, sobre todo el flujo de cliente nuevo + contacto nuevo.

### 2026-09-11 alfredo.pina: Sitio — reemplaza la foto de Alfredo (ojos cerrados) por una nueva, separada en 2 usos

Cambio pequeño hecho directo por Claude en esta sesión (sin pasar por la constructora) — Alfredo trajo una foto nueva de estudio (traje, brazos cruzados, ojos abiertos) para reemplazar `alfredo-work-photo.png`, cuyo mayor problema real era salir con los ojos cerrados.

- **Hallazgo antes de tocar nada**: ese único archivo alimentaba 2 usos muy distintos — fondo completo del Hero de `index.html` (con un degradado que funde la foto con el tema oscuro solo del lado izquierdo) y la foto de bio con marco en el PDF de Cotizaciones. Una foto de estudio con fondo blanco liso funciona bien en el marco del PDF pero se ve como un bloque blanco flotando en el Hero oscuro si se pega tal cual — diagnosticado como la causa real de un intento fallido que Alfredo ya había hecho antes por su cuenta.
- **Solución — 2 archivos con su propio tratamiento, ya no uno solo:** `assets/img/alfredo-hero.jpg` (la foto completa, con un filtro CSS nuevo en `.hero-photo-bg img` — `grayscale`+`brightness` bajo+`contrast` — que hunde el fondo blanco en el tema oscuro en vez de dejarlo brillante) y `assets/img/alfredo-headshot.png` (recorte cuadrado centrado en él, sin tratamiento de color, para el marco del PDF — ahí el fondo claro sí funciona). Copiada también a `api/assets/img/` para que la Function la encuentre al desplegar, mismo patrón que `firma-ap.png`.
- El recorte se hizo con Node+`sharp` (ver "Ya resueltos" — esta máquina no tiene ImageMagick ni Python de verdad).
- **Probado de dos formas reales, no solo leyendo código:** el Hero en el navegador local (desktop y mobile, ajustando el filtro 2 veces hasta que el fondo dejó de verse como un halo blanco) y un PDF de Cotización generado con datos de prueba, enviado directo a Alfredo para que lo revisara él mismo (no hay forma de tomar captura de un PDF local en el navegador de esta sesión). Alfredo confirmó que ambos se ven bien.
- Comit aparte, sin tocar el trabajo en curso de Grupos que la sesión constructora traía sin commitear al mismo tiempo (`admin/index.html`, `api/crearGrupo`, `api/editarGrupo`, `api/listGruposAdmin`, `sql/012_grupo_quita_columnas.sql`) — se dejaron intactos.
- **Pendiente para Alfredo:** ninguno — cambio confirmado y cerrado.

### 2026-09-11 alfredo.pina: Modelo — visor del esquema completo embebido en /admin (nace del Artifact ya publicado)

Sesión corta, ejecutada en la misma conversación donde se armó y actualizó el diagrama como Artifact aparte (ver esa sesión más abajo para el detalle de cómo se construyó el visor SVG interactivo la primera vez) — aquí solo se portó ese mismo contenido a un panel real de `/admin` y se le agregó el pedido nuevo de Alfredo: filas + última fecha por tabla con un botón de actualizar. Detalle técnico completo ya integrado en "Estado del proyecto" → Modelo arriba, no repetido aquí.

- Antes de construir, se le confirmó a Alfredo por qué esto no tiene costo extra (archivo estático, mismo tipo de recurso que cualquier página del sitio) y por qué queda 100% independiente de Claude (HTML/CSS/JS puro corriendo en su propia Azure, sin llamadas de vuelta a claude.ai) — la única dependencia externa es Google Fonts, que el admin ya usa de todas formas.
- Función nueva `getModeloEstadisticas` (SQL vía un solo `UNION ALL`, Table Storage vía `listEntities()` porque no hay `COUNT` nativo ahí) + panel `Modelo` completo en el sidebar (ítem suelto, debajo de Pendientes) — mismo diagrama arrastrable/zoom/buscador/leyenda que el Artifact, con IDs/clases `mdl-*` para no chocar con el resto de un admin ya grande, reusando los tokens de color del sitio en vez de definir los suyos.
- **Decisión propia, no pedida explícitamente:** las estadísticas se cargan solo al pulsar "Actualizar", nunca automáticamente al abrir el panel — mismo criterio que ya usa el proyecto en Tarifas/Respaldos de Configuración, para no despertar la base SQL serverless ni recorrer 8 tablas de Table Storage solo por curiosidad de entrar a ver el diagrama.
- Sin bugs de plataforma nuevos — la Function no empieza con "admin", y los nombres de tabla que se interpolan en el SQL son constantes fijas del archivo (nunca vienen del request), así que no hay superficie de inyección real pese a no usar parámetros ahí.
- Probado con `node --check` en la Function nueva y en el `<script>` completo del admin extraído (mismo patrón de verificación que ya usa el proyecto para cambios grandes), más un grep de integridad referencial confirmando que las 23 tablas y las 30 relaciones portadas coinciden exactamente con las del Artifact, sin transcripción rota. `npm test` (las 3 suites existentes, ninguna relacionada) sigue en verde.
- **Pendiente para Alfredo:** confirmar en producción — sin ningún script SQL que correr.

### 2026-09-11 alfredo.pina: Pendientes — pulido tras primera prueba real (checkbox, drag & drop, ordenar por fecha, separador de sidebar)

Primera sesión de "depurar/limpiar/embellecer" del proyecto — Alfredo empieza a cargar datos reales y probar en vivo, así que el rol cambia de construir módulos nuevos a pulir los ya construidos. Arrancó con 3 pedidos concretos sobre Pendientes (recién cerrado el día anterior) más uno de sidebar; detalle técnico completo ya integrado en "Estado del proyecto" → Pendientes arriba, no repetido aquí.

- **Checkbox casi invisible en ambos temas** — el borde usaba `var(--line)`, prácticamente el mismo tono que el fondo de la tarjeta en dark y en light. Corregido a un borde más grueso y de mayor contraste (`var(--text-dim)`) con hover al azul de marca.
- **Drag & drop para priorizar manualmente** — se evaluó como "barato" antes de construir porque el proyecto ya tiene el patrón resuelto tres veces (Cursos, Temas/Proyectos, Recursos: SortableJS + `handle` + reindexar a `0,10,20…` en `onEnd`) — se reusó tal cual sobre la tabla `Pendientes` (campo `orden` nuevo) con una Function nueva, `updateOrdenPendientes`. Las notas nuevas siguen naciendo arriba de la columna por default (mismo comportamiento de siempre).
- **Botón de ordenar por fecha** (más antiguo↔más reciente) en el encabezado de cada columna — reusa el mismo endpoint que el drag en vez de ser un modo de vista aparte.
- **Separador de sidebar** — Pendientes vivía pegado a Recursos sin ningún corte, se leía como parte de Productos aunque conceptualmente es un ítem suelto (ver el diseño original). Se agregó una línea + espacio arriba, mismo lenguaje que ya usa el pie del sidebar.
- Probado en el navegador local con `window.fetch` mockeado: contraste en ambos temas, el grip de arrastre apareciendo solo al hover, `Sortable.get()` confirmando el reenganche en cada render, y el botón de ordenar alternando correctamente con su toast. El arrastre físico en sí no se pudo simular de forma confiable con el mouse automatizado (limitación conocida de SortableJS con clics sintéticos) — validado por inspección de que usa exactamente el mismo patrón ya probado en producción, no por una corrida real de drag.
- Alfredo cerró el bloque satisfecho ("me gusta como quedó, me es muy útil") — no se esperan más cambios aquí salvo que algo falle en producción.
- **Pendiente para Alfredo:** confirmar en producción, sobre todo el drag real con mouse/touch.

### 2026-09-11 alfredo.pina: Sesión de director de proyecto — Grupos/Tracking Operación/Pendientes teorizados y confirmados, acceso de Viridiana, URLs limpias, validación de los 11 SQL

Sesión larga de rol "director de proyecto" (sin construir código directo, salvo la limpieza de URLs) corriendo en paralelo a 3 sesiones constructoras distintas el mismo día — Grupos, Tracking Operación y Pendientes se diseñaron aquí y se construyeron en esas sesiones casi en tiempo real, lo que causó varios choques de edición concurrente en `CLAUDE.md` (documentado abajo, no se perdió información real, solo hubo que reconciliar un par de veces).

- **URLs limpias** — `staticwebapp.config.json` con `rewrite` para `/cursos`/`/agenda`/`/recursos`/`/encuesta`/`/diagnostico` (los `.html` viejos se quedan funcionando, no se rompió nada); actualizados todos los `href` internos del sitio y los 2 generadores de link/QR del admin (Encuestas/Diagnóstico) para usar las rutas limpias. Único cambio de código hecho directo en esta sesión, sin pasar por la constructora — chico, mecánico, bajo riesgo.
- **Acceso de Viridiana resuelto** — gotcha real documentado: un dominio ya reclamado por un tenant M365 (`lifezen.com.mx`, por la licencia de Alfredo ahí) no permite crear una cuenta Microsoft personal libre con ese dominio; el fix fue resetear la contraseña de un usuario que ya existía en el tenant, no crear nada nuevo. Confirmado funcionando — la sección Actividad (construida días antes) ya muestra a los 2 administradores con su propio conteo real.
- **Grupos — diseño completo**: resuelto el conflicto de nombres (se llama "Grupos", no "Cursos" — el Catálogo existente se queda igual), diccionario de campos cerrado con Alfredo (incluye el patrón Cliente/Cliente final para el caso de reventa tipo Capacitanet→Clarios, confirmado con él cómo modelarlo), Nivel como multi-select en vez de abreviaturas ambiguas. Construido y confirmado el mismo día por la sesión constructora.
- **Tracking Operación + stepper de fase** — diseñado como espejo de Tracking Comercial (scorecard, alerta, tabs), más un componente nuevo iterado en 2 rondas de mockup: primero 6 mecánicas de interacción para el módulo de Diagnóstico (`https://claude.ai/code/artifact/48688dc0-...`), después el stepper de fase en cuadrados con 3 variantes de dónde vive — mini/expandido-con-fechas/interactivo (`https://claude.ai/code/artifact/fc84f38f-...`). Alfredo pidió específicamente que las fechas de cada fase se guardaran solas al cambiar de estatus — se diseñó `GrupoFaseHistorial` para eso. Construido y confirmado el mismo día.
- **Panel Pendientes** — módulo nuevo fuera del roadmap estratégico (herramienta personal de captura rápida, no un pilar de negocio): 5 contenedores fijos, mockup con 3 estados a la vista (nota normal, editando, agregando) aprobado tal cual (`https://claude.ai/code/artifact/39cc5d78-...`). Decisión técnica propia (no pedida por Alfredo, propuesta y aceptada): Table Storage en vez de la base SQL, porque el punto de la herramienta es capturar al instante sin esperar un auto-pause. Construido y confirmado el mismo día.
- **Validación de los 11 scripts SQL del proyecto** — Alfredo pidió confirmar que ya los había corrido todos; se armó una sola consulta (`UNION ALL` de checks por tabla/columna/constraint) contra `apcweb-backoffice`. Encontró un caso real: `sql/004_encuesta_constraint.sql` estaba marcado como corrido en este documento desde 2026-09-08 sin estarlo de verdad — corregido, corrido y confirmado. Lección para el proyecto: el checklist de este documento puede quedar mal marcado por error humano: cuando haya duda real de si algo se corrió, mejor confirmarlo contra la base que confiar ciegamente en una casilla marcada.
- **Choques de edición concurrente en `CLAUDE.md`** (para tener en cuenta en sesiones futuras que corran en paralelo con una constructora): varias veces esta sesión escribió al documento mientras la sesión constructora también lo hacía, y la última escritura ganaba — no se perdió información importante porque la sesión constructora documentaba bien lo que construía, pero si algún día sí importa, lo más seguro es evitar que dos sesiones escriban al mismo archivo en la misma ventana de tiempo.
- **Pendiente para Alfredo:** confirmar en producción Grupos, Tracking Operación y Pendientes con uso real; el resto del checklist queda igual que antes de esta sesión (ver "Pendientes de Alfredo" al inicio del documento, ya actualizado).

### 2026-09-11 alfredo.pina: Pendientes — captura rápida de notas (5 contenedores fijos)

Tercera sesión constructora del día, prompt aparte de Grupos/Tracking Operación — módulo chico y autocontenido, sin relación de datos con el resto del backoffice. Mockup ya aprobado de antemano, ejecutado tal cual sin pausas a preguntar. Detalle técnico completo ya integrado en "Estado del proyecto" → Pendientes arriba, no repetido aquí.

- Nueva tabla `Pendientes` en Table Storage (`apcwebrecursos`, misma cuenta que Cursos/Recursos/Temas, auto-creada por código) + 4 Functions (`listPendientes`/`crearPendiente`/`editarPendiente`/`archivarPendiente`) + ítem nuevo **Pendientes** en el sidebar (suelto, al nivel de Dashboard/Tracking) con el panel completo de 5 contenedores.
- **Decisión de arquitectura ya cerrada por el prompt, no una elección de esta sesión:** Table Storage en vez de la base SQL del backoffice, a propósito — esta herramienta necesita capturar al instante, y la base SQL serverless se auto-pausa (20-60s en frío), fricción que no tiene sentido para una libreta de notas rápidas.
- **Mejora de resiliencia agregada por iniciativa propia, no pedida en el prompt:** al probar el camino de error de `listPendientes` con `window.fetch` mockeado, se encontró que un fallo de carga dejaba el panel completo bloqueado con un mensaje de error y sin ninguna salida (ni un botón de reintentar, nada). Se corrigió para que un error de carga caiga a una rejilla vacía y usable (se puede seguir agregando notas) más un toast — nunca deja al usuario varado.
- Sin bugs de plataforma nuevos — los ya conocidos se evitaron desde el diseño (ninguna ruta empieza con "admin", etc.).
- Probado de punta a punta con `.claude/launch.json` + `window.fetch` mockeado en consola: alta de una nota en cada una de las 5 categorías, edición con guardado y con cancelación (confirmando que cancelar no llama a la API), archivar (sale de la lista activa, sube el contador de archivados) y desarchivar desde "ver archivados" (regresa a la lista activa, `fecha_archivado` se limpia), y el estado vacío de una categoría sin notas. `node --check` limpio en las 4 Functions y en `src/pendientes-tables.js`, sintaxis del `<script>` del admin verificada con Node.
- **Pendiente para Alfredo:** confirmar el flujo completo en producción — sin ningún script SQL que correr, la tabla se crea sola con la primera nota que se guarde.

### 2026-09-11 alfredo.pina: Tracking Operación + stepper de fase con historial (nace de Grupos)

Segunda sesión constructora del día para Fase 2 Operación, ejecuta el prompt de handoff que llegó justo después de confirmar que Grupos (sesión anterior, ver entrada de abajo) ya estaba listo en el repo — requisito previo explícito del prompt. Detalle técnico completo ya integrado en "Estado del proyecto" → Tracking Operación arriba, no repetido aquí.

- `sql/011_grupo_fase_historial.sql` (corrido el mismo día), `api/src/grupo-fase.js` (única fuente de verdad de la regla de fase — `derivarFase`/`estatusDeFase`/`registrarCambioFase`/`sembrarHistorialInicial`), `crearGrupo`/`editarGrupo` extendidos para loguear el historial dentro de su propia transacción, 3 Functions nuevas (`avanzarFaseGrupo`, `getHistorialFaseGrupo`, `getResumenGruposAdmin`), `listGruposAdmin` con `fase`/`dias_en_fase` calculados + `?vista=`, componente compartido `window.FaseStepper`, y el panel **Tracking Operación** completo (reemplaza el placeholder "PRONTO" del sidebar) + el stepper interactivo en Editar Grupo + el stepper expandido en la fila expandible de Consultar Grupos.
- **La regla de siembra al crear un Grupo necesitó un matiz que el prompt no dejaba 100% explícito y hubo que resolver con criterio propio:** para un Grupo que nace "Por iniciar" (el caso normal), usar `fecha_inicio` como fecha de esa primera parada del historial hubiera sido literalmente inventar una fecha futura — se corrigió a usar la fecha real de alta ("ahora") en ese caso, y reservar la inferencia desde `fecha_inicio`/`fecha_fin`/`fecha_cierre` solo para cuando el Grupo nace YA avanzado (carga retroactiva). Documentado en el código y en CLAUDE.md para que quede claro por qué no es "siempre usa fecha_inicio".
- Tracking Operación **vive en el mismo IIFE que Grupos** (no uno separado) — mismo patrón que ya usa Tracking Comercial/Cotizaciones, necesario para poder llamar `abrirEdicionGrupo(g)` directo desde la acción "Editar" de su tabla sin duplicar esa lógica. `toast()` se volvió panel-aware por la misma razón ya documentada para Cotizaciones (un toast en el panel no activo queda oculto).
- Reusa deliberadamente varios componentes visuales ya existentes en vez de crear nuevos: `.cot-kpis`/`.cot-kpi` para el scorecard, `.cot-alert` para la alerta, `.cot-semaforo` para el semáforo de días, `.radm-tabs` para los 3 tabs — el único CSS genuinamente nuevo es el del stepper (`.step-track`/`.step-item`/`.step-node`/etc.), calcado del mockup.
- Sin bugs de plataforma nuevos — los ya conocidos se evitaron desde el diseño (ninguna ruta nueva empieza con "admin", etc.).
- Probado de punta a punta en el navegador local con `.claude/launch.json` + `window.fetch`/`window.confirm` mockeados en consola — el mock replicó la lógica real de `grupo-fase.js` (no solo respuestas fijas) para poder probar de verdad la siembra retroactiva y el candado de "solo fase inmediata siguiente": alta normal vs. 2 variantes de alta retroactiva (a "Proyecto" y a "Cerrado", confirmando que Calificaciones/Diplomas se omiten sin fecha inventada cuando se saltan), avance por el stepper interactivo con `confirm()` aceptado y cancelado, el candado del backend rechazando un salto de fase, los 3 colores del semáforo y su ausencia fuera del tramo de cierre, scorecard/alerta/tabs con datos variados, y la navegación cruzada Tracking Operación → Editar → Grupos. `node --check` limpio en todo el backend nuevo/tocado y sintaxis del `<script>` del admin verificada con Node.
- **Pendiente para Alfredo:** confirmar el flujo completo en producción, sobre todo el stepper interactivo avanzando un Grupo real.

**Ronda de ajustes de sidebar, mismo día, tras revisar el resultado:** Grupos se movió al inicio del grupo OPERACIÓN (antes de Diagnósticos, ya no al final junto a Diplomas) — sin cambio de comportamiento, solo orden visual. Tracking Comercial y Tracking Operación perdieron su ícono propio (una gráfica de barras y 3 círculos conectados, respectivamente) — en su lugar, el indicador "en vivo" que antes solo vivía al final del renglón como punto circular ahora ocupa el espacio del ícono al inicio, y cambió de círculo a **cuadrado** (`border-radius:2px`, 8×8px) para seguir el mismo lenguaje "cuadrado, no círculo" que ya define el stepper de fase de Grupo. Antes cada Tracking llevaba icono + texto + punto al final; ahora es cuadrado + texto, un solo indicador en vez de dos elementos.

### 2026-09-11 alfredo.pina: Grupos — primer módulo de Fase 2 Operación (Alta de Grupo + Consultar Grupos)

Ejecuta el prompt de handoff que dejó lista la sesión de teorización del mismo día (diccionario de campos ya cerrado, ver "Pendientes de Alfredo" → Grupos) — sesión puramente de construcción, sin pausas a preguntar. Detalle técnico completo ya integrado en "Estado del proyecto" → Grupos arriba, no repetido aquí.

- `sql/010_grupos.sql` (corrido el mismo día), 3 Functions nuevas (`crearGrupo`/`editarGrupo`/`listGruposAdmin`) + sección **Grupos** completa en el admin (grupo Operación del sidebar, 2 pestañas: Consultar Grupos/Crear Grupo). Sin Tracking Operación todavía — eso se diseña después de que Alfredo pruebe este módulo con datos reales, mismo patrón que ya siguió Tracking Comercial con Cotizaciones.
- Reusa a propósito el mismo `resolverCliente` que ya usan Solicitud/Cotización/Diploma/Encuesta (duplicado, no compartido — criterio ya establecido en el proyecto), aplicado dos veces (Cliente y Cliente final) para soportar el caso de reventa/intermediario del roadmap (Capacitanet→Clarios) sin necesitar que ambas empresas ya existan de antemano.
- Único comportamiento nuevo de UI en el proyecto: pills multi-select (Herramientas/Nivel) que se alternan cada una sola, sin limpiar a sus hermanas — hasta ahora todos los `.sol-pill-group` del admin eran single-select (Modalidad/Participantes en Solicitudes). Mismo componente visual, comportamiento de clic distinto solo en este panel.
- Sin bugs de plataforma nuevos — los ya conocidos (ruta que no debe empezar con "admin", `CHECK` en el mismo batch que su `ALTER TABLE ADD`, etc.) se evitaron desde el diseño.
- Probado de punta a punta en el navegador local (`.claude/launch.json` + `window.fetch` mockeado en consola, mismo patrón de siempre): alta con Cliente y Cliente final nuevos por nombre (escenario de reventa), el Contacto recargándose contra el cliente correcto en cada combinación, validaciones de Herramientas/Nivel vacíos, edición precargando todos los campos exactos (incluyendo fechas sin corrimiento de zona horaria), filtros/orden de Consultar Grupos armando la URL correcta, y el link a la Cotización de origen en la fila expandible. `node --check` limpio en las 3 Functions y sintaxis del `<script>` del admin verificada con Node.
- **Pendiente para Alfredo:** confirmar el flujo completo en producción, sobre todo el caso de reventa/intermediario y el link a la Cotización de origen.

### 2026-09-10/11 alfredo.pina: Botón Despertar con reintento + Configuración a acordeón + Actividad (reemplaza Accesos)

Séptima sesión del día (cruzó la medianoche) — arrancó como una duda de Alfredo sobre por qué el botón Despertar a veces necesitaba 2 clics, siguió con un pedido de rediseño visual de Configuración, y terminó con una pregunta suya que cambió el alcance de la sección Accesos por completo. Detalle técnico ya integrado en "Estado del proyecto" arriba, no repetido aquí.

- **Botón Despertar:** diagnosticado que el comportamiento reportado (tachita roja, luego palomita al reintentar a mano) es real, no un bug — el timeout de conexión de 60s a veces gana la carrera contra el arranque en frío de la base, que sigue completándose de fondo en Azure aunque el cliente ya se haya rendido. Se agregó un reintento automático de 1 vez dentro del propio click handler.
- **Configuración → acordeón:** Alfredo pidió explícitamente que las 2 secciones ya construidas (Tarifas, Respaldos) más las 3 placeholder dejaran de sentirse como "texto suelto" y se agruparan en tarjetas con borde, con todo el encabezado como disparador de abrir/cerrar (no un botón aparte) — implementado igual para las 5 secciones.
- **Accesos → Actividad, la decisión más interesante de la sesión:** antes de construir nada, se le preguntó a Alfredo qué tan completa quería la sección Accesos (3 opciones, de solo-referencia a un roster real con backend). Su respuesta — "no le encuentro la funcionalidad en cualquier caso, dime qué no estoy viendo" — obligó a reconsiderar el planteamiento completo en vez de solo elegir una opción: la razón de fondo es que Azure (plan Free) no expone ningún API para ver/administrar roles desde la app, así que cualquier versión de "Accesos" iba a ser decorativa. Se le explicó eso directamente y, en la misma conversación, Alfredo ya había preguntado por separado si se podía ver cuántas veces entra cada administrador — esa sí es funcionalidad real y construible, así que se reencuadró la sección completa alrededor de eso en vez de forzar el plan original.
- Nuevo `api/src/auth.js` (decodifica `x-ms-client-principal` del lado servidor — nunca confía en un correo que mande el cliente), `api/src/actividad-storage.js` (tabla Table Storage `AdminActividad`, self-creando), Functions `registrarAccesoAdmin`/`listActividadAdmin`. El registro se dispara una vez por carga de `/admin`, junto al fetch de `/.auth/me` que ya existía.
- **Tráfico del sitio público** también salió en la conversación (Alfredo preguntó "¿puedo ver quién visita mi sitio?") — se presentaron 3 caminos con sus trade-offs, Alfredo confirmó que le interesa pero pidió dejarlo pendiente para después, sin construir nada esta sesión.
- Probado con `.claude/launch.json` + `window.fetch` mockeado en consola: las 5 tarjetas de Configuración abriendo/cerrando por clic en el encabezado con el chevron rotando, Tarifas/Respaldos sin regresión tras el rediseño, Actividad renderizando 2 administradores de prueba con avatar/iniciales/correo/conteo/última vez, confirmado por red que `registrarAccesoAdmin` se dispara solo una vez al cargar el panel, y el botón Despertar en sus 3 casos (falla+reintento exitoso, éxito directo, falla+falla).
- `npm test` sigue en verde.
- **Pendiente para Alfredo:** confirmar en producción, sobre todo que Viridiana aparezca en Actividad en cuanto entre por primera vez; retomar la analítica de tráfico del sitio cuando quiera (ver "Pendientes de Alfredo" → Deuda técnica).

### 2026-09-10 alfredo.pina: Diagnóstico — módulo completo de punta a punta (esquema + Functions + admin + frontend, una sola fase)

Sexta sesión del día, ejecuta el prompt de handoff que dejó lista la sesión de teorización de Diagnóstico (mismo día, ver "Pendientes de Alfredo" → Diagnósticos) — metodología y diseño de interacción ya cerrados de antemano, así que esta sesión fue puramente de construcción, sin pausas a preguntar. Detalle técnico completo ya integrado en "Estado del proyecto" → sección Diagnóstico arriba, no repetido aquí.

- `sql/009_diagnostico.sql` (nuevo, pendiente de correr), `api/src/diagnostico-storage.js` (contenedor Blob nuevo `diagnostico`, público a nivel blob — `access:'blob'` explícito, criterio inverso al de `respaldos`/`cotizaciones` que necesitan quedar privados), 9 Functions nuevas, sección **Diagnósticos** completa en el admin (le quita el "PRONTO" al sidebar, 3 pestañas: Preguntas/QR y Link/Resultados) y `diagnostico.html` nuevo (página pública oculta).
- Reusa el mismo esqueleto de `crearAutocomplete`/`apiGetConReintento`/`toast` que ya usan Encuestas/Diplomas/Solicitudes — duplicado a propósito dentro del IIFE nuevo, mismo criterio de encapsular por módulo que ya usa el proyecto en vez de compartir un helper entre secciones.
- **Reordenar preguntas resultó más simple que en Encuestas**: como `nivel` es una columna real (no algo que se arrastre entre secciones), no hizo falta el truco de offset (`BASE_ORDEN` por sección) que sí necesita `reordenarPreguntas` — cada nivel se reordena con valores `0,10,20…` sin arriesgar colisión, porque la consulta pública siempre ordena `(nivel, orden)` primero.
- **Vista previa de imagen antes de subir nada**: a diferencia de Diplomas (que sube la plantilla primero y luego refresca un `<img>` apuntando a una Function de streaming), aquí la imagen se previsualiza de inmediato con `URL.createObjectURL(file)` en el propio navegador de quien administra — no hay ida y vuelta al servidor solo para confirmar que se ve bien, y como el blob es público de todos modos no hace falta la Function de streaming que sí necesita el fondo privado de Diplomas.
- Sin bugs de plataforma nuevos esta vez — los ya conocidos (nombre de ruta no debe empezar con "admin", `access:'none'` inválido en Azure Blob, base64 en vez de binario crudo) se evitaron desde el diseño en vez de descubrirse en producción.
- Probado de punta a punta en el navegador local (`.claude/launch.json` + `window.fetch` mockeado en consola, mismo patrón de siempre): en `diagnostico.html` — 15 preguntas de prueba renderizando agrupadas por nivel con el contador `fx =PREGUNTA(n,15)` correcto, el checkbox de cada pregunta comportándose como single-select real (clic en otra opción quita la anterior, confirmado con `dataset.seleccionada`), validación de "faltan preguntas" contando exactamente las pendientes y haciendo scroll a la primera, envío completo con éxito mostrando la pantalla de gracias sin revelar nada, y el estado de error claro con `?h=` ausente; en el admin — alta de pregunta con las 4 validaciones en el orden correcto (texto → opciones → radio de correcta → imagen, esta última solo obligatoria en creación), edición conservando la imagen si no se sube una nueva (opciones prellenadas correctamente desde `opcion_a..d`), borrado con confirmación real, cambio entre los 3 niveles dentro de una herramienta, los 2 QR (Excel/Power BI) generándose con sus links y colores propios, el candado de polling de 20s del contador compartido (confirmado que deja de llamar a la red al cambiar de pestaña), y Resultados con tallies por banda + fila expandible mostrando correcto/incorrecto con ícono y texto (reusando tal cual `.sol-temario-toggle`/`.sol-desglose-row` de Solicitudes, sin duplicar esa mecánica).
- `npm test` (las 3 suites existentes, ninguna relacionada con Diagnóstico) sigue en verde.
- **Pendiente para Alfredo:** correr `sql/009_diagnostico.sql`; cargar el banco real de 30 preguntas (15 Excel + 15 Power BI) con imágenes de referencia reales desde el admin → Diagnósticos → Preguntas; confirmar el flujo completo en producción, sobre todo el primer envío real desde `diagnostico.html` con datos de un grupo de verdad.

### 2026-09-10 alfredo.pina: Botón "Despertar" + apiGetConReintento extendido a Diplomas/Encuestas

Quinta sesión del día — ejecuta el prompt de deuda técnica que había quedado armado (ver "Deuda técnica" en Pendientes de Alfredo). Alcance: una Function nueva, un ícono más en el topbar, y cerrar el hueco de reintentos que Diplomas/Encuestas nunca tuvieron.

- **`api/despertarBackoffice`** (v3 clásico, protegida rol `admin`, nombre sin empezar en "admin" — bug de plataforma ya conocido): un `SELECT 1` contra `apcweb-backoffice` vía `backoffice-db.js`, nada más — a propósito no reusa ninguna Function de negocio, para que despertar la base cueste lo mínimo posible.
- **Botón "Despertar" en el topbar** (`admin/index.html`, junto a Notificaciones/Configuración): 3 estados, cada uno con un ÍCONO distinto además del cambio de color (rayo en reposo → ícono girando "Despertando…" → check verde "Lista" por ~2.8s → regresa solo a reposo; si falla, X roja "No se pudo despertar la base") — nunca solo color, por la deuteranomalía de Alfredo. Un candado `enCurso` evita doble-click mientras está en progreso. No navega a ningún panel.
- **`apiGetConReintento` duplicado en los IIFEs de Diplomas y Encuestas** (mismo patrón textual que ya usan Clientes/Solicitudes/Cotizaciones/Configuración — a propósito no se comparte entre IIFEs). Diplomas: `buscarClientes` y `listDiplomas` pasaron a usarlo (`generarDiplomas`/`anularDiploma`/`agregarInstructor`/`eliminarInstructor`/`eliminarFirma` se quedaron con `apiPostJson` normal, sin reintento, para no arriesgar una escritura duplicada). Encuestas: `listPreguntasAdmin`, `getContadorRespuestas`, `listRespuestasEncuesta` y `buscarClientes` (`crearPregunta`/`eliminarPregunta`/`reordenarPreguntas`/`enviarRespuesta` igual se quedaron sin tocar). `getInstructores` (Diplomas y Encuestas) se dejó con `apiGet` normal a propósito — lee de Blob Storage (`plantillas-storage.js`), no de la base SQL, así que no sufre el auto-pause.
- **Grep de cierre pedido por el prompt** (confirmar que ningún otro panel se quedó sin este tratamiento) encontró 2 sitios más fuera del alcance original que sí le pegan a `apcweb-backoffice` con `apiGet` normal: el autocompletado de Cliente de Solicitudes (`crearAutocomplete`, ya tenía `apiGetConReintento` definido en su propio IIFE, solo faltaba usarlo en esa llamada) y el de "Ver Cotizaciones" (`crearAutocompleteFiltroCot`, mismo caso) — ambos corregidos de paso, mismo criterio que ya quedó documentado en "Ya resueltos" sobre buscadores contra la base serverless.
- Probado con `.claude/launch.json` + `window.fetch` mockeado en consola: los 3 estados visuales del botón Despertar (incluyendo que el candado bloquea doble-click), y el reintento con 1 fallo simulado antes de éxito en `listDiplomas` (Diplomas) y `getContadorRespuestas` (Encuestas) — confirmado que el toast "La base tardó en responder, reintentando…" aparece y el dato final llega correcto tras el segundo intento.
- **Pendiente para Alfredo:** confirmar en producción — clic en "Despertar" con la base realmente dormida (para ver el ciclo completo de ~20-60s reflejado nada más en el ícono girando, sin bloquear el resto del admin), y que Diplomas/Encuestas ya no truenen en silencio cuando la base tarda en arrancar.

### 2026-09-10 alfredo.pina: Reacomodo de navegación del admin — Prompt 2 (pestañas Consultar/Crear en Clientes, Solicitudes y Cotizaciones)

Cuarta sesión del día — ejecuta el Prompt 2, corrido después del Prompt 1 (ver entrada de abajo) tal como pedía su propio prompt de handoff. Estandariza Clientes/Solicitudes/Cotizaciones al mismo patrón `dash-etabs`/`dip-tabpanel` que ya usa Diplomas. Detalle técnico completo ya integrado en "Estado del proyecto" arriba (secciones Clientes/Solicitudes/Cotizaciones), no repetido aquí.

- **Clientes**: "Consultar Clientes" quedó 100% solo lectura (antes el panel de detalle también servía de formulario de alta/edición y de gestión de Contactos) — un solo botón "Editar" manda a la nueva pestaña "Crear Cliente", que ahora concentra el formulario de Cliente + la sección de Contactos (con el mismo candado de "un solo principal", sin cambios). `pintarContactos` ganó un parámetro `editable` para poder reusarse en los dos contextos (solo lectura vs. con botones de editar/borrar) sin duplicar la función completa.
- **Solicitudes**: `abrirFormSolicitud(sol)` unificó alta y edición en una sola función (antes solo existía alta) — en modo edición el campo Cliente queda de solo lectura (mismo criterio que ya usaba `editarSolicitud` desde el flujo de Cotizaciones, reusado tal cual, sin cambios en el backend) y se agregó el ícono de lápiz por fila para entrar a editar. El desglose expandible que ya existía para los temas ahora también muestra `participantes`/`modalidad`/`ciudad_sede`/`fecha_tentativa` (usando las clases genéricas `.cot-detalle-grid`/`.cot-detalle-item-label/-value` que ya existían para el mismo propósito en Cotizaciones) — se decidió no agregar columnas nuevas a una tabla que ya tenía 8.
- **Cotizaciones**: nueva pestaña "Ver Cotizaciones" (filtros Cliente/Estatus/fechas, carga perezosa — solo pide datos la primera vez que se abre esa pestaña) junto a "Crear Cotización". "Recotizar" no duplica ninguna lógica: llama tal cual a `precargarFormDesdeCotizacion`, la misma función que ya usaba "Nueva versión" desde Tracking Comercial. `listCotizacionesAdmin` ganó el parámetro `?estatus=` (match exacto) para poder filtrar cualquiera de los 7 estatus individuales — a diferencia de `?vista=`, pensado solo para las 4 categorías amplias del pipeline.
- Separador visual compartido `.scorecard-block` (border-bottom + padding) aplicado a Clientes y Solicitudes — un solo lugar para ajustarlo a futuro; Cotizaciones no lo necesita porque su scorecard vive en Tracking Comercial desde el Prompt 1.
- **Sin bugs nuevos que documentar en "Ya resueltos" esta vez** — a diferencia de las 2 sesiones anteriores del día (pdfmake, reacomodo de sidebar), este prompt no encontró ningún comportamiento roto al mover código; probablemente porque el patrón de pestañas ya estaba bien establecido por Diplomas y no había ningún componente compartido (toast, referencias `.click()` a botones movidos) cruzando el límite de panel esta vez.
- Probado navegando los 11 ítems del sidebar sin errores de consola, en ambos temas, más flujos completos simulando datos con `window.fetch` mockeado en consola (mismo patrón de siempre): Solicitudes — abrir "Editar" en una fila y confirmar que Cliente/Contacto/Herramienta/Temario/fecha/ciudad/participantes/modalidad/notas llegan precargados con el título dinámico correcto; Cotizaciones — "Ver Cotizaciones" mostrando una fila con sus 2 acciones, y "Recotizar" saltando a "Crear Cotización" con los datos de esa cotización (mismo resultado que "Nueva versión").
- **Pendiente para Alfredo:** confirmar en producción los 3 flujos completos (crear/editar Cliente con Contactos, crear/editar Solicitud, Ver Cotizaciones con sus filtros y "Recotizar") — ver el checklist detallado en "Pendientes de Alfredo".

### 2026-09-10 alfredo.pina: Reacomodo de navegación del admin — Prompt 1 (Tracking Comercial/Operación, sidebar reagrupado)

Tercera sesión del día — ejecuta el Prompt 1 del reacomodo de navegación dejado listo en la sesión de teorización de más abajo, corrida DESPUÉS de que la sesión de reconstrucción del PDF a pdfmake ya había confirmado su cambio a `admin/index.html` (mismo archivo, evitó chocar en paralelo). Alcance solo de sidebar/estructura — el rediseño interno a pestañas Consultar/Crear (Prompt 2) queda para después.

- Sidebar reordenado: Dashboard → **Tracking Comercial** (ítem nuevo, con `.admin-nav-live-dot` pulsando) → **Tracking Operación** (ítem nuevo, placeholder "PRONTO") → grupo **COMERCIAL** (Clientes, Solicitudes, Cotizaciones) → grupo **OPERACIÓN** (antes "Cierre": Diagnósticos, Encuestas, Diplomas) → grupo **PRODUCTOS** (Cursos, Recursos). `TITLES` y el array de auto-relleno de placeholders (`['diagnosticos', 'tracking-operacion']`) actualizados a juego.
- El contenido completo de la pestaña "Seguimiento" de Cotizaciones (scorecard de 5 KPIs, alerta, tabs Activas/Ganadas/Perdidas/Todas, tabla) se movió tal cual a `panel-tracking-comercial` — corta-pega estructural, sin tocar la lógica JS que ya lo alimentaba (sigue siendo el mismo IIFE de Cotizaciones, todo por id, no le importa en qué `admin-panel` viva el HTML). Cotizaciones se quedó con una sola pestaña, "Crear Cotización", ya activa por default — estado transicional a propósito, el Prompt 2 le agrega "Ver Cotizaciones" después.
- **3 bugs reales encontrados y corregidos al mover el HTML — ninguno visible con solo leer el diff, todos aparecían al usar el flujo real** (detalle completo en "Ya resueltos"): (1) el `toast()` compartido apuntaba a un `#cotToast` que se quedó dentro de `#panel-cotizaciones`, invisible en cuanto Tracking Comercial era el panel activo — se le dio su propio `#cotToastTracking` y `toast()` ahora elige según el panel `.active`; (2) "Nueva versión" (fila de Tracking Comercial) cambiaba de pestaña dentro de Cotizaciones sin cambiar de panel visible primero — el usuario se quedaba viendo Tracking Comercial mientras el formulario se armaba invisible debajo; (3) el handler de "Crear" hacía `.click()` sobre el botón "Seguimiento", que ya no existe (`TypeError` real tras cada cotización creada con éxito). Los 3 se corrigieron con `activarPanel('cotizaciones'|'tracking-comercial')` explícito en cada punto de cruce.
- Probado navegando los 11 ítems del sidebar uno por uno (sin servidor real, sirviendo el HTML directo) confirmando cero errores de consola en cada uno, en tema oscuro y claro — el punto que pulsa se ve correcto en ambos (usa `var(--blue-bright)`, ya theme-aware), y "Tracking Operación" confirmado sin el punto, solo con "PRONTO".
- **Pendiente para Alfredo:** confirmar en producción; el Prompt 2 (pestañas Consultar/Crear en Clientes/Solicitudes/Cotizaciones) sigue pendiente de armar, ver "Pendientes de Alfredo".

### 2026-09-10 alfredo.pina: Cotizaciones — el PDF se reescribe sobre pdfmake (sesión constructora, ejecuta el prompt de la sesión de teorización de abajo)

Segunda sesión del día — ejecuta el prompt de handoff que dejó lista la sesión de teorización anterior (ver entrada de abajo): reemplaza `pdf-lib` + plantilla PNG por `pdfmake` en el PDF de Cotización, replicando el mockup v4 ya aprobado. Detalle técnico completo ya integrado en "Estado del proyecto" → sección Cotizaciones arriba, no repetido aquí.

- `pdfmake@0.3.11` agregado a `api/package.json` (versión exacta, mismo criterio que el resto de dependencias) — API de Node de esta versión (`require('pdfmake')` con `setFonts`/`setLocalAccessPolicy`/`createPdf(...).getBuffer()`) es distinta a ejemplos viejos que usan `pdfmake/src/printer` directo; se confirmó con una prueba mínima antes de escribir el archivo real.
- 4 fuentes nuevas descargadas (Space Grotesk Medium/SemiBold, JetBrains Mono Regular/Medium) a `api/assets/fonts/` — **desde los TTF estáticos de `google-webfonts-helper`, no desde el repo oficial `google/fonts`**, que solo trae la fuente variable (`SpaceGrotesk[wght].ttf`) sin un peso fijo embebible directo. `firma-ap.png`/`alfredo-work-photo.png` copiadas a `api/assets/img/` (las Functions no leen fuera de `api/` al desplegar).
- `api/src/cotizacion-pdf.js` reescrito de punta a punta: 3 páginas construidas con tablas-sin-borde a modo de "cajas"/"pills" con fondo de color (la forma idiomática de pintar un fondo detrás de texto en pdfmake, ya que un `canvas` no compone con texto que fluye encima), `absolutePosition` solo para decoración (franja de acento, marcas de esquina, glow, footer) y contenido normal fluido para todo lo demás — clave para que el Temario pagine solo cuando es largo.
- `crearCotizacion`/`previsualizarCotizacion` ya no descargan ningún `fondoBuffer`; reciben `dirigido_a`/`objetivo` del front y los pasan tal cual al PDF (no se guardan en la base). Eliminadas `subirPlantillaCotizacion`/`getPlantillaCotizacion`/`getGuiaCoordenadasCotizacion` (código + rutas en `staticwebapp.config.json`), y las funciones correspondientes en `plantillas-storage.js` (lo de Diplomas se queda intacto). Pestaña "Plantilla" quitada del admin (HTML + JS), quedan solo Seguimiento/Crear Cotización.
- **2 bugs reales de pdfmake encontrados y corregidos probando** (primera vez que este proyecto usa la librería — detalle completo en "Ya resueltos"): una tabla con `absolutePosition` muy cerca del borde inferior fuerza una página en blanco extra en vez de solo dibujarse ahí (corregido dándole más margen); y el resumen "Horas totales" del temario podía quedar solo en una página casi en blanco cuando los temas llenaban la página anterior casi exacto (corregido agrupándolo `unbreakable` con el último tema).
- Probado generando PDFs reales (no solo revisando código) con las 6 combinaciones de herramienta/color, un temario corto (2 temas) y uno largo de prueba (10 temas, para forzar la paginación — el temario real de Excel/Power BI no llega a tanto), y un caso sin `dirigido_a`/`objetivo` (temario Personalizado) — confirmado visualmente en el navegador (splitting cada PDF en páginas sueltas con `pdf-lib` para poder verlas una por una) que: las 6 combinaciones de color son legibles (incluyendo Power BI, la más fácil de arruinar), el temario largo pagina sin cortar un tema a la mitad, el callout "Dirigido a/Objetivo" no deja hueco cuando viene vacío, y el pie de página con el correo de contacto sale de `COTIZACION_CONTACT_EMAIL`.
- `npm test` (las 3 suites existentes, ninguna relacionada con Cotizaciones) sigue en verde tras el `npm install` que regeneró `package-lock.json` con la dependencia nueva.
- **Pendiente para Alfredo:** configurar la Application Setting `COTIZACION_CONTACT_EMAIL` en Azure (con su correo real de LifeZen mientras tanto); correr `sql/008_cotizacion_datos.sql`; confirmar el flujo completo en producción con datos reales.

### 2026-09-10 alfredo.pina: Reacomodo de navegación del admin — Tracking Comercial/Operación, pestañas Consultar/Crear (teorización, sin construir)

Segunda parte de la sesión de teorización del día, ya con el manual de marca y el diseño de Cotizaciones cerrados. Alfredo trajo un cambio de fondo: quiere 2 tableros ("Tracking Comercial"/"Tracking Operación") accesibles directo desde el sidebar, sin enterrarlos dentro de un módulo — porque son "pizarras vivas" que va a revisar día a día.

- **Diagnóstico se reclasifica de Comercial a Operación** — corrección real al modelo, no solo acomodo visual: la teoría original (sesión del 2026-09-05) lo puso en Comercial asumiendo que calificaba leads antes de cotizar; en la práctica Alfredo casi siempre lo usa ya vendido el curso, para nivelar el grupo por conocimiento previo. Actualizado en "Modelo del negocio y roadmap estratégico" (pilar Operación y Fase 1/2).
- **Fase 1.3 "CRM en admin" se cierra sin construir aparte** — la idea original (conectar Solicitudes+Cotizaciones+seguimiento) ya la resuelve "Tracking Comercial" (el tablero de Seguimiento que ya existía dentro de Cotizaciones, promovido a ítem de sidebar). Con esto, Fase 1 — Comercial queda completa en su alcance original.
- **Renombrar "Cierre"→"Operación" en el sidebar reusa un nombre que queda libre** — Clientes y el placeholder "Tracking" se mudan a otro lado, así que el nombre "Operación" (que además ya usaba el pilar de arriba desde el inicio del roadmap) queda disponible para el grupo que hoy tiene Encuestas/Diplomas, más Diagnósticos que se suma ahí. Efecto colateral bueno: cuando se construya la Fase 2 real (Grupos/Alumnos/Instructor), cae naturalmente en este mismo grupo, sin tener que reordenar otra vez.
- **Ícono "vivo"**: Alfredo pidió una viñeta que "prenda y apague como semáforo" en los ítems de Tracking para que se sientan vivos — se valida como fácil de construir reusando el patrón ya existente `.float-chip .dot` del sitio público (punto con glow + animación de pulso, respetando `prefers-reduced-motion`). Aclarado que no choca con la regla de deuteranomalía porque no codifica un estado (bien/mal/urgente) — es un solo color fijo que solo comunica "esto tiene datos en vivo". Decisión de diseño propia (no pedida explícitamente): el pulso solo va en "Tracking Comercial" (sí tiene datos reales) — "Tracking Operación" sigue siendo un placeholder sin construir, así que llevaría el tag "PRONTO" normal, no el pulso (pulsar algo vacío se sentiría roto).
- **Clientes, Solicitudes y Cotizaciones se estandarizan al patrón de 2 pestañas que ya usa Diplomas** ("Consultar X"/"Crear X") — Alfredo dudaba entre simplicidad (todo en un formulario aparte) y organización (poder ver contactos rápido); la propuesta que se acordó separa **ver** de **hacer**: contactos de solo lectura en "Consultar Clientes" (para el caso común de "rápido, ¿cuál es su correo?"), edición de verdad solo en "Editar Cliente". Confirmado revisando el código real de `admin/index.html` — hoy Clientes ya es maestro-detalle, Solicitudes es una sola vista con formulario inline (`solFormWrap`), y Cotizaciones ya bajó a 2 pestañas (Seguimiento/Crear) porque la sesión de pdfmake ya quitó Plantilla.
- **"Recotizar" no necesita un estatus nuevo** — Alfredo mismo concluyó, tras pensarlo, que "anular" no tiene sentido porque debe conservar historial; "Nueva versión" (ya construida, deja la vieja como "Reemplazada") cubre exactamente eso. Cerrado sin construir nada extra.
- **Columnas faltantes de Solicitud** (`participantes`/`modalidad`/`ciudad_sede`/`fecha_tentativa`, agregadas hace varias sesiones pero nunca mostradas en ningún lado del admin) — confirmado leyendo el código real de la tabla (`panel-solicitudes`, 8 columnas actuales, ninguna de esas 4). Se decidió meterlas en el desglose expandible que ya existe para los temas, no como columnas nuevas — evita una tabla más ancha de lo necesario.
- Se encontró un `<div id="panel-tracking">` vacío ya existente en el código (placeholder de una versión anterior del sidebar) — señalado para que el Prompt 1 lo reutilice/limpie en vez de dejarlo suelto.
- Todo esto se partió en **2 prompts separados** (sidebar+Tracking primero, pestañas Consultar/Crear después) para no chocar con la sesión constructora del PDF de pdfmake que corría en paralelo — ambos esperan a que esa sesión termine. Prompt 1 ya entregado a Alfredo; Prompt 2 queda pendiente de armar.
- **Pendiente para Alfredo:** correr el Prompt 1 en la sesión constructora (después de que termine la de pdfmake); confirmar el Prompt 2 antes de que se arme.

### 2026-09-10 alfredo.pina: Manual de marca + rediseño del PDF de Cotizaciones a pdfmake (sesión de teorización, sin construir)

Sesión de "director de proyecto" — Alfredo pidió simplificar visualmente la Cotización sin perder contenido y rebrandearla con su marca (ya no LifeZen). Nada de esto se construyó todavía, todo quedó preparado para la sesión constructora.

- **Manual de marca** (artifact publicado): logo/firma, colores (base + marca + paleta por herramienta + semáforo), tipografías, el motivo `fx`, iconografía/accesibilidad, tono de voz, y un checklist de contenido pendiente para la Cotización — `https://claude.ai/code/artifact/f8f8603f-4310-4b79-9b17-bf64cc32670f`. En el camino se encontró y corrigió un error de documentación: el color real de la firma (`assets/img/firma-ap.png`) es azul de marca `#3d7fff`, no gris carbón como decía `CLAUDE.md` desde el inicio del proyecto — nunca se había verificado contra el archivo real.
- **3 rondas de mockup del diseño de la Cotización** (mismo artifact, reemplazado en cada ronda, URL final `https://claude.ai/code/artifact/95e92c06-bf83-41df-8aa9-34b8fdbcea19`): v1 plano/blanco (rechazado, "sin vida") → v2 dark/tech extremo (reusando la cuadrícula, barras de fórmula y anillos de KPI reales del sitio) → v3/v4 punto medio: papel claro con tono y acentos de color reales (franja de color por herramienta, cuadrícula sutil, data-bars estilo Excel proporcionales a horas, tarjeta de precio con degradado suave). **Recomendación explícita de Claude, no solo gusto del usuario:** v4 es mejor para vender en este contexto — el documento lo procesan RH/Contabilidad de empresas tradicionales, se imprime/archiva/fotocopia, y un fondo oscuro pierde legibilidad y autoridad en ese flujo real, aunque se vea más "producto de software" en pantalla.
- **Contenido real recolectado para la Cotización nueva** (ya no genérico ni de LifeZen): bio corta tomada del Hero/Experiencia de `index.html` (no de la semblanza del PPT de LifeZen), foto ya existente (`assets/img/alfredo-work-photo.png`), los 6 términos y condiciones completos de una cotización real de referencia (curso Copilot 365 para KEMET) sin recortar ninguno, y decisión de usar un correo de contacto **configurable vía Application Setting** (`COTIZACION_CONTACT_EMAIL`) en vez de hardcodeado, porque Alfredo todavía no tiene correo `@alfredopina.ai` activo — mientras tanto se configura con su correo real de LifeZen para no mandar cotizaciones con un contacto que rebota.
- **Decisión técnica:** el PDF deja `pdf-lib`+plantilla PNG (que Alfredo tenía que diseñar y subir) por **`pdfmake`** — mismo criterio de siempre de evitar Puppeteer/navegador headless, pero ahora también sin imagen de fondo ni coordenadas x/y a mano; pdfmake pagina solo si el temario es largo. Esto elimina por completo la pestaña "Plantilla" de Cotizaciones y las 3 Functions que la soportaban (`subirPlantillaCotizacion`/`getPlantillaCotizacion`/`getGuiaCoordenadasCotizacion`).
- Prompt completo entregado a Alfredo para la sesión constructora — cubre la dependencia nueva, las fuentes (Space Grotesk/JetBrains Mono) e imágenes a agregar a `api/assets/`, la tabla de colores "seguros para texto" por herramienta (varios de los 6 colores de marca no tienen contraste suficiente como texto sobre blanco, ej. el amarillo de Power BI), qué Functions cambian/desaparecen, y las limitaciones reales de pdfmake vs. el mockup (sin blur, sin conic-gradient — aproximaciones explicadas para no atorar la sesión tratando de replicar algo que PDF no soporta).
- **Pendiente para Alfredo:** correr esa sesión constructora; mientras tanto, correr `sql/008_cotizacion_datos.sql` (sigue pendiente, sin relación con este cambio) y confirmar el flujo rediseñado de Crear Cotización del 2026-09-09 (ver entrada de abajo) si no lo ha hecho ya.

### 2026-09-09 alfredo.pina: Cotizaciones — Fase 3 Comercial completa, la pieza más grande del bloque

Cuarta sesión del día, después de Solicitudes. Construye Cotizaciones completo (PDF, backend, admin de 3 pestañas, plantilla) sobre el esquema `Cotizacion` que ya existía desde Fase 1 — detalle técnico completo en "Estado del proyecto" → sección Cotizaciones arriba, no repetido aquí. El primer mensaje de Alfredo sobre esta fase llegó truncado (se cortó a media instrucción SQL); en vez de adivinar el resto del alcance (PDF/pricing/versionado/correo, una superficie grande), se preguntó explícitamente antes de construir — Alfredo mandó la especificación completa después.

- `sql/008_cotizacion_datos.sql`, `api/src/cotizacion-pdf.js`, `api/src/cotizaciones-storage.js`, extensión de `api/src/plantillas-storage.js`, 9 Functions nuevas (`previsualizarCotizacion`, `crearCotizacion`, `listCotizacionesAdmin`, `getResumenCotizacionesAdmin`, `actualizarEstatusCotizacion`, `getCotizacionPdf`, `subirPlantillaCotizacion`, `getPlantillaCotizacion`, `getGuiaCoordenadasCotizacion`) + sección Cotizaciones en el admin (Seguimiento/Crear Cotización/Plantilla).
- **`window.TemarioSelector`** — el selector de temario que vivía solo dentro de Solicitudes se extrajo a un componente compartido, y el IIFE de Solicitudes se refactorizó para consumirlo (cumple el pedido explícito de Alfredo de "no repetirlo desde cero"). Probado que produce el mismo payload exacto que antes del refactor — regresión limpia, sin romper nada de lo ya construido y confirmado.
- Implementado el mockup de Seguimiento ya aprobado tal cual (scorecard de 5 KPIs, alerta de ≥10 días sin respuesta, semáforo/badge por fila, 3 acciones por fila).
- **Bug real de CSS encontrado y corregido esta sesión** (nueva familia, ver "Ya resueltos"): `.cot-alert{ display:flex }` le ganaba al atributo `hidden` por tener la misma especificidad que `[hidden]` del user-agent — el banner de alerta se quedaba visible vacío. Corregido con `.cot-alert[hidden]{ display:none; }` explícito.
- Probado de punta a punta con `window.fetch` mockeado (mismo patrón de siempre): scorecard/alerta/semáforo/badges con datos variados, los 4 tabs de vista, selector de temario compartido (Estándar/Personalizado + suma de horas), cálculo de precio con/sin descuento de grupo chico, precarga desde Solicitud y desde "Nueva versión" (resolviendo el temario estándar por nombre contra el catálogo), Vista previa y Crear, y regresión completa de Solicitudes.
- **Pendiente para Alfredo:** correr `sql/008_cotizacion_datos.sql`; diseñar y subir la plantilla de fondo real (hoy no hay ninguna, el PDF usa posiciones placeholder); confirmar el flujo completo en producción.

**Segunda pasada el mismo día** — Alfredo probó en producción justo después de correr `sql/008` y reportó dos síntomas: el buscador de cliente de Crear Cotización no mostraba resultados, y "Solicitud de origen" nunca se llenaba. Diagnosticado como un solo bug (el segundo era consecuencia del primero: sin poder elegir un cliente, el `onChange` que carga Solicitudes nunca se disparaba) — el buscador usaba `apiGet` sin reintento, así que una base dormida lo hacía fallar en silencio. Corregido a `apiGetConReintento` + toast visible en caso de fallo real (ver "Ya resueltos").

**Tercera pasada el mismo día — rediseño de Crear Cotización**, a partir de feedback directo de Alfredo: toda Cotización debe nacer de una Solicitud, no de captura libre. Se preguntó primero (nombre para separar Seguimiento a su propio módulo, alcance, y borrado real vs. estatus) antes de tocar código — Alfredo pidió posponer lo de separar Seguimiento ("luego lo trabajo") y no llegó a responder las otras dos, así que se tomó la decisión por default más consistente con el resto del proyecto (estatus `Descartada`, nunca borrado real) y se avisó explícitamente.
- Crear Cotización pasó de un formulario libre (cliente/herramienta/temario a mano, con "Solicitud de origen" opcional) a 3 vistas dentro de la misma pestaña: lista de Solicitudes activas para cotizar → detalle de solo lectura + precio → edición de la Solicitud si algo cambió. Detalle completo del flujo en "Estado del proyecto" arriba.
- **Function nueva `editarSolicitud`** (UPDATE, mismas validaciones que `crearSolicitud`) + estatus nuevo **`Descartada`** agregado a `actualizarEstatusSolicitud` (el "eliminar" de la lista — nunca borra de verdad, mismo criterio que Cliente/Diploma).
- El autocompletado de Cliente que se había arreglado en la segunda pasada quedó **eliminado por completo** en este rediseño (ya no hace falta, el cliente viene resuelto desde la Solicitud) — la corrección en sí (usar `apiGetConReintento`) se queda documentada como regla general para cualquier buscador nuevo contra esta base.
- **Ajuste propio encontrado probando el rediseño, antes de que Alfredo lo topara:** la primera versión cargaba las tarifas (`cargarTarifasCache`) de una vez al abrir el panel Cotizaciones — se corrigió a perezoso (solo al abrir la pestaña Crear Cotización), mismo criterio que Tarifas/Respaldos en Configuración, para no despertar la base sin necesidad.
- Probado de punta a punta con `window.fetch` mockeado: la lista de Solicitudes activas, Cotizar (resumen de solo lectura con el desglose completo de temas), Editar desde la lista y desde dentro del detalle (con precarga correcta del temario), Descartar, el cálculo de precio, Crear armando el payload completo, "Nueva versión" saltando directo al detalle (con y sin Solicitud ligada), y regresión de Solicitudes.
- **Pendiente para Alfredo:** confirmar el flujo rediseñado en producción; decidir (cuando quiera retomarlo) el nombre y alcance del módulo de tracking que se separaría de Seguimiento — quedó pausado a su pedido, ver "Pendientes de Alfredo" al inicio del documento.

### 2026-09-09 alfredo.pina: Solicitudes — Fase 2 Comercial completa

Tercera sesión del día, después de la ronda de pulido de Clientes. Construye Solicitudes completo (backend + admin) sobre la tabla que ya existía desde Fase 1 — detalle técnico completo en "Estado del proyecto" → sección Solicitudes arriba, no repetido aquí.

- 3 Functions nuevas (`listSolicitudesAdmin`, `crearSolicitud`, `actualizarEstatusSolicitud`) + sección Solicitudes en el admin, sin tocar el esquema SQL (la tabla `Solicitud` ya existía).
- El selector de temario reusa `getCatalogoCursos` (la misma Function pública de `cursos.html`) en vez de inventar una consulta nueva — mismos datos, mismo patrón herramienta→Estándar/Personalizado, solo con estilo de admin en vez del constructor público.
- A propósito, sin botón "Convertir a Cotización" ni cambio automático de estatus — Cotización no existe todavía (llega en Fase 3), y un botón que no hace nada hubiera confundido más que ayudado.
- Probado de punta a punta con `fetch` mockeado: cliente nuevo y existente, temario estándar y personalizado (suma de horas en vivo), alta de contacto desde el mini-form inline, y cambio de estatus persistiendo.
- **Pendiente para Alfredo:** confirmar el flujo completo en producción.

**Segunda pasada el mismo día**, tras el primer vistazo de Alfredo — pidió 4 datos más para Solicitud (fecha tentativa, ciudad/sede, participantes, modalidad, los mismos que ya captura el form público de `cursos.html`), aclaró que sí necesita el desglose completo de temas (con descripción) guardado incluso para temarios estándar —no solo el nombre—, un scorecard igual al de Clientes, orden bidireccional igual que en Clientes, y que los botones "+ Nuevo…" no se sentían con forma de botón.

- `sql/007_solicitud_datos_cotizacion.sql` (nuevo, pendiente de correr): 4 columnas nuevas en `Solicitud`, 2 con CHECK.
- `temas_json` dejó de guardar solo el nombre en modo "estándar" — ahora siempre lleva el desglose completo (temas + descripción), resuelto del catálogo público en el momento de guardar.
- Nueva Function `getResumenSolicitudesAdmin` + scorecard (Manuales/Sitio/% Atendidas) y 3 botones de orden (Fecha/Cliente/Horas) — mismo patrón exacto que se construyó para Clientes horas antes en la misma sesión.
- `.radm-btn-add` (usado en todo el admin, no solo Solicitudes) pasó de "ghost" a llevar un tinte de fondo — en el momento no se pudo reproducir un bug real de borde invisible como los de la sesión anterior, así que se interpretó como una observación de diseño. **Corrección: sí había un bug real, encontrado en la siguiente pasada — ver la tercera entrada de este mismo día, justo abajo.**
- **Pendiente para Alfredo:** correr `sql/007_solicitud_datos_cotizacion.sql`; confirmar en producción.

**Tercera pasada el mismo día** — Alfredo confirmó que el botón "+ Nuevo contacto" seguía sin funcionar, y pidió separar Horas en su propia columna numérica (la necesita para la Cotización) porque la tabla no mostraba el detalle del temario/temas, y que el bloque de filtros+orden seguía viéndose desordenado.

- **Bug real encontrado esta vez, con repro en el navegador:** `.radm-btn-add:disabled` no tenía NINGÚN estilo propio — un botón deshabilitado se veía idéntico a uno habilitado (mismo color, misma opacidad). "+ Nuevo contacto" arranca deshabilitado hasta elegir un cliente EXISTENTE de la lista (un cliente nuevo no tiene id todavía) — Alfredo probablemente lo probó sin seleccionar la sugerencia del autocompletado (o con un cliente nuevo) y, al verse el botón exactamente igual que si estuviera activo, parecía roto. Corregido con `opacity:.4` + `cursor:default` en `:disabled`, más un `title` explicando la condición.
- **Segundo bug real, mismo patrón que el de `border-color` de la sesión pasada pero en `background`:** el tinte de fondo que se le agregó a `.radm-btn-add` en la pasada anterior (`background:color-mix(...)`) resolvía a **transparente del todo** en la hoja de estilos real (confirmado con `rule.style.backgroundColor` vacío, aunque una prueba aislada del mismo CSS sí funcionaba) — por eso el botón deshabilitado y el habilitado se veían igual de "sin forma" incluso después del refuerzo visual de la pasada anterior. Corregido cambiando `background` → `background-color` (long-hand) en `.radm-btn-add` y su `:hover` — arregla el tinte en TODO el admin, no solo en Solicitudes. Detalle completo y la regla general (evitar shorthands `border`/`background` con `var()`+`color-mix()` en esta hoja) en "Ya resueltos" arriba.
- **Tabla de Solicitudes**: columna "Horas" separada (numérica, para usarla directo en la Cotización) de la columna "Temario / Temas", que ahora muestra el nombre real (temario estándar) o los nombres de los primeros temas (personalizado) en vez de solo un conteo — con un botón que despliega una fila de desglose completo debajo (cada tema con nivel, horas y **descripción**, ya que `temas_json` desde la pasada anterior guarda ese detalle completo).
- **Layout**: filtros + orden + "Nueva Solicitud" ahora viven dentro de una sola tarjeta (`.sol-toolbar`, igual patrón que `.cli-toolbar` de Clientes) con una línea divisoria entre el bloque de filtros y el de orden, en vez de flotar sueltos sobre el fondo del panel.
- Probado de punta a punta en el navegador: botón deshabilitado vs habilitado con opacidad visiblemente distinta, fondo de "+ Nuevo Cliente"/"+ Nueva Solicitud" con el tinte correcto tras el fix, columna Horas separada, desglose de temas expandiéndose con descripción y el chevron rotando.
- **Pendiente para Alfredo:** confirmar en producción, sobre todo el flujo de "+ Nuevo contacto" con un cliente existente seleccionado (clic real en la sugerencia, no solo escribir el nombre).

### 2026-09-09 alfredo.pina: Clientes — ronda de ajustes tras la primera prueba real (layout, planta, antigüedad, orden, scorecard, tarifas con confirmación)

Alfredo probó el módulo recién construido y trajo una lista concreta de ajustes en una sola sesión — detalle técnico completo ya integrado en "Estado del proyecto" → sección Clientes/Contactos/Tarifas arriba, no repetido aquí.

- `sql/006_comercial_ajustes.sql` (nuevo, pendiente de correr por Alfredo): `planta` en `Contacto`, `cliente_desde` en `Cliente`.
- **Rediseño de layout**: de acordeón (mucho espacio muerto a la derecha, señalado por Alfredo) a maestro-detalle — lista compacta a la izquierda, detalle del cliente seleccionado a la derecha. Nueva Function `getResumenClientesAdmin` para el scorecard de 3 KPIs que pidió (Clientes/Contactos/% Activos).
- **Orden y búsqueda ampliada**: 3 botones de orden (Nombre/Última cotización/Antigüedad) resueltos con un solo query parametrizado en `listClientesAdmin` (`CASE WHEN @sort = ...`); "última cotización" y "% activos" ya apuntan a la tabla `Cotizacion` real (vacía hasta Fase 1.2) en vez de quedar pendientes — arrancan solos cuando esa fase exista, sin volver a tocar este código. Búsqueda ahora también encuentra por nombre de contacto (`EXISTS` contra `Contacto`), pedido explícito de Alfredo porque a veces recuerda antes a la persona que a la empresa.
- **Tarifas por hora**: Alfredo pidió explícitamente ser "más precavido" porque solo la toca ~1 vez al año — se quitó el autoguardado al perder el foco (de la sesión anterior) y se reemplazó por un botón "Actualizar" por fila (deshabilitado hasta que cambia el valor) + `confirm()` con el precio anterior y el nuevo antes de escribir en la base. De paso, borde/glow de color por herramienta (misma paleta de marca que ya usa el resto del admin).
- **Configuración**: quitada la sub-sección Plantillas (ya no vivirá ahí); Tarifas y Respaldos pasaron de auto-cargar sus datos cada vez que se abre Configuración a un botón "Desplegar" explícito — evita despertar la base SQL/pegarle a Table Storage sin que Alfredo vaya a usar esa sección, mismo criterio que ya se aplicó al polling del contador de Encuestas en una sesión anterior.
- Probado de punta a punta en el mismo servidor HTTP local con `fetch` mockeado: layout maestro-detalle con selección, alta de cliente con año de antigüedad, contacto con planta, los 3 órdenes (antigüedad con nulls al final confirmado), búsqueda por nombre de contacto, scorecard con números reales, flujo de tarifa aceptando y cancelando el `confirm()`, y el colapsable de Respaldos disparando la carga solo al desplegar.

**Segunda pasada el mismo día**, tras ver el rediseño ya desplegado — Alfredo trajo pulido visual concreto: separar el scorecard, reordenar el toolbar (se sentía "revuelto"), hacer el orden bidireccional (reportó que "ordenar por Nombre no funcionó" — resultó ser que clic en un criterio ya activo no hacía nada visible, no un bug de datos), conectar visualmente la fila seleccionada con su panel de detalle, y arreglar el botón "Desplegar" que se veía "como una palabra suelta perdida por ahí".
- `listClientesAdmin` ahora acepta `?dir=asc|desc` — mismo patrón de query parametrizado con `CASE WHEN`, sin SQL dinámico. Clic en un criterio de orden ya activo alterna dirección en vez de no hacer nada; ícono ↑/↓ junto al criterio activo.
- **2 bugs reales de CSS encontrados probando en el navegador, no visibles solo leyendo el código** (detalle completo y la regla a seguir en "Ya resueltos" arriba): mezclar el shorthand `border-color` con un long-hand de un solo lado (`border-left-color`) en la misma regla hacía que el navegador calculara los otros 3 lados como inválidos — el borde de selección de Clientes nunca se pintaba; y un selector descendiente compuesto (`.padre.estado .hijo`) no aplicaba el `transform` del chevron de "Desplegar" pese a que `querySelector` confirmaba que sí calzaba. Ambos corregidos con patrones más simples (shorthand completo en vez de mezclar; clase de estado directo en el elemento afectado en vez de en un ancestro).
- **Bug real adicional, mismo criterio:** `panel-config` nunca declaró la variable CSS `--c` que usan `.radm-btn`/`.radm-btn-add` — los botones "Desplegar" y "Respaldar ahora" llevaban meses sin borde visible ahí (no reportado hasta ahora porque nadie había mirado con atención esa esquina). Corregido agregando `--c` al panel completo.
- **Pendiente para Alfredo:** correr `sql/006_comercial_ajustes.sql`; confirmar el flujo completo en producción, incluyendo el orden en ambas direcciones.

### 2026-09-08 alfredo.pina: Clientes/Contactos/Tarifas — arranca Fase 1 Comercial, esquema completo de Solicitud/Cotizacion adelantado

Primera sesión constructora de Fase 1 (bloque Comercial del roadmap). Partió de un prompt de handoff con el esquema y alcance ya definidos — ver el detalle técnico completo en "Estado del proyecto" → sección Clientes/Contactos/Tarifas arriba, no repetido aquí.

- `sql/005_comercial.sql`: agrega `notas` a `Cliente`, crea `Contacto` y `TarifaHerramienta` (con Functions/UI reales en esta sesión), y adelanta el esquema completo de `Solicitud`/`Cotizacion` (sin Functions todavía — llegan en Fase 1.1/1.2) para no tener que volver a tocar el modelo de datos cuando les toque su turno.
- 9 Functions nuevas + sección Clientes en el admin (dentro de Operación, le quita el "PRONTO") + mini editor de Tarifas dentro de Configuración — detalle completo arriba.
- **Bug real encontrado y corregido probando en el navegador**: el formulario de alta/edición de Contacto no se cerraba tras guardar exitosamente (quedaba abierto con los datos ya enviados, aunque el toast confirmaba y el contacto sí aparecía arriba en la lista) — faltaba un `form.remove()` explícito, porque el formulario vivía como hermano de la lista de contactos, no dentro de ella, así que redibujar la lista nunca lo tocaba.
- **Primera vez que este proyecto usa el navegador integrado con un servidor local real** (`npx http-server` vía `.claude/launch.json`, nuevo) en vez de solo mockear `fetch` por consola sin servidor — posible gracias a que Alfredo instaló Node la sesión anterior. Probado de punta a punta con `window.fetch` mockeado en memoria: alta de cliente, expandir/colapsar contactos sin perder estado de otros formularios abiertos, 2 contactos marcando ambos como principal (confirmado que el candado de "un solo principal por cliente" se refleja bien en la UI), edición de notas, y guardado de tarifa al perder el foco.
- **Pendiente para Alfredo:** correr `sql/005_comercial.sql` en el Query Editor; confirmar el mismo flujo en producción; ajustar las 6 tarifas de $1,200 de ejemplo a precios reales.

### 2026-09-08 alfredo.pina: Node.js instalado — primeras pruebas reales del proyecto, bug de timezone encontrado

Sesión larga de teorización + cierre de pendientes de las 3 auditorías (backups, PITR, protección de datos de Blob, Viridiana pendiente, alerta de costos), que terminó con un hito real: Alfredo instaló Node.js en su equipo (guiado paso a paso, primera vez que lo usa) — hasta ahora, **ninguna sesión de este proyecto había podido correr código de verdad**, todo se validaba leyendo con cuidado.

- Con Node instalado: `npm install` real en `api/` (188 paquetes, regenerando `package-lock.json` desde el que solo tenía `node-ical` de hace varias sesiones) y `npm test` corrido de verdad por primera vez.
- **Bug real encontrado de inmediato, invisible por lectura de código**: `test-availability.js` fallaba 6 de 26 verificaciones. Causa: `findDay()` construía una fecha con setters UTC pero la leía con getters de hora LOCAL de la máquina — en cualquier equipo fuera de UTC+0 (como el de Alfredo) eso recorre el calendario un día y compara contra el día equivocado. El código real (`getAvailability`/`availability-logic.js`) nunca tuvo el problema, es un bug aislado de la prueba. Corregido a UTC de punta a punta; las 3 suites (`test-availability`, `test-backup`, `test-rate-limit`) ya pasan limpias contra una instalación real con `npm ci`.
- Workflow de deploy cambiado de `npm install` a `npm ci` (más reproducible, ya no hay desincronía de lockfile).
- **De regalo, primer `npm audit` real del proyecto**: 6 vulnerabilidades moderadas, todas transitivas (vía `mssql`→`tedious`→`@azure/identity`, y vía `@azure/data-tables`→`uuid`), ninguna alcanzable por el uso real del proyecto (SQL usa autenticación simple, no Azure AD; nada llama a `uuid` con buffer propio). El fix requiere `mssql@12.7.1` (salto de 2 versiones mayores) — **no se forzó**, por el riesgo de romper Diplomas/Encuestas sin poder probarlo contra la base real todavía. Queda pendiente de decisión con Alfredo, ver checklist de pendientes.
- Antes de esto, en la misma sesión: se cerraron en producción los 3 pendientes de la auditoría de resiliencia (soft-delete + versionado de blobs en `apcwebrecursos`, PITR de `apcweb-backoffice` confirmado en 7 días — límite fijo de la oferta gratuita + auto-pausa), y se creó `RUNBOOK.md` (checklist accionable de reconstrucción de infraestructura, pendiente desde la auditoría de resiliencia y que se me había quedado pendiente en una sesión anterior). También se consolidó definitivamente el checklist de pendientes de Alfredo, eliminando la duplicación que había quedado con la vieja sección "Pendiente / roadmap".
- Discusión de arquitectura útil en el camino: Alfredo notó correctamente que Cotizaciones (Fase 1.2, futura) va a necesitar referenciar Temas/Temarios (que viven en Table Storage, no SQL) — se acordó que la solución es una "foto" de los datos del tema al momento de crear la Cotización (no una referencia viva), evitando necesitar JOINs entre las dos bases; anotado para cuando se diseñe Cotizaciones a detalle.
- **Pendiente para Alfredo:** decidir sobre las vulnerabilidades de `npm audit` (ver arriba); dar a Viridiana rol Lector en `GR_AlfredoPina`; configurar la alerta de Cost Management para SQL (quedó a medio hacer, un error de "correo duplicado" en el asistente).

### 2026-09-08 alfredo.pina: Respaldos de Table Storage + candado de intentos generalizado

Dos partes independientes, cada una probada por separado. Detalle técnico completo ya integrado en "Estado del proyecto" arriba.

- **Respaldos**: cierra el hallazgo Alto de la auditoría de resiliencia del mismo día — backend completo (`backup-tables.js` + 4 Functions: timer semanal, manual, listar, descargar) más la sección nueva en admin → Configuración → Respaldos. Blob privado nuevo `respaldos`, autocreado por código.
- **Candado de intentos generalizado**: `checarBloqueo`/`registrarIntentoFallido`/`limpiarIntentos` salieron de `getRecursos` a `api/src/rate-limit.js`, parametrizados por `table`+`partitionKey`, listos para que Diagnóstico (Fase 1.4) los reuse sin copiarlos a mano — todavía no conectado a nada nuevo, solo reubicado. En el camino se corrigió un bug de UX real que se encontró probando: `recursos.html` no mostraba el mensaje real del backend en un 429 (bloqueado), solo un genérico que confundía.
- Ambas partes probadas sin Node real (sigue sin estar instalado en esta máquina): 2 pruebas locales nuevas sin red (`test-backup.js`, `test-rate-limit.js` con una tabla falsa en memoria simulando Table Storage — 10 fallos bloquean, código correcto limpia, fail-open si la tabla "truena") verificadas a mano línea por línea; el admin y `recursos.html` sí se probaron de verdad abriendo los HTML en el navegador y disparando los flujos por consola (confirmado: el botón "Respaldar ahora" y la lista fallan con un mensaje claro sin backend real, como se espera; el mensaje de 429 ya se ve correcto en la pantalla de desbloqueo).
- **Pendiente para Alfredo (no requiere a Claude):** confirmar en producción con datos reales — generar un respaldo manual y revisarlo, y esperar al primer lunes para el automático.
- **Corrección el mismo día** (ver entrada siguiente, más reciente): el "timer semanal" descrito arriba nunca llegó a desplegarse — Static Web Apps rechazó el build por usar `timerTrigger`. Reemplazado por HTTP + secreto compartido disparado desde GitHub Actions.

### 2026-09-08 alfredo.pina: Bug de plataforma — timerTrigger no soportado, respaldo automático corregido a HTTP + cron de GitHub Actions

Descubierto porque el deploy del respaldo automático (entrada anterior) falló en producción — Alfredo compartió el log de GitHub Actions, ahí se vio el error real de Oryx.

- **Causa**: las Functions "administradas" de Static Web Apps (tier Free) solo soportan `httpTrigger` — un `timerTrigger` como el de `api/respaldoSemanal/` hace que Oryx rechace el build **completo** del deploy, no solo esa Function (por eso Alfredo tampoco veía el botón nuevo de Respaldos: el deploy entero nunca llegó a producción). Documentado en "Ya resueltos" para no repetirlo.
- **Corrección**: `api/respaldoSemanal/` borrado, reemplazado por `api/respaldoAutomatico/` — Function HTTP pública (sin rol admin, sin entrada en `staticwebapp.config.json`) que valida un secreto compartido en el header `x-backup-secret` contra la Application Setting `BACKUP_CRON_SECRET`. Disparada por el nuevo `.github/workflows/respaldo-semanal.yml` (cron `0 6 * * 1`, más `workflow_dispatch` para poder correrlo a mano desde la pestaña Actions sin esperar al lunes).
- Diagnosticado leyendo directamente el código del repo (sin Node) y pidiéndole a Alfredo capturas del log real de GitHub Actions — el mismo patrón de diagnóstico que ya se ha usado varias veces en el proyecto cuando no hay acceso directo a logs de Azure.
- **Pendiente para Alfredo (no requiere a Claude):** generar el secreto y guardarlo en Application Settings (Static Web App) y en GitHub Actions Secrets, con el mismo valor en ambos lados — sin eso, `respaldoAutomatico` regresa 401 y no hay respaldo real todavía. Correr el workflow a mano una vez (`workflow_dispatch`) para confirmar que sí genera un archivo en el contenedor `respaldos`.

### 2026-09-08 alfredo.pina: Lote de correcciones — trilogía de auditorías de seguridad/resiliencia/calidad

Sesión de ejecución directa (no exploratoria): aplica los 12 puntos ya diagnosticados por las 3 auditorías de solo lectura corridas antes ese mismo día (seguridad, resiliencia, calidad de código). Probado bloque por bloque con lectura/grep cuidadoso — esta máquina sigue sin Node/npm, así que no hay corrida real de `npm test` ni de la Function contra Table/SQL reales; falta confirmar en producción.

- **Seguridad:** `buscarClientes`/`enviarRespuesta`/`getInstructores`/`getPreguntasEncuesta` ya no regresan `err.message` crudo al cliente (mensaje genérico + log real solo en `context.log.error`; `enviarRespuesta` distingue con un flag `safe` sus propios mensajes de validación intencionales de un error real del driver). `getRecursos` ahora lleva rate limiting por curso (tabla nueva `IntentosCodigo`, autocreada — 10 fallos en 15 min bloquean 5 min, fail-open si Table Storage falla). Comillas simples escapadas (`api/src/odata-escape.js`) antes de interpolar en cualquier filtro OData de Table Storage, en los 7 archivos identificados más los 2 que se renombraron abajo.
- **Limpieza:** borrado `api/getResumenProductosAdmin/` (código muerto). Carpetas `adminGetRecursos`→`getRecursosAdmin` y `adminListCursos`→`listCursosAdmin` (ya coinciden con su `route` real). Nuevo `api/src/http.js` con `JSON_HEADERS` compartido — las 44 Functions que lo repetían literal más `getAvailability` (que no mandaba `Cache-Control: no-store`) ahora lo importan de ahí. Workflow de GitHub Actions corre `npm test` antes del deploy — **con un ajuste sobre lo pedido:** el `package-lock.json` estaba desactualizado (solo registraba `node-ical`, de antes de que se agregaran `mssql`/`pdf-lib`/`@azure/*`), así que `npm ci` habría fallado por desincronía; se usó `npm install` en su lugar, dejado documentado en el mismo workflow para volver a `npm ci` en cuanto alguien con Node regenere el lockfile completo. `api/package.json` con las 6 dependencias a versión exacta (sin `^`).
- **Robustez:** `generarDiplomas` detecta colisión de folio (error SQL Server 2627/2601) y regresa un mensaje amigable en vez del error crudo del driver. `sql/004_encuesta_constraint.sql` (nuevo, pendiente de correr por Alfredo en el Query Editor del portal, igual que los anteriores) agrega el `UNIQUE (respuesta_id, pregunta_id)` a `EncuestaRespuestaDetalle`. `buildSlotGrid` recorta el último slot del día con `Math.min` si `workingHours`/`slotMinutes` no son múltiplo exacto — con la config actual (07:00-19:00, bloques de 60 min) no cambia nada, es defensa para si Alfredo edita `agenda-config.json` a futuro.
- **Accesibilidad:** las 5 páginas públicas (`index.html`, `cursos.html`, `agenda.html`, `encuesta.html`, `recursos.html`) ya tienen skip-link (`.skip-link`, oculto salvo foco de teclado, agregado a `assets/css/style.css` y replicado en el `<style>` inline de las 3 páginas que no comparten esa hoja) y `<main id="contenido">` — **un ajuste sobre lo pedido:** el `<footer>` de cada página se dejó FUERA de `<main>` (landmark `contentinfo` propio) en vez de envolver "todo lo que no es el nav" literal, para no anidar dos landmarks de tipo distinto.
- **Pendiente para Alfredo (no requiere a Claude):** correr `sql/004_encuesta_constraint.sql` en el Query Editor; en algún momento con Node a mano, regenerar `package-lock.json` (`npm install` dentro de `api/`) y cambiar el workflow de `npm install` a `npm ci`; confirmar en producción que el rate limiting de `getRecursos` y el resto de las Functions tocadas siguen funcionando igual (esta sesión no pudo probar contra Table/SQL reales).

### 2026-09-08 alfredo.pina: Encuestas — quitar footer de contacto, reintento automático y nav consistente

Continuación directa de la ronda de ajustes del día anterior, ya con Alfredo probando la encuesta en producción real (primera vez que la abrió desde fuera del admin). Detalle técnico ya integrado en "Estado del proyecto" → sección Encuestas arriba.

- Alfredo pidió quitar la sección "Hablemos" del footer de `encuesta.html` por distraer del formulario — se dejó un footer mínimo con solo el copyright.
- **Bug real reportado por Alfredo probando en producción:** "no me aparece nada" al entrar a la encuesta directo (vs. sí funcionaba entrando por el QR). Causa: la URL real de producción lleva `.7.` antes de `azurestaticapps.net` (ya documentado como gotcha conocido) — fácil de teclear mal a mano, mientras que el QR siempre trae la URL correcta porque la genera el propio admin.
- **Segundo bug real, más serio:** "cargando" un buen rato y luego "error de red", que luego sí cargó solo. Causa raíz: la base SQL serverless se auto-pausa tras 1h y el primer intento de conexión después de eso puede tardar hasta 60s en "despertarla" — a veces ese primer fetch se cae por timeout antes de que la base termine de arrancar. Se agregó reintento automático con pausas crecientes (ver "Estado del proyecto" arriba) para que esto quede invisible para quien esté contestando en vivo.
- Alfredo preguntó cómo podría él mismo despertar la base antes de mostrar el QR al grupo — la respuesta fue que ya lo tiene cubierto sin código nuevo: la pestaña QR/Link del admin ya dispara una consulta SQL al abrirla, solo hace falta abrirla un par de minutos antes en vez de al momento.
- Alfredo notó que `encuesta.html` no mostraba "Recursos" en el nav — se descubrió que `agenda.html` tampoco lo tenía (un link que se agregó a `index.html`/`cursos.html` en su momento pero nunca se retomó ahí). Se estandarizó: `agenda.html`, `recursos.html` y `encuesta.html` ahora muestran el mismo trío Conóceme/Cursos/Recursos.
- Probado en el mismo servidor HTTP estático local de siempre: el reintento automático probado en sus dos caminos (éxito tras fallar un par de veces, y agotar los 3 reintentos con espera real de ~40s confirmando que sí muestra el error final), y el nav de las 3 páginas confirmado por DOM directo (el navegador de pruebas de esta sesión reporta `window.innerWidth: 0` de forma consistente — no refleja un problema real, es la pestaña sin foco/oculta en este entorno).

### 2026-09-07 alfredo.pina: Encuestas — ronda de ajustes de UX y candado de la base de datos

### 2026-09-07 alfredo.pina: Encuestas — ronda de ajustes de UX y candado de la base de datos

Segunda pasada el mismo día, tras la primera prueba real de Alfredo del módulo recién construido — lista larga de ajustes concretos (nav/footer, copy, slider, reordenar secciones) más 2 preguntas suyas que llevaron a arreglos reales. Detalle técnico completo ya integrado en "Estado del proyecto" → sección Encuestas arriba, no repetido aquí.

- Nav y footer completos en `encuesta.html` (antes minimalistas) para que no se sintiera una página huérfana; reordenadas las secciones (Curso y Materiales antes que Instructor); botón de envío y pantalla de gracias con el motivo `fx` de marca.
- Empresa sin campo de código visible — se deriva del nombre automáticamente si es una empresa nueva, ya que Alfredo confirmó que él se encarga de tener las empresas reales precargadas de antemano.
- Data-bar convertido en slider real (`pointerdown`+`pointermove`, más teclado) — en el camino se encontró y corrigió un bug real: `setPointerCapture` puede tronar según el dispositivo y, sin try/catch, eso impedía que la calificación se registrara.
- Alfredo preguntó **"¿cómo funciona el borrado/edición de preguntas?"** → se respondió en el chat y además se agregó una nota fija en el admin (Preguntas) explicándolo ahí mismo, para no depender de la memoria de la sesión.
- Alfredo preguntó **"¿el polling de 20s no consume muchos recursos?"** → la respuesta llevó a encontrar un problema real: el contador de la pestaña QR se seguía actualizando aunque el admin estuviera en otra sección, manteniendo despierta la base SQL serverless sin necesidad (cada consulta le resetea el auto-pause) y gastando su cupo gratis de vCore-segundos/mes más rápido de lo debido. Se corrigió con un candado de 3 condiciones (`encQrTabActiva` + panel Encuestas activo + `!document.hidden`) que se revisa en cada tick antes de llamar a la API — el `setInterval` sigue vivo pero no hace red si no se cumple. Encontrado y corregido **antes** de subir el cambio, a partir de la pregunta de Alfredo, no reportado después.
- Rediseñada la pestaña QR/Link del admin (dos tarjetas tipo `.dip-paso`, mismo lenguaje que Plantilla de Diplomas) y agregado el KPI de "respuestas de hoy" junto al total.
- Probado en el mismo servidor HTTP estático local de siempre, incluyendo simular el paso del tiempo (esperas reales de ~20s) para verificar el candado de polling en los 3 escenarios: activo, cambiado de pestaña, y de vuelta.

### 2026-09-07 alfredo.pina: Encuestas — módulo completo de punta a punta (Fase 3.1, cierra Fase 3 junto con Diplomas)

Sesión partiendo de un prompt de handoff ya armado (esquema SQL + 8 Functions + página pública + admin), mismo estilo que la sesión de Diplomas del mismo día. Ver el detalle técnico completo en "Estado del proyecto" → sección Encuestas arriba, no repetido aquí.

- **Reuso de infraestructura de Diplomas** (misma base `apcweb-backoffice`, mismo helper de conexión) en vez de levantar nada nuevo — el único módulo con backend propio de Fase 3 que faltaba.
- **Decisión de seguridad tomada en el momento, no consultada antes de ejecutar:** `buscarClientes` y `getInstructores` estaban protegidas (rol admin) porque solo las usaba Diplomas; Encuestas las necesita desde `encuesta.html`, que es pública sin login. Se optó por quitarles la protección en `staticwebapp.config.json` en vez de duplicar Functions, por ser datos de baja sensibilidad (nombre/código de cliente, nombre de instructor ya impreso en diplomas) — detalle completo y el trade-off arriba, para poder revertir si algún día deja de ser aceptable.
- **`pregunta_id` sin FK real en `EncuestaRespuestaDetalle`** — decisión de esquema explícita para que `eliminarPregunta` (borrado real, no soft-delete) nunca tumbe ni se lleve consigo respuestas históricas ya guardadas.
- Control de escala construido como celda con formato condicional (`.xcell`, data-bar de Excel) — mismo lenguaje visual ya validado en el mockup aprobado por Alfredo en una sesión de teorización previa (opción 4 de varias alternativas evaluadas).
- Tarjeta imprimible con QR construida con Canvas 2D nativo en vez de una librería de captura de HTML, para poder exportarla como un solo PNG descargable reusando el mismo QR ya generado en pantalla — replica el segundo mockup aprobado (fondo tipo hoja de cuadrícula, fx =CALIFICAR_INSTRUCTOR(), chiste, QR, pie de página).
- **Bug propio encontrado y corregido en esta misma sesión, antes de dar el módulo por bueno:** el botón "+ Agregar" de Preguntas no estaba deshabilitado durante la carga inicial — si se hacía clic ahí antes de que el fetch a la base (que puede tardar hasta 60s si estaba dormida) terminara, la nueva pregunta calculaba su `orden` contra un banco todavía vacío y quedaba mal ordenada. Se encontró probando en el navegador con `window.fetch` mockeado y datos de prueba, no fue reportado por Alfredo.
- Probado de punta a punta en un servidor HTTP estático local temporal (PowerShell `HttpListener`, ya que esta máquina no tiene Node) con `window.fetch` mockeado en consola: encuesta pública completa (empresa nueva y existente, validación de preguntas sin responder), y las 3 pestañas del admin (alta/edición/borrado de preguntas con confirm, QR con la librería externa cargando bien, tarjeta imprimible verificada capturando el canvas antes de descartarlo, y Resultados armando columnas dinámicas correctas).
- **Pendiente para Alfredo (no requiere a Claude):** reemplazar los textos de pregunta de ejemplo por los definitivos, y probar el QR en vivo en la próxima sesión de cierre real.

### 2026-09-07 alfredo.pina: Diplomas — módulo completo de punta a punta (Fase 0 a 3, adelantado de Fase 3 del roadmap)

Sesión larga, todo en una sola pasada partiendo de un prompt de handoff que Alfredo trajo ya armado (infraestructura + backend + admin). Adelanta Diplomas (normalmente Fase 3) por el atraso real de 7-8 grupos sin diploma emitido, y es el primer módulo del proyecto en Azure SQL — ver el detalle técnico completo en "Estado del proyecto" → sección Diplomas arriba, no repetido aquí.

- **Fase 0 (infra):** Alfredo creó la base Azure SQL nueva (`apcweb-backoffice`/`apcweb-sql-server`, serverless con la oferta gratis, dentro de `GR_AlfredoPina`) guiado paso a paso por el portal — incluyó resolver que "admin" no es válido como usuario del servidor, activar el firewall de servicios de Azure + su propia IP, y correr el script de creación de tablas desde el Query Editor. Contenedor Blob privado `diplomas` nuevo en `apcwebrecursos`.
- **Fase 1 (backend base):** helper de conexión SQL, folio con consecutivo por Cliente+Año, PDF de prueba con plantilla placeholder — probado end-to-end vía scripts pegados en la consola del navegador (mismo patrón que la migración de Cursos), incluyendo el caso "No Aprobado" (consume folio, sin PDF) y anular+listar.
- **Bug encontrado en producción, no en el diseño:** la base serverless se auto-pausa tras 1h — la primera conexión después de una pausa tardaba más de los 15s default de `mssql` y daba timeout real. Se subió a 60s.
- **Fase 2 (plantilla real):** Alfredo mandó 3 diseños de diploma (estilo LifeZen, explícitamente de prueba — se rediseñan con marca alfredopina.ai después), se eligió el azul por espacio/orden visual. Iterado en varias rondas de captura→ajuste→captura (nombre más grande, curso/fechas más grandes, folio más separado del label, fuente Inter real en vez de la Helvetica genérica de PDF vía `@pdf-lib/fontkit`) hasta que quedó a gusto de Alfredo. La firma reutiliza la que ya usa el sitio como logo (`assets/img/firma-ap.png`).
- **Fase 3 (admin):** pestañas Crear Diplomas y Consultar Diplomas construidas completas de una vez (no se iteró en texto primero para esta parte — Alfredo ya había dado suficiente contexto en el prompt de handoff). Zip client-side con JSZip, sin tocar el backend.
- **Ronda grande de feedback de UX** tras la primera prueba real: date pickers invisibles en oscuro, Enter para agregar fila, todos los campos obligatorios con `*`, "Ver" forzaba descarga (cambiado a `Content-Disposition: inline`), nombre de zip con rango de folios, botones Limpiar filtros/Ver todos, Instructor y Nivel como listas en vez de texto libre, hints de formato — todo en una sola tanda de ajustes.
- **Pestaña "Plantilla" nueva** (fuera del alcance original, decidida a mitad de sesión): antes de construirla se paró a **iterar en texto** (a pedido explícito de Alfredo) el alcance del "editor visual de coordenadas" — se decidió NO construirlo (2-3x el trabajo del resto, no se paga con la frecuencia real de cambios de diseño) a favor de: vista previa + subir plantilla nueva + firmas por instructor + generar PDF de prueba + **guía de coordenadas descargable** (PDF con recuadros marcando dónde cae cada dato, para usar de referencia al diseñar). Sobre esa base, ronda 2 de feedback agregó: instructores dinámicos (antes hardcodeados) con alta/baja desde el admin, eliminar firma sin eliminar instructor, y el rediseño en 3 pasos numerados.
- **2 bugs propios corregidos en el momento** (no de la plataforma, ver "Ya resueltos"): `registerFontkit` faltante en la función nueva de la guía, y un `onerror` que reemplazaba un `<img>` con `id` por un `<span>`, rompiendo la actualización posterior tras subir un archivo.
- Cada pieza se probó en el navegador local (sin backend, esta máquina no tiene Node) antes de cada push — validaciones, parseo del pegado tipo Excel, apertura/cierre de mini-formularios — y luego en producción real por Alfredo con datos de prueba en cada ronda.
- **Pendiente para Alfredo (no requiere a Claude):** subir la plantilla rediseñada (marca alfredopina.ai) usando la guía de coordenadas, y procesar el backlog real de 7-8 grupos.

### 2026-09-06 alfredo.pina: Fase 0 v1.3.1 — regla de "curso completo" simplificada a solo el manual
- Ajuste rápido horas después de construir v1.3: Alfredo notó que en la práctica un curso solo lleva un manual, así que la regla de completitud (antes: ≥3 temas + ≥1 proyecto + las 5 categorías de recursos) se simplificó a **completo = ≥1 recurso tipo manual cargado**, sin más condiciones
- Cambio de una sola línea en `api/getResumenCompletitudAdmin/index.js` (la variable `completo`); temas y proyectos se siguen calculando y mostrando en el drill-down como contexto, ya no afectan el check verde
- Efecto secundario importante, ya documentado arriba: un curso de Recursos **ya no necesita estar vinculado a un Temario Estándar** para contar como completo — el manual se cuenta directo por `herramienta_cursoId`. El vínculo `temarioId` sigue existiendo y sigue sirviendo para mostrar temas/proyecto en el drill-down, solo dejó de ser requisito
- Ajustado también el texto del drill-down en `admin/index.html` (antes decía "sin vincular... no se puede evaluar", ya no es cierto con la regla nueva)
- Probado con `window.fetch` mockeado en consola (3 casos: curso con manual sin vincular a temario, curso vinculado con temas/proyecto pero sin manual, curso con manual y datos completos) — el check verde respondió correctamente en los 3 casos

### 2026-09-06 alfredo.pina: Fase 0 v1.3 — anillos de completitud, vínculo Cursos↔Recursos y arreglos de v1.2
- Cierra los 4 puntos que habían quedado pendientes de la v1.2 más 3 bugs/ajustes chicos que Alfredo encontró usándola — sesión sin pausas, todo ya estaba decidido en texto de la sesión anterior
- **Construido el vínculo Cursos↔Recursos** que faltaba: campo `temarioId` nuevo en el Curso de Recursos (`api/crearCurso`, `api/adminListCursos`) + dropdown en su admin para vincularlo por nombre a un Temario Estándar publicado — Alfredo ya no tiene que escribir IDs a mano, que era su preocupación original
- Nueva Function `getResumenCompletitudAdmin` — cruza por primera vez los dos módulos (Cursos y Recursos) para calcular qué % de los cursos publicados de cada herramienta están "completos" (regla ya cerrada en la sesión anterior: ≥3 temas, ≥1 proyecto desbloqueado, las 5 categorías de recursos con al menos 1 elemento)
- Dashboard → Productos: agregados los 6 anillos circulares animados (SVG, colores rojo oscuro/amarillo/verde ya elegidos por Alfredo por su deuteranomalía, siempre con el % en texto) y el drill-down (clic en anillo o en la barra, ambos abren el mismo detalle debajo — título del curso, temas, proyecto(s) que desbloquea, categorías de recursos cargadas, check en los completos)
- Arreglos reportados por Alfredo tras probar v1.2 en producción:
  1. Contraste de los íconos sol/luna del switch de tema — el activo ahora resalta (blanco o naranja), el inactivo se ve claramente apagado, ya no se veían igual de grises los dos
  2. Configuración no tenía salida — se agregó botón "← Volver" (y el mismo patrón a Notificaciones) que regresa al último panel real, guardado en `ultimoPanel`
  3. La campana de notificaciones no hacía nada — ahora abre un panel placeholder "próximamente" (igual patrón que Configuración)
- Probado en local igual que las sesiones anteriores (sin Node/Functions Core Tools): además del chequeo habitual de consola/navegación, se mockeó `window.fetch` desde la consola del navegador para simular las respuestas de `getCatalogoCursosAdmin`/`getResumenCompletitudAdmin`/`listCursosAdmin`/`listTemariosAdmin` y así verificar visualmente los anillos, el drill-down (clic para abrir/cerrar desde ambos disparadores) y el dropdown de vinculación con datos realistas — sin tocar el repo
- **Pendiente para la siguiente sesión:** confirmar en producción con datos reales (Alfredo va a tener que ir a cada curso de Recursos y vincularlo a su Temario Estándar para que el % de completitud tenga sentido — hoy todos los cursos existentes tienen `temarioId` vacío); retomar cuando él quiera la propuesta de renombrar Curso/Temario y la posible pestaña Bitácora

### 2026-09-06 alfredo.pina: Fase 0 v1.2 — topbar, Configuración y rediseño de Productos
- Sesión iterada primero en texto (a pedido explícito de Alfredo: "iteremos primero en texto, pregúntame y propónme mejoras antes de ejecutar") antes de tocar código — varias rondas de preguntas/respuestas, detalle completo de las decisiones en la sección "Estado del proyecto" arriba, no repetido aquí
- Topbar reconstruido de cero: notificaciones + engrane de Configuración + switch de tema tipo píldora + avatar/nombre/correo + ícono de logout, en ese orden — reemplaza el toggle cuadrado y el link de texto "Cerrar sesión" de antes
- Nuevo panel Configuración (solo accesible por el engrane, no vive en el sidebar) con 4 secciones placeholder — se acordó que idioma NO hace falta (sitio 100% en español) y que Accesos eventualmente gestionará el rol de **Viridiana**, la esposa de Alfredo, que será la segunda administradora del panel — dato nuevo para tener en cuenta en cualquier trabajo futuro de roles/permisos
- Tema claro con tinte azul sutil en vez de gris plano
- Dashboard: pestañas rediseñadas al estilo "hoja de Excel" (mismo lenguaje visual que `recursos.html`), pestaña Producto renombrada a Productos y su contenido reconstruido por completo — 4 KPI con tooltip + gráfico de barras por herramienta animado — reemplazando las 6 tarjetas con barra de progreso de v1.1
- **Alcance recortado a propósito por Alfredo al cierre**: los anillos circulares de "% de curso completo" por herramienta, el vínculo Cursos↔Recursos que esos anillos necesitan, y el drill-down al hacer clic en una herramienta quedaron **diseñados y acordados en texto pero sin construir** — Alfredo pidió explícitamente dejarlos para después ("ahorita no lo requerimos pero lo resolveremos después"). Ver el detalle completo (definición de "curso completo", colores del anillo ya elegidos por su deuteranomalía, enfoque del campo `temarioId`) en la sección "Estado del proyecto" arriba — **no rediseñar desde cero en la siguiente sesión, ya está todo decidido, solo falta construir**
- La propuesta de renombrar "Temario Estándar"→"Curso" y "Curso" (Recursos)→"Ficha de Recursos" quedó a medias — Alfredo no la entendió bien en el momento y prefirió posponerla; retomarla con más ejemplos antes de tocar cualquier etiqueta
- Probado en local igual que las sesiones anteriores (sin Node/Functions Core Tools en esta máquina): navegación completa del sidebar, las 4 pestañas del Dashboard, apertura de Configuración y el toggle de tema, todo sin errores de consola; diseño de KPIs y gráfico de barras verificado inyectando datos de ejemplo por consola del navegador (sin tocar el repo)
- **Pendiente para la siguiente sesión:** confirmar en producción que el topbar/tema claro/Dashboard se ven bien con datos reales; retomar los 4 puntos de alcance recortado cuando Alfredo lo pida

### 2026-09-05 alfredo.pina: Fase 0 v1.1 — ajustes de Alfredo tras la primera pasada
- Ajustes rápidos pedidos por Alfredo sobre el Fase 0 recién construido (ver entrada de abajo), mismo día:
  1. Grupo **PRODUCTO** movido al final del sidebar (Comercial → Operación → Cierre → Producto) — es catálogo/soporte, no parte del flujo operativo de una venta, tiene sentido que no encabece
  2. Nuevo placeholder **Tracking** en Operación (junto a Clientes) — Alfredo lo va a usar para el mapa de qué cursos está dando y en qué estatus
  3. Dashboard pasó de 4 secciones apiladas a **4 pestañas reales** (`.dash-tabs`), mismo orden que el sidebar, con texto más grande/con peso — Alfredo pidió resaltar más los nombres de los pilares (Comercial/Operación/Cierre/Producto), que antes eran una etiqueta mono chiquita
  4. Comercial/Operación/Cierre ahora usan el componente `.admin-empty` (icono+título+texto) en vez de la línea gris plana de antes — mismo ajuste de "resaltar el texto"
  5. **Rediseño completo de la pestaña Producto**: Alfredo pidió explícitamente "un diseño que no dé tristeza, que me motive" — se cambió de una lista de filas de texto plano a tarjetas `.dprod-card` con número grande, ícono, color de la paleta de marca por tarjeta y barra de progreso. Decisión de diseño: el número grande destaca lo ya publicado/logrado, no el total, para que se lea como avance
- Sin cambios de backend — la Function `getResumenProductosAdmin` de la sesión anterior se quedó igual, solo cambió cómo se consume/presenta en el HTML
- Probado en local igual que la sesión anterior (sin Node/Functions Core Tools en esta máquina): navegación entre las 9 secciones del sidebar y las 4 pestañas del Dashboard sin errores de consola; diseño de las tarjetas de Producto verificado inyectando datos de ejemplo por consola del navegador (sin tocar el repo) ya que no hay backend local
- Alfredo cerró la sesión llamando a este punto "v1.1" — informal, no hay un número de versión real en el código, es solo la manera en que él referencia esta segunda pasada del día
- **Pendiente para la siguiente sesión:** confirmar en producción que los números reales de las tarjetas de Producto se ven bien (colores/barra de progreso) una vez desplegado; seguir con el diseño a detalle de Fase 1

### 2026-09-05 alfredo.pina: Fase 0 — sidebar en 4 grupos + Dashboard con métricas de Producto
- Ejecuta el prompt de handoff generado en la sesión de teorización (ver entrada de abajo), ajustado en el camino de 3 a 4 grupos (se separó "Cierre" de "Operación") para que coincida con la versión final del roadmap ya documentada en "Modelo del negocio y roadmap estratégico"
- Sidebar de `admin/index.html` reorganizado en 4 secciones visuales — Comercial/Producto/Operación/Cierre — sin tocar ningún `data-panel` ni la lógica de ruteo existente; agregado un ítem placeholder nuevo "Clientes" en Operación (tag "PRONTO", mismo patrón que los demás módulos sin construir) porque hoy ningún módulo vive todavía en ese pilar
- Nueva Function `getResumenProductosAdmin` (v3 clásico, protegida rol admin, mismo patrón de un solo recorrido por tabla que `getCatalogoCursosAdmin`): cuenta cursos con código de descarga y cuántos ya tienen recursos cargados, por herramienta, leyendo las tablas `Cursos`/`Recursos` del módulo Recursos
- Panel Dashboard reescrito con las 4 secciones; Producto trae tarjetas de métrica reales (una llamada a `getCatalogoCursosAdmin` + una a `getResumenProductosAdmin`, sumadas en el cliente), las otras 3 son placeholder sin datos — sin filtros todavía, a propósito
- Probado en local sirviendo el shell estático sin backend (esta máquina no tiene Node/Functions Core Tools instalados): confirmado que el Dashboard carga sin errores de consola (el `fetch` fallido se captura igual que ya hacía el resto del panel) y que la navegación entre todos los paneles, incluido el placeholder nuevo de Clientes, sigue funcionando
- **Pendiente para la siguiente sesión:** el candado real y los números reales del Dashboard solo se pueden confirmar desplegado (mismo patrón ya documentado para `/admin`); seguir con el diseño a detalle de Fase 1 (1.1 Solicitudes o 1.2 Cotizaciones+Cliente, a decidir con Alfredo)

### 2026-09-05 alfredo.pina: Sesión de teorización — modelo de negocio en 3 pilares + roadmap
- Sesión explícitamente sin construcción: Alfredo pidió que Claude actúe como "director de proyecto" (roadmap/estrategia/mockups en texto) en vez de builder por defecto — ver memoria `feedback_rol_director_proyecto`
- Se identificó y documentó el modelo de negocio en 3 pilares (Comercial/Producto/Operación) a partir de que Alfredo notó que ya automatizaba esos roles sin planearlo — detalle completo en la sección nueva "Modelo del negocio y roadmap estratégico" arriba
- Decisiones cerradas: Diagnóstico es herramienta Comercial (no Producto, y va al final de Fase 1 porque necesita diseño de metodología antes de construirse); Cliente = siempre empresa, y nace en Fase 1 (1.2 Cotizaciones), no hasta Fase 2, porque Alfredo ya tiene clientes reales que dar de alta, no todos son leads nuevos; la fase "proyecto práctico" del Grupo es una herramienta de venta, no solo un status; hay 3 instructores externos ocasionales (entidad ligera en Fase 2.1); el bloque Comercial+Operación nuevo va en la misma base Azure SQL Basic que ya paga (~$5 USD/mes) — reutilizarla fue decisión final de Alfredo, sin necesidad de una base aparte
- **Roadmap de fases final** (ver sección "Modelo del negocio y roadmap estratégico" arriba para el detalle — la primera versión que propuso Claude, ordenada por dependencia técnica, se descartó a favor de esta, ordenada por el ciclo de vida de una venta): Fase 0 (sidebar admin + Dashboard) → Fase 1 Comercial (1.1 Solicitudes, 1.2 Cotizaciones+Cliente, 1.3 CRM en admin, 1.4 Diagnósticos) → Fase 2 Operación (2.1 Clientes extendido+Grupos+Alumnos+Instructor, 2.2 tracking de estatus) → Fase 3 Cierre (3.1 Encuestas, 3.2 Diplomas)
- Se generó (no se construyó) un prompt de handoff para una sesión constructora aparte, para ejecutar Fase 0: reorganizar sidebar en 3 grupos visuales + Function nueva `getResumenProductosAdmin` (mismo patrón que `getCatalogoCursosAdmin`, cuenta cursos/recursos del módulo Recursos) + Dashboard con tarjetas reales solo en la sección Producto
- **Pendiente para la siguiente sesión:** confirmar con Alfredo si Fase 0 ya se ejecutó (revisar si el prompt se corrió en otra sesión) antes de seguir con el diseño a detalle de Fase 1 (empezar por 1.1 Solicitudes o 1.2 Cotizaciones+Cliente, a decidir con él)

### 2026-09-05 alfredo.pina: Cursos — módulo nuevo completo (backend + admin + cutover público)
- Sesión larga, todo en una sola pasada, aprobada de antemano con Alfredo vía preguntas explícitas antes de construir: 3 tablas nuevas (`Temas`/`TemariosEstandar`/`Proyectos`), 12 Functions, panel admin nuevo, y reescritura completa de `cursos.html` para las 6 herramientas — ver el detalle completo en "Estado del proyecto" → sección Cursos arriba, no repetido aquí
- Decisiones de producto confirmadas con Alfredo antes de ejecutar: módulo independiente de Recursos (sin fusión de tablas), un Proyecto requiere TODOS sus temas seleccionados (no basta uno), mismo patrón borrador/publicado que Recursos aplicado también a cuándo aparece cada pestaña
- **Sin bugs de plataforma nuevos esta vez** — los 3 ya conocidos de la sesión de Recursos (nombre de ruta no debe empezar con "admin", `context.res` con `body`+`headers` no `jsonBody`, archivo en base64 no binario crudo) se evitaron desde el diseño en vez de descubrirse en producción
- Migrado el contenido real de Excel (9 temas, 2 temarios, 3 proyectos) y Power BI (10 temas, 2 temarios, 10 proyectos) desde `cursos.html` viejo hacia las tablas nuevas, vía script pegado en la consola del navegador logueado en `/admin` — sin pedirle a Alfredo ningún secreto ni connection string, reutilizando su cookie de sesión ya autenticada
- Secuencia de deploy en 2 commits separados a propósito (backend+admin primero, `cursos.html` hasta después de migrar) para que el sitio público nunca se quedara sin el temario real de Excel/Power BI
- Alfredo ya probó el admin en producción cargando contenido real de Power Apps (2 temas + 1 temario + 1 proyecto) antes de que se hiciera el cutover — quedó incluido en el mismo commit del cutover
- **Pendiente para la siguiente sesión (le toca a Alfredo, no requiere a Claude):** terminar de cargar Power Apps, y cargar Power Automate/IA Aplicada/Ofimática desde cero, todo vía admin → Cursos

### 2026-09-04 alfredo.pina: Recursos — backend real, camino de escritura (admin) + 2 bugs de plataforma
- Iterado en texto el diseño del panel antes de construir: qué se configura (cursos por herramienta, materiales, códigos, textos, orden, estado), 3 decisiones de arquitectura confirmadas con Alfredo — subida de archivo por la Function (no SAS, más simple aunque terminó necesitando un ajuste, ver abajo), estado borrador/publicado por curso, y layout del admin espejo del sitio público
- Agregado el campo `estado` (`borrador`/`publicado`) y `orden` a la tabla `Cursos` — `api/getRecursos` ahora exige `estado === "publicado"` además del código, para que un curso a medio armar no sea alcanzable desde el sitio público
- Creadas 9 Functions nuevas (v3 clásico, mismo patrón `body`+`headers` que ya usaba `getRecursos`): `getCatalogoRecursos` (pública) y 8 protegidas con `allowedRoles:["admin"]` — `crearCurso`, `eliminarCurso`, `uploadRecurso`, `editarRecurso`, `eliminarRecurso`, `updateOrden`, `listCursosAdmin`, `getRecursosAdmin`
- `recursos.html` reescrito para pedir `/api/getCatalogoRecursos` al cargar en vez de usar los objetos `TOOLS`/`CURSOS` hardcodeados
- Construido el panel completo en `admin/index.html` → sección Recursos: 3 niveles con SortableJS para drag & drop, formularios inline en vez de modales, más un switch de tema claro/oscuro y mejor contraste de texto (Alfredo no distinguía bien las etiquetas apagadas)
- **Desplegado y depurado en vivo — 2 bugs de plataforma encontrados y corregidos** (detalle completo en "Ya resueltos" arriba):
  1. `adminListCursos`/`adminGetRecursos` regresaban 404 real solo estando autenticado — el nombre empezaba con "admin" y chocaba con la regla `/admin/*`. Se renombraron a `listCursosAdmin`/`getRecursosAdmin`. En el camino también se agregó `Cache-Control: no-store` a todas las respuestas (no era la causa raíz, pero es buena práctica y no sobra)
  2. `uploadRecurso` fallaba al subir cualquier archivo porque el cuerpo binario crudo llegaba como `string`, no `Buffer`, en el runtime de managed functions — se cambió a mandar el archivo en base64 dentro de un JSON normal
  - Ambos se diagnosticaron pidiéndole a Alfredo que revisara la consola del navegador (F12) y probara URLs/`fetch()` puntuales — sin acceso directo a los logs de Azure, esa fue la única forma de ver el error real
- **Confirmado funcionando de punta a punta en producción**: Alfredo subió el manual real (~15MB) al curso `excel-bi` sin problema — cierra también la duda pendiente del límite de tamaño de archivo
- Ronda final de ajustes tras revisión visual de Alfredo en `recursos.html`: contraste de `--text-dim`/`--text-faint` subido **en todo el sitio público** (`assets/css/style.css`, no solo el admin — override explícito de Alfredo a la paleta "cerrada"), corregido el espaciado pegado del título "MANUAL" (tenía `margin-top:0` a propósito para no duplicar el espacio de las pestañas, pero quedaba demasiado pegado), y agregado `Content-Disposition` al subir un archivo para que la descarga conserve el nombre real en vez del nombre interno del blob
- **Pendiente para la siguiente sesión:** publicar el curso `excel-bi` desde el admin (corrigiendo de paso el nombre "Basico-Intermedio" sin acento) para que aparezca en `recursos.html`, volver a subir los manuales de prueba para que les aplique el nombre de descarga correcto, y probar el resto de tipos de recurso (Casos, Plantillas, Contenido Complementario) y el reordenamiento por drag & drop

### 2026-09-04 alfredo.pina: Recursos — backend real, camino de lectura
- Creada la Function `api/getRecursos` (v3 clásico, pública): recibe herramienta + curso + código, valida el código contra la tabla `Cursos` de Azure Table Storage y, si coincide, regresa los recursos de la tabla `Recursos` agrupados por tipo (manuales, casos, plantillas, skills, extra)
- Creado `api/src/recursos-tables.js` — cliente compartido de Table Storage (reutilizable por las Functions de administración que faltan)
- `recursos.html` reescrito para pedir los datos a `/api/getRecursos` en vez de usar el objeto `CURSOS` hardcodeado — ya no expone el código ni el contenido en el código fuente. Los datos ya desbloqueados se cachean en `sessionStorage` por curso (para no repetir la llamada al cambiar de pestaña)
- Skills ahora usa botón "Copiar" (al portapapeles) en vez de "Descargar", según lo acordado con Alfredo
- Agregado `RECURSOS_STORAGE_CONNECTION` a `api/local.settings.json.example`
- Alfredo creó el Storage Account (`apcwebrecursos`), el contenedor `recursos` y las tablas `Cursos`/`Recursos` desde el portal, guiado paso a paso (primera vez que usa ese tipo de recurso) — ver el checklist completo en la sección de arriba
- Bug encontrado y corregido en el camino: `context.res.jsonBody` no se serializaba en este runtime (regresaba 200 con cuerpo vacío) — se corrigió a `body` + `headers` explícitos, igual que `getAvailability`
- **Confirmado funcionando de punta a punta en producción**: se creó una entidad de prueba en `Cursos` a mano desde el portal y se desbloqueó un curso real en `recursos.html` contra el Storage real (recursos vacíos porque la tabla `Recursos` todavía no tiene contenido — esperado)
- Pendiente para la siguiente sesión: la parte de administración (subir archivos, crear curso, reordenar, rotar código) en `admin/index.html` → sección Recursos, más las Functions `uploadRecurso`, `crearCurso`, `updateOrden`, `gestionarCodigo`

### 2026-09-04 alfredo.pina: Backoffice — base de login con Entra ID (arranque Fase 2)
- Creado `staticwebapp.config.json` protegiendo `/admin` y `/admin/*` con el rol `admin` (redirige a login de Microsoft si no hay sesión)
- Creada la Function `api/GetRoles` (v3 clásico) que le da el rol `admin` solo a los correos en `ADMIN_EMAILS` — hoy solo `alfredo.pina@lifezen.com.mx`, fácil de ampliar después
- Construido el shell de `admin/index.html`: menú lateral con los 7 módulos del backoffice (Dashboard funcional con bienvenida, el resto "Próximamente"), correo de sesión + cerrar sesión en la barra superior
- Corregido un bug de overlap en el topbar del admin en pantallas angostas (le faltaba `flex-wrap` y truncar el texto largo de depuración de `/.auth/me`)
- **Incidente:** ese primer push (commit `91b4fd2`) tumbó todo el sitio en producción — el bloque `auth.rolesSource` de `staticwebapp.config.json` requiere el plan Standard y este sitio está en Free, así que Azure rechazó el deploy completo (no solo `/admin`). Diagnosticado vía GitHub Actions (el log de "Build And Deploy" tiene el error exacto) y corregido en el commit `fc3fc80`: se quitó el bloque `auth` y se borró `api/GetRoles/`. La asignación del rol `admin` se mueve a invitación manual desde el portal de Azure (ver arriba).
- Alfredo generó y aceptó su invitación de rol `admin` — **`/admin` confirmado funcionando en producción**, protegido y accesible con su cuenta de M365

### 2026-09-04 alfredo.pina: Recursos — ajustes de UX tras primera revisión
- Quitado el eyebrow "RECURSOS" del título (alineado con `agenda.html`/`cursos.html`, que van directo al `h2`), título cambiado a "Descarga los recursos habilitados para tu próximo curso.", y el breadcrumb ahora muestra una línea descriptiva en el nivel raíz y se vuelve funcional al entrar a una herramienta
- Candado: ícono en línea con el texto (antes apilado), animación real al desbloquear (botón "Validando…" con spinner → "Acceso concedido" → transición con fade/slide a los recursos) y mensaje de error con guiño sarcástico + shake + cuenta regresiva animada "3…2…1"
- Reestructurados los recursos por curso: Manual (ahora arreglo, por si algún día hay más de uno), Casos Prácticos (ícono en vez de pill numerado, con botón de descarga), y dos secciones nuevas — Plantillas y Skills (prompts de IA) — todas comparten un mismo componente de tarjeta (`renderResGrid`) para no duplicar código
- Contenido Complementario (antes "Recursos Extra") ahora aparece junto con el footer real del sitio en vez de un botón aislado de YouTube — se agregó el link de YouTube (placeholder) al `contact-meta` del footer en las 4 páginas que lo tienen (`index.html`, `cursos.html`, `agenda.html`, `recursos.html`)
- Agregado un manual real de prueba (PDF de Excel Intermedio-Avanzado, proporcionado por Alfredo) en `assets/recursos/excel/intermedio-avanzado/manual.pdf`, conectado al botón de descarga de ese curso — verificado que descarga correctamente (200, tamaño exacto)
- `sessionStorage` para el desbloqueo se dejó igual (Alfredo confirmó que el comportamiento actual le parece bien)
- Corregido bug de espaciado: el breadcrumb quedaba pegado al título (`h2.section-title` sin margen + `.breadcrumb` sin `margin-top`) en las 3 vistas de `recursos.html` — agregado `margin-top:16px` al breadcrumb, mismo valor que usa `.section-sub` en el resto del sitio

### 2026-09-04 alfredo.pina: Recursos — frontend de prueba (Fase 1)
- Creado `recursos.html`: biblioteca de descargas con navegación en 3 niveles (Herramientas → Curso → Recursos), reutilizando el componente `.course-card`/`.courses-grid` de `index.html` para el nivel 1, y una nueva pestaña estilo "hoja de Excel" (`.excel-tabs`) para elegir entre los 2 cursos de cada herramienta
- Alcance de prueba: solo Excel y Power BI funcionales; Power Apps, Power Automate, IA Aplicada y Ofimática muestran el mismo patrón "PRÓXIMAMENTE" que ya existe en `cursos.html`
- Candado de acceso por curso con la barra `fx =DESBLOQUEAR("código")` (mismo lenguaje visual que `agenda.html`) — código hardcodeado en el JS de la página, sin backend todavía (decisión explícita para esta fase, ver nota en Estado del proyecto)
- Agregada la liga "Recursos" al `nav-links` de `index.html` y `cursos.html` (visible, sin ocultar — decisión de Alfredo: el sitio todavía no se comparte y corre en dominio de prueba)
- Backend (Blob/Table Storage, Azure Functions) y panel `/admin` quedaron fuera de esta sesión a propósito — ver `prompt-code-recursos-y-backoffice.md` para el alcance completo cuando se retome
- Probado el flujo completo en navegador (desbloqueo correcto, código incorrecto, cambio de pestaña, tarjetas bloqueadas) sirviendo el repo con un servidor estático local temporal (no se agregó al proyecto)

### 2026-09-03 alfredo.pina: Limpieza, favicon y bug
- Clonado el repo por primera vez en el equipo de Alfredo y agregado este `CLAUDE.md` al repo
- Eliminado el símbolo SVG fantasma `pineappleMark` (definido pero nunca usado) de `index.html` y `cursos.html`
- Agregado favicon (ícono "fx", mismo motivo visual del sitio) en 16x16, 32x32 y apple-touch-icon, enlazado en `index.html`, `cursos.html` y `agenda.html`
- Agregadas meta tags Open Graph básicas (`og:title`, `og:description`, `og:image`, `og:url`, `og:type`) en `index.html` y `cursos.html`
- Creado `.gitignore` (protege sobre todo `api/local.settings.json`, que puede tener secretos)
- Corregido bug de scroll: los links del menú aterrizaban con el título de la sección tapado por las barras sticky (nav + barra "Conóceme" debajo) — ajustado `scroll-margin-top` con un valor distinto por breakpoint en `style.css`

